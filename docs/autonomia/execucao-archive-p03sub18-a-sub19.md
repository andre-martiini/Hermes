# Arquivo de execução — P03 sub-entregas 18/N e 19/N

Blocos movidos de docs/autonomia/execucao.md em 22/09/2026, pela tarefa agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega P03 20/N — o arquivo ativo passaria de ~50KB, no limite já estabelecido para re-arquivamento, sem re-arquivamento desde o décimo quinto arquivo de arquivo (execucao-archive-p03sub16-a-sub17.md). Nenhum conteúdo foi alterado — relocação, não edição. Cobre: P03 sub-entrega 18/N (idempotentHint, terceira fatia de 9 tools) e P03 sub-entrega 19/N (idempotentHint, quarta fatia de 9 tools).

---

```yaml
plano: plano-hermes-autonomo-2026-09-06
base_commit: 185181fff3a61ea193217b9c3b634ecf51df441b
pacote: "P03 sub-entrega 18/N -- idempotentHint por handler (passo 3 do plano), terceira fatia: campo `idempotencia` classificado por leitura direta do handler real para 9 tools (3 IDEMPOTENTE, 6 NAO_IDEMPOTENTE) -- objetivos estrategicos (strategy_tools.py, sem nenhum teste dedicado antes desta sub-entrega), Modo Secretario self-service e contato prioritario (secretario_whatsapp.py), e ingestao de anexo (tools/anexar_arquivo.py) -- escolhida entre as opcoes deixadas pela sub-entrega 17/N (mais uma fatia de idempotentHint era \"o padrao incremental mais estabelecido\"); duas tools do mesmo modulo estrategico investigadas e deliberadamente deixadas sem classificacao (excluir_objetivo_estrategico, gerenciar_item_estrategico), mesmo criterio de revogar_promocao_autonomia (sub-entrega 16/N)"
estado: validado
inicio: "2026-09-15T14:10:00Z"
fim: "2026-09-15T15:08:25Z"
arquivos_alterados:
  - "functions/tools/inventory.py (9 classificacoes novas: IDEMPOTENTE -- desativar_modo_secretario (reset deterministico de singleton sem campo variavel), cancelar_contato_prioritario_secretario (.update() sem checar status atual, converge para CANCELADO), editar_objetivo_estrategico (.update() deterministico por objetivo_id, sem append); NAO_IDEMPOTENTE -- ativar_modo_secretario (desativa_em recalculado a partir de 'agora', estende prazo), preparar_contato_prioritario_secretario (valido_ate recalculado + reset total da conversa vinculada a cada chamada), remover_anexo (ArrayUnion com timestamp novo + erro na repeticao pos-remocao), criar_objetivo_estrategico (ID automatico do Firestore a cada chamada), preparar_upload (token aleatorio novo a cada chamada), anexar_arquivo (arquivo novo no Drive + item novo no pool a cada chamada); notas de investigacao (sem classificar) em excluir_objetivo_estrategico e gerenciar_item_estrategico)"
  - "functions/tools/registry.py (docstring de mcp_annotations() atualizado: contagem 18->27 de ~59, ~41->~32 restantes, nova secao descrevendo a fatia 18/N incluindo os dois casos deliberadamente nao classificados)"
  - "functions/test_mcp_annotations.py (as 9 tools novas acrescentadas as listas parametrizadas de IDEMPOTENTE/NAO_IDEMPOTENTE; contagem do docstring do modulo corrigida de '9 das ~59' para '27 das ~59' -- estava desatualizada desde a sub-entrega 17/N, achado incidental nesta sub-entrega, nao um novo erro introduzido por ela)"
  - "functions/test_secretario_whatsapp.py (+4 testes: test_ativar_modo_secretario_repetir_com_duracao_estende_desativa_em, test_desativar_modo_secretario_repetir_e_idempotente, test_cancelar_contato_prioritario_repetir_e_idempotente, test_preparar_contato_prioritario_repetir_estende_valido_ate -- cada um prova por handler real, com _agora_sp/agora_sp controlados, a classificacao correspondente)"
  - "functions/test_strategy_tools.py (NOVO -- strategy_tools.py nao tinha nenhum teste dedicado antes desta sub-entrega, achado real: mesma classe de gap ja vista em claim_action_dedup_slot na sub-entrega 16/N. 3 testes com fake Firestore minimo proprio: repetir criar_objetivo_estrategico cria dois objetivos distintos; repetir editar_objetivo_estrategico produz os mesmos valores de negocio; repetir nao acumula nem duplica diretrizes)"
decisoes:
  - id: p03-sub18-escolha-continuar-idempotentHint-em-vez-de-p04
    motivo: "sub-entrega 17/N deixou a decisao a cargo da execucao autonoma: (a) mais uma fatia de idempotentHint (padrao incremental mais estabelecido), (b) fatia envelope, (c) novo levantamento de outputSchema, ou (d) perguntar ao Andre sobre P04. Escolhido (a) -- mesmo padrao incremental ja usado 2x no mesmo dia (16/N, 17/N), sem necessidade de investigacao exploratoria nova, e ainda ha 32 tools elegiveis restantes. P04 (pedidos duraveis/lease/ledger) e um pacote muito maior que uma sub-entrega pequena e revisavel comportaria de uma vez -- decisao de trocar de pacote fica melhor a cargo do Andre quando ele revisar esta PR, nao decidida unilateralmente por esta execucao."
    autoridade: existente_ou_nova
  - id: p03-sub18-escopo-9-de-41-restantes-priorizando-modulos-sem-teste
    motivo: "das ~41 tools restantes apos a sub-entrega 17/N, priorizado um lote de 9 com evidencia solida e sem ambiguidade genuina, incluindo deliberadamente strategy_tools.py (zero testes antes desta sub-entrega) e secretario_whatsapp.py/tools/anexar_arquivo.py (ja tinham suite de testes robusta reaproveitavel, reduzindo risco de infra nova). Mesmo padrao incremental ja usado nas sub-entregas 16/N e 17/N."
    autoridade: existente_ou_nova
  - id: p03-sub18-excluir-objetivo-estrategico-sem-classificacao-mesmo-criterio-revogar-promocao
    motivo: "ref.delete() em si nao tem efeito adicional se repetido (Firestore), mas carregar_objetivo_estrategico (fail-closed) barra a segunda chamada ANTES de chegar em delete(), devolvendo status=error/objetivo_nao_encontrado em vez de um 'ja excluido' gracioso -- mesma ambiguidade resposta-muda-mas-ambiente-nao ja aceita para revogar_promocao_autonomia (sub-entrega 16/N). Revisao adversarial desta sub-entrega notou que, sob leitura literal da definicao (efeito no AMBIENTE, nao na resposta), a tool poderia ser classificada IDEMPOTENTE -- mantido conservador por consistencia com o precedente ja estabelecido; nao bloqueante, registrado para o Andre como ponto de discussao."
    autoridade: existente_ou_nova
  - id: p03-sub18-gerenciar-item-estrategico-sem-classificacao-comportamento-multi-modal
    motivo: "o comportamento de idempotencia depende do parametro `acao` interno a esta unica tool: adicionar gera ID novo a cada chamada (NAO_IDEMPOTENTE), editar sobrescreve por item_id (IDEMPOTENTE), remover erra na segunda chamada (mesma ambiguidade de revogar_promocao_autonomia), concluir regrava dataConclusao com timestamp novo a cada chamada (NAO_IDEMPOTENTE). Um hint unico nao descreveria os quatro ramos honestamente -- 'um hint errado e pior que a omissao' (docstring de mcp_annotations)."
    autoridade: existente_ou_nova
  - id: p03-sub18-rodada1-sem-achado-parada
    motivo: "1a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto da implementacao): verificou as 9 classificacoes linha a linha contra o codigo real dos 3 modulos tocados, rodou os testes novos e alterados, e stress-testou empiricamente o caso mais arriscado de gerenciar_item_estrategico (migracao de itens legados em formato string dentro da transacao) para confirmar que a nota de nao-classificacao esta correta, mesmo sem classificar a tool. Nao achou nada -- nenhuma classificacao incorreta, nenhum teste vacuo, nenhuma conta errada nos totais do docstring. Unico ponto levantado foi a discussao nao-bloqueante sobre excluir_objetivo_estrategico (ver decisao acima). Criterio de parada atingido na primeira rodada -- relatado aqui como resultado valido, sem inventar achado para parecer completo."
    autoridade: existente_ou_nova
testes:
  comandos:
    - "python3 -m venv + pip install -r requirements.txt (ambiente limpo, clonado publicamente via git clone do repositorio)"
    - "python3 -m unittest test_strategy_tools test_secretario_whatsapp test_mcp_annotations test_tool_inventory test_anexar_arquivo -v (arquivos isolados, repetido apos a rodada de revisao)"
    - "python3 -m unittest discover -s . -p 'test_*.py' -q (suite completa, baseline antes da implementacao e depois da revisao)"
  resultados:
    - "Baseline (origin/main, commit 185181f, PR #271/#272 ja mescladas): 1913/1913, 0 falhas, 0 erros."
    - "Arquivos isolados apos a implementacao completa: 129/129, 0 falhas, 0 erros (inclui os 3 novos de test_strategy_tools.py + os 4 novos de test_secretario_whatsapp.py, entre outros ja existentes)."
    - "Suite completa apos toda a implementacao: 1920/1920, 0 falhas, 0 erros, sem regressao (1913 baseline + 7 novos)."
evidencias:
  - "1 rodada de revisao adversarial independente (Agent tool, general-purpose, sem contexto da implementacao) -- ver decisoes para o relato completo. Nao achou nada de errado; parada aqui, conforme a disciplina (escalar so enquanto uma rodada acha algo real)."
  - "Sanity check do PR: changed_files=5, additions=336, deletions=17 no retorno de argos_criar_pr_repositorio -- bate exatamente com os 5 arquivos deliberadamente alterados (inventory.py, registry.py, test_mcp_annotations.py, test_secretario_whatsapp.py, test_strategy_tools.py [novo]) e com o `git diff --stat` local capturado antes do envio, nenhum arquivo extra ou nao intencional no diff."
  - "Os 5 arquivos enviados via mcp__Argos__argos_escrever_arquivo_repositorio (delegado a um subagente dedicado, com as mesmas instrucoes de integridade desta skill) com o `sha` retornado por cada escrita comparado contra `git hash-object` local -- 4 bateram de primeira; 1 (test_secretario_whatsapp.py) teve mismatch no primeiro envio por uma quebra de linha final perdida no caminho de escrita (diagnosticado via `git show origin/<branch>:<path> | tail -c 20 | od -c` contra o local), corrigido e reenviado com sucesso na segunda tentativa. Verificacao adicional, independente do proprio retorno do Argos: `git fetch` do branch remoto + `git diff` contra `origin/<branch>` para os 5 arquivos -- diff vazio, bytes identicos."
  - "PR #273 aberta (https://github.com/andre-martiini/Hermes/pull/273), com o relato completo da revisao adversarial e dos testes no corpo. AINDA NAO MESCLADA no momento do registro deste diario -- aguardando revisao e merge manual do Andre."
migracao:
  dry_run: null
  executada: false
flags:
  antes: {}
  depois: {}
pendencias:
  - "ABERTA: PR #264 (aberta por engano durante a correcao do diario da sub-entrega 14/N) continua aberta com um bug real de escaping YAML -- deve ser fechada sem mesclar pelo Andre; nao ha tool de fechamento de PR disponivel via Argos."
  - "ABERTA: PR #273 (esta sub-entrega) aguardando revisao e merge do Andre."
  - "ABERTA, deliberadamente: idempotentHint para as ~32 tools de escrita/leitura_e_escrita restantes continua fora de escopo -- proxima fatia natural. excluir_objetivo_estrategico e gerenciar_item_estrategico foram investigadas e deliberadamente deixadas sem classificacao por ambiguidade genuina (ver decisoes) -- excluir_objetivo_estrategico e candidata a revisitar se o Andre concordar com a leitura literal (ambiente nao muda, so a resposta) discutida na revisao adversarial."
  - "Pendencias ja registradas em blocos anteriores e nao tocadas por esta sub-entrega continuam abertas: ver blocos P03 sub-entrega 16/N e 17/N acima para a lista completa (religar inventario tipado a decisao de politica real; ambiguidades de classificacao; card do Telegram de rascunho degradado; observabilidade de claim pendente; TTL do Firestore para mcp_jobs; limite de 200KB do Argos; passo 2 do P02 como hardening futuro; inconsistencias nao-bloqueantes de sub-entregas 7/N, 10/N, 12/N, 13/N; salvar_memoria_global sem teste de handler dedicado; caveat de concorrencia em registrar_saude/consultar_investimentos; outputSchema para as ~99 tools restantes do catalogo)."
proximo_pacote: "P03 -- PR #273 (idempotentHint, terceira fatia de 9 tools) aberta, aguardando revisao e merge do Andre. A proxima execucao agendada deve primeiro confirmar se a PR #273 foi mesclada (e registrar essa confirmacao no proximo bloco, mesmo padrao ja usado para as sub-entregas 16/N e 17/N) antes de decidir o proximo passo: (a) mais uma fatia de idempotentHint para as ~32 tools de escrita restantes; (b) a fatia envelope restante do passo 3 do P03; (c) um levantamento NOVO do catalogo para retomar outputSchema; ou (d) perguntar ao Andre se prefere avancar direto para P04 (pedidos duraveis/lease/ledger) -- com tres fatias de idempotentHint entregues (27 de 59 tools) e outputSchema com sete tools entregues, a opcao (d) continua cada vez mais razoavel a cada sub-entrega que passa dentro do passo 3 do P03. A cargo da execucao autonoma agendada, salvo nova prioridade do Andre. NOTA DE PROCESSO: PR #264 continua aberta e precisa ser fechada manualmente pelo Andre (sem tool de fechamento via Argos) -- ver pendencias."
```

---

```yaml
plano: plano-hermes-autonomo-2026-09-06
base_commit: 266d7b5e4480b843c0e508835d908c47db63452c
pacote: "P03 sub-entrega 19/N -- idempotentHint por handler (passo 3 do plano), quarta fatia: campo `idempotencia` classificado por leitura direta do handler real para mais 9 tools (3 IDEMPOTENTE, 6 NAO_IDEMPOTENTE), chegando a 36 de ~59 tools elegiveis -- confirmado no inicio desta execucao que a PR #273 (sub-entrega 18/N) ja tinha sido mesclada pelo Andre (commit 8c2143e em main) e que o diario da propria sub-entrega 18/N (PR #274) tambem ja estava mesclado (commit 266d7b5 em main), entao esta fatia partiu de main atualizado, sem branch pendente; escolhida entre as opcoes deixadas pela sub-entrega 18/N -- mais uma fatia de idempotentHint, o padrao incremental mais estabelecido, em vez de perguntar ao Andre sobre P04 nesta execucao autonoma sem supervisao direta"
estado: pronto_para_revisao
inicio: "2026-09-15T17:15:00Z"
fim: "2026-09-15T18:23:00Z"
arquivos_alterados:
  - "functions/tools/inventory.py (9 classificacoes novas: IDEMPOTENTE -- salvar_pop_global (dedup por titulo OU gatilho normalizado antes de escrever, `.set(merge=True)` no mesmo doc), atualizar_personalidade (singleton `system/copilot_soul` via `.set(merge=True)`), resolver_conflito_memoria (os dois ramos de decisao escrevem por `memoria_id` ja conhecido, nunca ID novo); NAO_IDEMPOTENTE -- registrar_correcao_procedimento (uuid4 novo + `.set()` incondicional, sem dedup), resolver_conflito_procedimento (com confirmar_contrato=True cria documento novo com uuid4 a cada chamada, mesmo ja arquivado), editar_plano_acao (ArrayUnion incondicional em acompanhamento a cada chamada, mesmo padrao de editar_acao), gerar_relatorio (report_id=uuid4 + `.set()` incondicional, sem dedup por titulo/contexto), gerar_imagem (nome de blob com uuid4 a cada chamada + incremento de cota via check_and_increment_limit), criar_rascunho_whatsapp (ID automatico + `.set()` incondicional, sem dedup por destinatario/mensagem/motivo); mais 5 tools investigadas e deliberadamente deixadas SEM classificacao por ambiguidade genuina, mesmo criterio de revogar_promocao_autonomia/gerenciar_item_estrategico -- decidir_elevacao e decidir_promocao_autonomia (falham-fechado na repeticao mas devolvem so {ok:False,erro:texto livre}, sem status estruturado tipo 'ja decidido'), confirmar_acao (idempotencia depende da tool delegada em cada chamada), mutar_portal_compras_publico e mutar_lista_compras (multiplos ramos por parametro `acao` interno, pelo menos um ramo -- toggle -- violando idempotencia por definicao)"
  - "functions/tools/registry.py (docstring de mcp_annotations() atualizado: contagem 27->36 de ~59 tools investigadas, nova secao descrevendo a fatia 19/N incluindo o achado metodologico do handler real e as 5 tools deixadas ambiguas; contagem de tools nao investigadas atualizada para ~15, mais 8 investigadas-e-ambiguas no total)"
  - "functions/test_telegram_extended.py (NOVO -- tools/telegram_extended.py nao tinha nenhum teste de HANDLER dedicado antes desta sub-entrega, so cobertura de validacao de argumentos em outros arquivos; mesma classe de gap ja vista em strategy_tools.py na sub-entrega 18/N. 10 testes com o fake de Firestore com estado real ja usado e testado por test_outbox_aprovacao.py (`_MockDb`, reaproveitado em vez de reimplementado): salvar_pop_global (dedup por titulo e por intersecao de gatilhos), atualizar_personalidade (singleton), resolver_conflito_memoria (os dois ramos de decisao), resolver_conflito_procedimento (preview vs. escrita real, duas chamadas geram dois documentos distintos), editar_plano_acao (ArrayUnion incondicional a cada chamada), gerar_relatorio (uuid novo a cada chamada, Gemini mockado via generate_content_logged))"
  - "functions/test_hermes_tools.py (+2 testes: registrar_correcao_procedimento -- duas chamadas geram dois IDs distintos --, gerar_imagem -- dois nomes de blob distintos e check_and_increment_limit chamado a cada vez, nao so na primeira)"
  - "functions/test_outbox_aprovacao.py (+1 teste: criar_rascunho_whatsapp -- repetir a mesma chamada cria dois rascunhos distintos e dispara dois cards no Telegram, sem dedup)"
  - "functions/test_mcp_annotations.py (contagem do docstring do modulo atualizada 27->36; listas parametrizadas IDEMPOTENTE/NAO_IDEMPOTENTE estendidas com as 9 tools novas; 2 testes que usavam criar_rascunho_whatsapp como exemplo de tool 'ainda nao investigada' corrigidos para usar schedule_whatsapp_message, que continua sem classificacao; assert exato de criar_rascunho_whatsapp atualizado para incluir idempotentHint:false)"
decisoes:
  - id: p03-sub19-continuar-idempotenthint-em-vez-de-perguntar-sobre-p04
    motivo: "sub-entrega 18/N deixou a decisao explicitamente a cargo da execucao autonoma agendada: (a) mais uma fatia de idempotentHint, (b) fatia envelope, (c) novo levantamento de outputSchema, ou (d) perguntar ao Andre sobre P04 -- com a nota de que (d) 'continua cada vez mais razoavel a cada sub-entrega que passa'. Escolhido (a) mais uma vez: e uma sessao agendada sem supervisao direta (ver <unattended_operation>), e o padrao incremental de idempotentHint segue solido, de baixo risco e com 23 tools elegiveis ainda por investigar -- nao ha decisao irreversivel nem ambiguidade que justifique parar e esperar o Andre decidir. A troca de pacote (P03->P04) e uma decisao de escopo maior, mais bem tomada pelo Andre ao revisar esta PR do que unilateralmente por uma execucao agendada.\n"
    autoridade: existente_ou_nova
  - id: p03-sub19-handler-real-e-telegram-extended-nao-main-closure
    motivo: "achado ANTES mesmo da revisao adversarial (nao um erro corrigido depois, mas uma verificacao feita durante a propria investigacao): para 6 das 9 tools desta fatia (salvar_pop_global, resolver_conflito_memoria, atualizar_personalidade, resolver_conflito_procedimento, editar_plano_acao, gerar_relatorio), main.py tem uma closure homonima usada pelo canal 'copiloto web', mas o servidor MCP (mcp_server.py -> tools/hermes_tools.py::execute -> _TELEGRAM_TOOLS/_via_telegram) despacha para tools/telegram_extended.py::execute, uma implementacao SEPARADA e independente (confirmado por comentario explicito no proprio codigo-fonte: 'esta funcao e independente da salvar_pop_global embutida em main.py'). A classificacao e os testes desta sub-entrega leem e exercitam o codigo de tools/telegram_extended.py, nao o de main.py -- analisar o arquivo errado teria invalidado 6 das 9 classificacoes.\n"
    autoridade: existente_ou_nova
  - id: p03-sub19-escopo-9-de-32-restantes-priorizando-evidencia-solida
    motivo: "das 32 tools de escrita/leitura_e_escrita deixadas pendentes pela sub-entrega 18/N, um agente de levantamento dedicado investigou 22 classificacoes com evidencia solida (leitura direta de handler) entre 29 tools candidatas -- mais que o suficiente para uma fatia. Escolhido um lote de 9 priorizando diversidade de modulos (memoria_e_procedimentos, acoes_tarefas, utilitario, whatsapp) e evidencia sem ambiguidade genuina, deixando os grupos coesos restantes (o trio aprovar/descartar_rascunho_whatsapp + consumir/consultar/solicitar_autorizacao_argos, e o grupo confirmar_edicao_em_lote/editar_acoes_em_lote/confirmar_reagendamento_em_lote/reagendar_acoes_em_lote) para fatias futuras -- mesmo padrao incremental ja usado nas sub-entregas 16/N-18/N.\n"
    autoridade: existente_ou_nova
  - id: p03-sub19-rodada1-achado-erro-de-contagem-no-docstring
    motivo: "1a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto da implementacao): confirmou a alegacao central sobre o handler real (telegram_extended.py, nao main.py) lendo a cadeia de dispatch ponta a ponta, verificou as 9 classificacoes linha a linha contra o codigo real, e achou 1 problema real -- o docstring de tools/registry.py dizia 'para 5 das 9 tools' quando a lista de nomes logo depois trazia 6 nomes, nao 5 (erro de contagem/digitacao, sem impacto funcional: tools/inventory.py em si estava correto e consistente). Corrigido de 5 para 6.\n"
    autoridade: existente_ou_nova
  - id: p03-sub19-rodada2-sem-achado-parada
    motivo: "2a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto de nenhuma rodada anterior): releu o diff inteiro do zero, confirmou que a correcao 5->6 e numerica e gramaticalmente consistente com o resto do paragrafo, re-verificou as 9 classificacoes e as 5 notas de ambiguidade linha a linha contra o codigo real, recontou os totais do docstring (36 classificadas, 8 investigadas-e-ambiguas, 15 nao investigadas, soma 59), e rodou a suite completa dos arquivos de teste tocados e a suite inteira. Nao achou nada novo. Criterio de parada atingido -- relatado aqui como resultado valido, sem inventar achado para parecer completo.\n"
    autoridade: existente_ou_nova
testes:
  comandos:
    - "python3 -m venv + pip install -r requirements.txt (ambiente limpo, clonado publicamente via git clone do repositorio)"
    - "python3 -m unittest test_telegram_extended test_hermes_tools test_outbox_aprovacao test_mcp_annotations test_tool_inventory -v (arquivos isolados, repetido apos cada rodada de revisao)"
    - "python3 -m unittest discover -s . -p 'test_*.py' -q (suite completa, baseline antes da implementacao e depois de cada rodada de revisao)"
  resultados:
    - "Baseline (origin/main, commit 266d7b5, PR #273 e #274 ja mescladas): 1920/1920, 0 falhas, 0 erros."
    - "Arquivos isolados apos a implementacao completa + as 2 rodadas de revisao/correcao: 208/208, 0 falhas, 0 erros (test_telegram_extended [novo, 10 testes] + test_hermes_tools + test_outbox_aprovacao + test_mcp_annotations + test_tool_inventory)."
    - "Suite completa apos toda a implementacao e as 2 rodadas de revisao: 1933/1933, 0 falhas, 0 erros, sem regressao (1920 baseline + 13 novos)."
evidencias:
  - "2 rodadas de revisao adversarial independente (Agent tool, general-purpose, cada uma sem contexto da implementacao ou de rodadas anteriores) -- ver decisoes para o relato completo. Rodada 1 achou 1 problema real (erro de contagem '5 das 9' -> '6 das 9' no docstring de tools/registry.py, sem impacto funcional); rodada 2 nao achou nada -- parada aqui, conforme a disciplina (escalar so enquanto uma rodada acha algo real)."
  - "Sanity check do PR: changed_files=6, additions=555, deletions=25 no retorno de argos_criar_pr_repositorio -- bate exatamente com os 6 arquivos deliberadamente alterados (inventory.py, registry.py, test_telegram_extended.py [novo], test_hermes_tools.py, test_outbox_aprovacao.py, test_mcp_annotations.py) e com o `git diff --cached --stat` local capturado antes do envio, nenhum arquivo extra ou nao intencional no diff."
  - "Os 6 arquivos enviados via mcp__Argos__argos_escrever_arquivo_repositorio (delegado a um subagente dedicado, com as mesmas instrucoes de integridade desta skill) com o `sha` retornado por cada escrita comparado contra `git hash-object` local -- 5 bateram de primeira; 1 (tools/inventory.py) teve mismatch no primeiro envio (diferenca de contagem de linhas em branco entre duas funcoes de nivel superior no fim do arquivo, nao conteudo), diagnosticado e corrigido na segunda tentativa. Verificacao adicional, independente do proprio retorno do Argos: `git fetch` do branch remoto + `git diff` contra `origin/<branch>` para os 6 arquivos -- diff vazio, bytes identicos."
  - "PR #275 aberta (https://github.com/andre-martiini/Hermes/pull/275), com o relato completo das 2 rodadas de revisao e dos testes no corpo. AINDA NAO MESCLADA no momento do registro deste diario -- aguardando revisao e merge manual do Andre."
migracao:
  dry_run: null
  executada: false
flags:
  antes: {}
  depois: {}
pendencias:
  - "ABERTA: PR #264 (aberta por engano durante a correcao do diario da sub-entrega 14/N) continua aberta com um bug real de escaping YAML -- deve ser fechada sem mesclar pelo Andre; nao ha tool de fechamento de PR disponivel via Argos."
  - "ABERTA: PR #275 (esta sub-entrega) aguardando revisao e merge do Andre."
  - "ABERTA, deliberadamente: idempotentHint para as ~23 tools de escrita/leitura_e_escrita restantes continua fora de escopo -- proxima fatia natural, incluindo dois grupos coesos ja identificados (aprovar/descartar_rascunho_whatsapp + consumir/consultar/solicitar_autorizacao_argos; confirmar_edicao_em_lote/editar_acoes_em_lote/confirmar_reagendamento_em_lote/reagendar_acoes_em_lote). decidir_elevacao, decidir_promocao_autonomia, confirmar_acao, mutar_portal_compras_publico e mutar_lista_compras foram investigadas e deliberadamente deixadas sem classificacao por ambiguidade genuina (ver decisoes) -- decidir_elevacao/decidir_promocao_autonomia sao candidatas a revisitar se o Andre concordar com a leitura literal (ambiente nao muda, so a resposta) ja discutida para revogar_promocao_autonomia/excluir_objetivo_estrategico."
  - "ABERTA, deliberadamente: mutar_lista_compras teve so 2 dos 4 ramos internos (criar/atualizar) investigados com confianca; remover/import_batch/limpar_planejamento nao foram aprofundados nesta sub-entrega."
  - "Pendencias ja registradas em blocos anteriores e nao tocadas por esta sub-entrega continuam abertas: ver blocos P03 sub-entrega 16/N, 17/N e 18/N (arquivados ou acima) para a lista completa (religar inventario tipado a decisao de politica real; ambiguidades de classificacao; card do Telegram de rascunho degradado; observabilidade de claim pendente; TTL do Firestore para mcp_jobs; limite de 200KB do Argos; passo 2 do P02 como hardening futuro; inconsistencias nao-bloqueantes de sub-entregas 7/N, 10/N, 12/N, 13/N; salvar_memoria_global sem teste de handler dedicado; caveat de concorrencia em registrar_saude/consultar_investimentos; outputSchema para as ~99 tools restantes do catalogo)."
proximo_pacote: "P03 -- PR #275 (idempotentHint, quarta fatia de 9 tools) aberta, aguardando revisao e merge do Andre. A proxima execucao agendada deve primeiro confirmar se a PR #275 foi mesclada (e registrar essa confirmacao no proximo bloco, mesmo padrao ja usado para as sub-entregas 16/N-18/N) antes de decidir o proximo passo: (a) mais uma fatia de idempotentHint para as ~23 tools de escrita restantes (36 de 59 ja classificadas); (b) a fatia envelope restante do passo 3 do P03; (c) um levantamento NOVO do catalogo para retomar outputSchema (sete tools entregues ate a sub-entrega 15/N, nenhuma candidata nova investigada desde entao); ou (d) perguntar ao Andre se prefere avancar direto para P04 (pedidos duraveis/lease/ledger) -- com quatro fatias de idempotentHint entregues, a opcao (d) segue cada vez mais razoavel, mas a cargo da execucao autonoma agendada decidir, salvo nova prioridade do Andre. NOTA DE PROCESSO: PR #264 continua aberta e precisa ser fechada manualmente pelo Andre (sem tool de fechamento via Argos) -- ver pendencias."
```
