# Arquivo de execução — P03 sub-entrega 21/N

Bloco movido de docs/autonomia/execucao.md em 23/09/2026, pela tarefa agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega P03 23/N — o arquivo ativo tinha passado de ~50KB (54816 bytes) sem re-arquivamento desde o décimo sétimo arquivo de arquivo (execucao-archive-p03sub20.md). Nenhum conteúdo foi alterado, com uma exceção: o campo `estado` foi atualizado de `pronto_para_revisao` para `validado`, porque o bloco P03 sub-entrega 22/N (ainda ativo em execucao.md) já confirma, com evidência própria, que as PRs #306 e #307 desta sub-entrega 21/N foram mescladas por aquela execução antes de iniciar trabalho novo — mesmo critério já usado para os arquivos de arquivo anteriores (relocação + confirmação de validação, não edição de conteúdo). Cobre: P03 sub-entrega 21/N (idempotentHint, sexta fatia de 4 tools — grupo edição/reagendamento em lote de ações).

---

```yaml
plano: plano-hermes-autonomo-2026-09-06
base_commit: e795135468b72534836efa1a36ab9bb0e209aaa9
pacote: "P03 sub-entrega 21/N -- idempotentHint por handler (passo 3 do plano), sexta fatia: campo `idempotencia` classificado por leitura direta do handler real para o grupo coeso edicao/reagendamento em lote de acoes, ja identificado como proxima fatia natural pela sub-entrega 20/N -- confirmar_edicao_em_lote, editar_acoes_em_lote, confirmar_reagendamento_em_lote, reagendar_acoes_em_lote (as 4 NAO_IDEMPOTENTE) -- confirmado nesta execucao, via clone git direto do repositorio, que as PRs #304 (codigo da sub-entrega 20/N) e #305 (diario da 20/N) ja tinham sido mescladas pelo Andre antes do inicio desta execucao (main em e795135, ultimo commit e o da propria PR #305) -- sem hiato desta vez: a execucao anterior (8h BRT) tinha aberto a PR #304 as 11:55 UTC, e o Andre ja tinha mesclado ambas antes desta execucao das 11h BRT (14:06 UTC) comecar"
estado: validado
inicio: "2026-09-22T14:06:00Z"
fim: "2026-09-22T14:41:00Z"
arquivos_alterados:
  - "functions/tools/inventory.py (4 classificacoes novas, todas NAO_IDEMPOTENTE: confirmar_edicao_em_lote e editar_acoes_em_lote sao dois wrappers finos sobre a MESMA callable main.py::confirmarEdicaoEmLote; confirmar_reagendamento_em_lote e reagendar_acoes_em_lote sao dois wrappers finos sobre a MESMA callable main.py::confirmarReagendamentoEmLote -- as duas callables fazem batch.update(..., 'acompanhamento': firestore.ArrayUnion([diary_entry])) por item, com um diary_entry novo (timestamp now_iso) a cada chamada bem-sucedida, incondicionalmente, sem checagem de snapshot_ts (diferente da versao singular confirmarEdicaoAcao). reagendar_acoes_em_lote acumula um segundo motivo: preparar_reagendamento_em_lote e refeito a cada chamada, e no modo filtro_data filtra exatamente pelo campo (data_limite) que a confirmacao acabou de mudar -- repetir tende a nao achar mais nada e falhar cedo, auto-limitacao parcial e dependente dos dados, nao uma protecao estrutural, e inexistente no modo task_ids)"
  - "functions/tools/registry.py (docstring de mcp_annotations() atualizado: contagem corrigida de 43 para 47 de ~61 tools elegiveis -- nota de honestidade acrescentada porque o diario da sub-entrega 20/N tinha registrado essa contagem como tendo ido para 48, mas o texto do docstring continuou em 43 ate esta sub-entrega chegar e contar programaticamente; nova secao descrevendo a fatia 21/N; paragrafo final atualizado para as ~6 tools realmente nao investigadas, nomeadas uma a uma, incluindo confirmar_edicao_acao como candidata obvia da proxima fatia)"
  - "functions/test_mcp_annotations.py (as 4 tools novas acrescentadas na lista parametrizada NAO_IDEMPOTENTE; contagem no docstring do modulo corrigida de 43 para 47)"
  - "functions/test_confirmar_lote_nao_idempotente.py (NOVO -- 5 testes por HANDLER REAL: chamam main.confirmarEdicaoEmLote/confirmarReagendamentoEmLote de verdade via inspect.unwrap (contorna o decorador @https_fn.on_call, que exige contexto de app Flask fora do emulador -- mesma tecnica ja usada em test_secretario_whatsapp.py::TestAutomationSettingsCallable) com um FakeDB minimo que captura os batch.update() reais, provando que duas chamadas identicas gravam DUAS notas de diario com timestamps diferentes apesar do valor final dos campos convergir; mais 3 testes no nivel dos wrappers Python (tools/hermes_tools.py) provando que editar_acoes_em_lote delega para a mesma callable sem dedup proprio, que reagendar_acoes_em_lote nao pula a confirmacao em repeticao via task_ids, e que via filtro_data a segunda chamada tende a nao achar nada e nao confirma de novo)"
decisoes:
  - id: p03-sub21-continuar-grupo-coeso-ja-identificado-pela-sub20
    motivo: "sub-entrega 20/N ja tinha identificado o grupo confirmar_edicao_em_lote/editar_acoes_em_lote/confirmar_reagendamento_em_lote/reagendar_acoes_em_lote como proxima fatia natural, coesa (duas callables compartilhadas em main.py, dois pares de wrappers em tools/hermes_tools.py). Escolhido continuar o padrao incremental em vez de perguntar ao Andre sobre P04 nesta execucao autonoma sem supervisao direta -- mesmo raciocinio ja usado nas sub-entregas 16/N-20/N: nao ha decisao irreversivel nem ambiguidade que justifique parar, e ainda ha tools elegiveis por investigar (~6 nunca visitadas apos esta fatia). A troca de pacote (P03->P04) continua melhor decidida pelo Andre ao revisar as PRs acumuladas, nao unilateralmente por uma execucao agendada."
    autoridade: existente_ou_nova
  - id: p03-sub21-confirmado-sem-hiato-desta-vez
    motivo: "diferente da sub-entrega 20/N (que encontrou um hiato de ~7 dias sem disparos deste plano), esta execucao confirmou, via clone git direto, que a cadencia de 4x/dia aprovada pelo Andre em 13/09/2026 esta funcionando: a execucao das 8h BRT (hoje, 22/09/2026) abriu a PR #304 as 11:55 UTC e o Andre ja tinha mesclado tanto ela quanto a PR #305 (diario) antes desta execucao das 11h BRT comecar, ~3h depois. Nao ha pendencia de processo a sinalizar sobre o agendamento nesta entrada."
    autoridade: existente_ou_nova
  - id: p03-sub21-classificacao-das-4-tools-nao-idempotente-por-array-union-incondicional
    motivo: "leitura direta de main.py::confirmarEdicaoEmLote e confirmarReagendamentoEmLote confirma que ambas fazem batch.update com firestore.ArrayUnion([diary_entry]) por item, diary_entry novo (now_iso) a cada chamada, sem nenhuma checagem de estado previo -- diferente da versao singular confirmarEdicaoAcao, que tem snapshot_ts. Mesmo criterio ja usado para editar_acao (sub-entrega 16/N): o valor final dos campos editados converge, mas o diario de cada acao cresce a cada repeticao -- efeito adicional, logo NAO_IDEMPOTENTE. confirmar_edicao_em_lote e editar_acoes_em_lote sao literalmente a mesma callable (so o nome do parametro de entrada muda, items vs itens); mesma relacao entre confirmar_reagendamento_em_lote e reagendar_acoes_em_lote."
    autoridade: existente_ou_nova
  - id: p03-sub21-achado-reagendar-acoes-em-lote-filtro-data-se-auto-limita
    motivo: "leitura de tools/hermes_tools.py::preparar_reagendamento_em_lote e _coletar_tarefas_lote revela um segundo motivo de nao-idempotencia especifico de reagendar_acoes_em_lote: quando a selecao e por filtro_data (em vez de task_ids), a consulta filtra por data_limite == filtro_data -- exatamente o campo que a confirmacao acabou de mudar. Repetir a MESMA chamada apos um sucesso tende a nao encontrar mais nenhuma acao com aquele filtro_data, e a preparacao devolve ERRO sem chegar a confirmar de novo. Documentado na nota do inventario e coberto por teste dedicado (test_filtro_data_apos_sucesso_tende_a_nao_encontrar_mais_nada) como uma auto-limitacao PARCIAL e dependente dos dados -- nao uma protecao estrutural (nao existe no modo task_ids, e nao e garantida nem no modo filtro_data se outra acao calhar de cair de volta na mesma data)."
    autoridade: existente_ou_nova
  - id: p03-sub21-correcao-honesta-da-contagem-43-vs-48-do-diario-da-sub20
    motivo: "ao contar programaticamente (python3 -c a partir de tools.inventory._INVENTORY) quantas tools elegiveis tem idempotencia classificada, o resultado antes desta sub-entrega era 43 -- nao 48 como o diario da sub-entrega 20/N tinha registrado (\"contagem 36->43->48\"). O texto do docstring de registry.py::mcp_annotations continuava dizendo 43 ate esta sub-entrega chegar, confirmando que o valor real nunca tinha ido para 48; o diario da sub-entrega 20/N descreveu uma edicao que nao aconteceu. Corrigido nesta sub-entrega para 47 (43 + as 4 desta fatia) com uma nota de honestidade explicita no proprio docstring, em vez de silenciosamente escrever 47 por cima sem registrar a divergencia -- decisao de nao reabrir/reescrever o bloco ja validado da sub-entrega 20/N (proibido pelo cabecalho deste diario), so corrigir o artefato de codigo e registrar aqui o que foi encontrado."
    autoridade: existente_ou_nova
  - id: p03-sub21-rodada1-nao-achou-nada
    motivo: "1a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto da implementacao): releu de forma independente os handlers reais (main.py::confirmarEdicaoEmLote/confirmarReagendamentoEmLote/confirmarEdicaoAcao, tools/hermes_tools.py::editar_acoes_em_lote/reagendar_acoes_em_lote/preparar_reagendamento_em_lote/_coletar_tarefas_lote), verificou cada afirmacao da nota do inventario contra o codigo -- incluindo a claim mais sutil sobre filtro_data/data_limite --, rodou os testes novos e a suite completa ela mesma, e conferiu a contagem 47/~61 programaticamente. Nao achou nada de errado -- resultado relatado aqui como esta, sem forcar um achado para parecer mais completo. Criterio de parada atingido (uma rodada limpa e suficiente quando a primeira ja nao acha nada, mesmo padrao ja implicito na disciplina deste plano)."
    autoridade: existente_ou_nova
testes:
  comandos:
    - "git clone --depth 1 do repositorio publico (ambiente limpo, sem uso do device bridge -- sessao rodando sem acesso ao computador do Andre)"
    - "python3 -m venv + pip install -r requirements.txt"
    - "python3 -m unittest test_confirmar_lote_nao_idempotente test_mcp_annotations test_tool_inventory -v (arquivos isolados)"
    - "python3 -m unittest discover -s . -p 'test_*.py' -q (suite completa, baseline antes da implementacao e depois da revisao)"
  resultados:
    - "Baseline (origin/main, commit e795135, PRs #304/#305 ja mescladas): 2118/2118, 0 falhas, 0 erros."
    - "Apos a implementacao completa + a rodada de revisao adversarial: 2123/2123, 0 falhas, 0 erros, sem regressao (2118 baseline + 5 testes novos)."
    - "test_confirmar_lote_nao_idempotente isolado: 5/5 OK. test_mcp_annotations isolado: 24/24 OK."
evidencias:
  - "1 rodada de revisao adversarial independente (Agent tool, general-purpose, sem contexto da implementacao) -- nao achou nada de errado, ver decisoes para o relato completo. Parada aqui, conforme a disciplina (escalar so enquanto uma rodada acha algo real; nao achar nada na 1a rodada e um resultado valido de parada)."
  - "Sanity check do PR: changed_files=4, additions=353, deletions=15, commits=4 no retorno de argos_criar_pr_repositorio -- bate exatamente com os 4 arquivos deliberadamente alterados (inventory.py, registry.py, test_mcp_annotations.py, test_confirmar_lote_nao_idempotente.py novo)."
  - "Os 4 arquivos enviados via mcp__Argos__argos_escrever_arquivo_repositorio (delegado a um subagente dedicado, com as mesmas instrucoes de integridade desta skill) com o `sha` retornado por cada escrita comparado contra `git hash-object` local -- os 4 bateram de primeira. Verificacao adicional independente: `git fetch` do branch remoto + comparacao de conteudo contra `origin/<branch>` para os 4 arquivos -- identico nos 4 (o arquivo novo comparado via `git show`+`diff` direto, ja que nunca tinha sido `git add`-ado no indice local do clone, o que faria `git diff` mostra-lo como 'deletado' por um detalhe de plumbing do git, nao um problema de conteudo)."
  - "PR #306 aberta (https://github.com/andre-martiini/Hermes/pull/306), com o relato completo da revisao e dos testes no corpo. AINDA NAO MESCLADA no momento do registro deste diario -- aguardando revisao e merge manual do Andre."
migracao:
  dry_run: null
  executada: false
flags:
  antes: {}
  depois: {}
pendencias:
  - "ABERTA: PR #264 (aberta por engano durante a correcao do diario da sub-entrega 14/N) continua aberta com um bug real de escaping YAML -- deve ser fechada sem mesclar pelo Andre; nao ha tool de fechamento de PR disponivel via Argos."
  - "ABERTA: PR #306 (esta sub-entrega) aguardando revisao e merge do Andre."
  - "ABERTA, ja registrada no bloco da sub-entrega 20/N e nao tocada por esta: demanda DEV-2026-0013 (race entre consultar_autorizacao_argos e decidir_autorizacao/consumir_autorizacao, pode apagar silenciosamente uma decisao real de autorizacao Argos) -- aguarda priorizacao do Andre."
  - "ABERTA, deliberadamente: idempotentHint para as ~6 tools de escrita/leitura_e_escrita ainda nunca investigadas continua fora de escopo -- acompanhar_processo_sipac, confirmar_edicao_acao (candidata obvia -- versao singular do par coberto nesta sub-entrega), consolidar_whatsapp, consultar_contatos_prioritarios_secretario, registrar_inscricao_bolsa_publica, schedule_whatsapp_message. Mais 8 tools (decidir_elevacao, decidir_promocao_autonomia, confirmar_acao, mutar_portal_compras_publico, mutar_lista_compras, revogar_promocao_autonomia, excluir_objetivo_estrategico, gerenciar_item_estrategico) seguem investigadas e deliberadamente sem classificacao por ambiguidade genuina -- ver notas de cada uma em tools/inventory.py."
  - "Pendencias ja registradas em blocos anteriores e nao tocadas por esta sub-entrega continuam abertas: ver blocos P03 sub-entrega 16/N a 20/N (arquivados ou acima) para a lista completa (religar inventario tipado a decisao de politica real; ambiguidades de classificacao; card do Telegram de rascunho degradado; observabilidade de claim pendente; TTL do Firestore para mcp_jobs; limite de 200KB do Argos; passo 2 do P02 como hardening futuro; salvar_memoria_global sem teste de handler dedicado; caveat de concorrencia em registrar_saude/consultar_investimentos; outputSchema para as tools restantes do catalogo; a fatia envelope restante do passo 3 do P03)."
proximo_pacote: "P03 -- PR #306 (idempotentHint, sexta fatia de 4 tools) aberta, aguardando revisao e merge do Andre. A proxima execucao agendada deve primeiro confirmar, via leitura direta do repositorio (nao so deste diario), se a PR #306 foi mesclada -- e registrar essa confirmacao no proximo bloco, mesmo padrao ja usado para as sub-entregas 16/N-20/N -- antes de decidir o proximo passo: (a) mais uma fatia de idempotentHint para as ~6 tools de escrita nunca investigadas (47 de ~61 ja classificadas; confirmar_edicao_acao e a candidata mais obvia, mesma familia desta sub-entrega); (b) a fatia envelope restante do passo 3 do P03; (c) um levantamento NOVO do catalogo para retomar outputSchema (sete tools entregues ate a sub-entrega 15/N, nenhuma candidata nova investigada desde entao); ou (d) perguntar ao Andre se prefere avancar direto para P04 (pedidos duraveis/lease/ledger) -- com seis fatias de idempotentHint entregues, a opcao (d) segue cada vez mais razoavel, mas a cargo da execucao autonoma agendada decidir, salvo nova prioridade do Andre. NOTAS DE PROCESSO: (1) PR #264 continua aberta e precisa ser fechada manualmente pelo Andre (sem tool de fechamento via Argos); (2) demanda DEV-2026-0013 (achado de race real em consultar_autorizacao_argos) aguarda priorizacao do Andre -- ver pendencias; (3) desta vez NAO houve hiato -- a cadencia de 4x/dia continua funcionando normalmente."
```
