# Arquivo de execução — P03 sub-entrega 32/N

Bloco movido de docs/autonomia/execucao.md em 25/09/2026, pela tarefa agendada de avanço autônomo, ao acrescentar as entradas do fix de DST (achado do Codex na PR #326) da sub-entrega P04 1/N — o arquivo ativo estava em 52153 bytes, acima do limite de ~50KB, sem re-arquivamento desde o vigésimo oitavo arquivo (execucao-archive-p03sub31.md). Nenhum conteúdo foi alterado — o bloco já estava com `estado: validado` (a própria sub-entrega 32/N confirmou o merge da PR #323 desta sub-entrega 31/N; a mesclagem da PR #325 desta sub-entrega 32/N foi confirmada pela sub-entrega P04 1/N seguinte, que a mesclou diretamente). Cobre: P03 sub-entrega 32/N (outputSchema, décima sexta e última tool da lista original de 5 — listar_conversas_whatsapp, backed por tools/whatsapp_tools.py::listar_conversas, achado real da própria 1a rodada de revisão adversarial sobre forma única->oneOf, corrigido antes da PR).

---

```yaml
plano: plano-hermes-autonomo-2026-09-06
base_commit: eaa6a66a919cf69b72ac24973b6c21232f4435ae
pacote: "P03 sub-entrega 32/N -- outputSchema/structuredContent por handler (passo 3 do plano), decima sexta tool: listar_conversas_whatsapp, backed por tools/whatsapp_tools.py::listar_conversas, exposta via tools/hermes_tools.py::_whatsapp(\"listar_conversas\"). ULTIMA candidata da lista original de 5 da sub-entrega 27/N -- esgota essa lista (as outras 4 ja resolvidas: consultar_contatos_prioritarios_secretario 28/N, consultar_promocoes_autonomia_sugeridas 29/N, obter_fila_atencao 30/N, consultar_elevacoes_sugeridas 31/N). Confirmado no inicio desta execucao (mcp__github__list_pull_requests, estado aberto) que nao havia PR pendente desta rotina, e que o bloco anterior (sub-entrega 31/N) estava com um estado desatualizado no diario -- PR #323 ja confirmada mesclada (mcp__github__pull_request_read), mas o bloco ainda dizia estado: pronto_para_revisao; corrigido para validado nesta execucao, sem reescrever mais nada do bloco (ver arquivos_alterados abaixo)."
estado: validado
inicio: "2026-09-25T08:15:16Z"
fim: "2026-09-25T09:05:00Z"
arquivos_alterados:
  - "functions/tools/registry.py (1 outputSchema novo: listar_conversas_whatsapp, decima sexta tool com contrato publicado -- 92 de 108 tools hoje sem contrato, deliberadamente. A REDACAO INICIAL desta sub-entrega modelou forma UNICA, sem oneOf, assumindo que listar_conversas nunca levanta WhatsAppNaoMonitorado/ValueError, as duas excecoes que o wrapper _whatsapp intercepta -- ACHADO REAL da 1a rodada de revisao adversarial (antes da PR): `limite = max(1, min(int(args.get(\"limite\") or 60), 200))` em whatsapp_tools.py nao e protegido contra `limite` nao-numerico (ex.: \"abc\"), que levanta ValueError DENTRO de listar_conversas -- propagado ate o wrapper, cujo `except ValueError as exc: return {\"erro\": str(exc)}` converte a excecao num dict REAL, publicado como structuredContent por mcp_server._handle_tools_call (que so olha se o resultado e um dict E ha outputSchema, sem distinguir erro reconhecido de sucesso). Corrigido para oneOf de 2 branches (sucesso/erro) antes da PR -- ver decisao p03-sub32-codex... nao, ver decisao p03-sub32-oneof-corrigido abaixo (achado da revisao ADVERSARIAL PROPRIA desta vez, nao do Codex -- primeira sub-entrega da serie em que isso acontece). WhatsAppNaoMonitorado continua nunca levantada por listar_conversas (so por _exigir_monitorado, nunca chamada por ela), entao o ramo de erro tem uma forma so (`{\"erro\": string}`). A mesma rodada tambem achou 2 imprecisoes de comentario, corrigidas no mesmo commit: (1) total/monitoradas sao calculados sobre a lista JA FILTRADA por apenas_monitoradas (quando True, o padrao quando o argumento e omitido, um chat nao monitorado nunca chega a entrar em conversas -- o `continue` acontece ANTES do `append`), nao a lista COMPLETA como a redacao original dizia -- mas segue verdade que nao ha corte de QUERY do Firestore (`.limit()`) antes dessa contagem, so o corte de `limite` na resposta (`conversas[:limite]`), que nunca afeta total/monitoradas; (2) chat_name (`dados.get(\"chat_name\") or chat_id`) NAO tem a mesma coercao `str(...)` que chat_id tem (`str(dados.get(\"chat_id\") or snap.id)`) -- RISCO ACEITO, documentado no comentario, nao corrigido por ser mudanca de COMPORTAMENTO fora do escopo de uma fatia so-schema (nenhum escritor real conhecido grava chat_name nao-string). ultima_atividade continua o unico campo nullable do item (_iso(None) devolve None explicito). Docstring de output_schema() atualizado de 93/108 para 92/108, apos a decima sexta entrada.)"
  - "functions/test_output_schema.py (docstring do modulo atualizado para citar a decima sexta tool, incluindo a correcao de forma unica->oneOf e as 2 imprecisoes de comentario corrigidas; TestOutputSchema ganhou test_listar_conversas_whatsapp_tem_schema_oneof_sucesso_e_erro (renomeado de _forma_unica apos o achado da rodada 1, com as assercoes do ramo de erro `{\"erro\": string}` acrescentadas) + paridade renomeada para 16 tools (test_paridade_dezesseis_tools_tem_output_schema_hoje); TestHandleToolsListOutputSchema ganhou test_listar_conversas_whatsapp_publica_output_schema (assertIn oneOf em vez de assertNotIn, apos o achado) + listas de paridade atualizadas; TestIntegracaoHandleToolsCallStructuredContent ganhou 5 testes novos -- sucesso (com apenas_monitoradas=False explicito nos argumentos, corrigindo uma INCONSISTENCIA DE FIXTURE que a propria rodada 1 achou: a versao original misturava argumentos={} (apenas_monitoradas=True implicito) com um item mockado monitorada=False, forma que o handler real NUNCA produziria para esses argumentos), lista vazia, paridade campo a campo contra schema[\"oneOf\"][0], erro mockado (mesmo padrao das tools anteriores), e -- o mais importante, fechando a lacuna que a propria rodada 1 apontou (nenhum dos 3 testes originais exercitava a cadeia real _whatsapp->listar_conversas, todos mockavam mcp_server.execute_tool) -- um teste de CADEIA REAL, sem mockar execute_tool, importando os fakes _Ctx/_Db de test_whatsapp_tools.py (mesmo padrao de import cruzado entre arquivos de teste ja usado por test_email_nao_entregue.py/test_regressao_proatividade.py/test_telegram_extended.py), provando que limite=\"abc\" propaga ValueError de verdade ate mcp_server._handle_tools_call e bate com o ramo de erro publicado.)"
  - "docs/autonomia/execucao.md (este bloco; corrigido tambem o estado do bloco da sub-entrega 31/N, de pronto_para_revisao para validado -- PR #323 ja estava mesclada, so o diario nao tinha sido atualizado apos o merge por uma execucao anterior. Arquivo em 34288 bytes antes desta escrita, bem abaixo do limite de ~50KB -- nenhum arquivamento necessario nesta sub-entrega.)"
decisoes:
  - id: p03-sub32-ultima-candidata-lista-original
    motivo: "listar_conversas_whatsapp era a UNICA candidata nao investigada restante da lista original de 5 da sub-entrega 27/N (as outras 4: consultar_contatos_prioritarios_secretario, consultar_promocoes_autonomia_sugeridas, obter_fila_atencao, consultar_elevacoes_sugeridas, todas ja resolvidas em sub-entregas anteriores). Resolvida nesta sub-entrega, esgotando essa lista -- ver proximo_pacote abaixo para a recomendacao de migrar para P04 na proxima execucao."
    autoridade: existente_ou_nova
  - id: p03-sub32-nenhuma-excecao-alcancavel-exceto-limite
    motivo: "Busca exaustiva por `raise` no corpo de listar_conversas e nos 3 helpers que ela chama (_allowlist/_captura_total/_leitura_total, todos via _config_ingest, que so faz .get() sobre o dict de settings) confirma que nenhum deles levanta excecao. WhatsAppNaoMonitorado so e levantada por _exigir_monitorado, nunca chamada por listar_conversas. A UNICA excecao alcancavel e o ValueError implicito de `int(args.get(\"limite\") or 60)` quando o valor nao parseia como inteiro."
    autoridade: existente_ou_nova
  - id: p03-sub32-oneof-corrigido
    motivo: "Achado REAL da 1a rodada de revisao adversarial (Agent tool, sem contexto da implementacao): a redacao original modelou forma unica (sem oneOf), alegando que listar_conversas nunca levanta as excecoes que o wrapper _whatsapp intercepta -- falso para ValueError (ver decisao acima). Corrigido para oneOf de 2 branches (sucesso: total/monitoradas/conversas/observacao; erro: so a chave erro) antes da PR. Diferente das ultimas 2 sub-entregas (30/N, 31/N), em que o achado de nulabilidade veio do Codex NA PR, desta vez o achado veio da PROPRIA revisao adversarial, antes de a PR existir."
    autoridade: existente_ou_nova
  - id: p03-sub32-total-monitoradas-comentario-corrigido
    motivo: "A mesma rodada 1 achou que o comentario original ('total/monitoradas calculados sobre a lista COMPLETA de conversas') estava impreciso: quando apenas_monitoradas e True (o padrao quando omitido), o `continue` em whatsapp_tools.py exclui um chat nao monitorado ANTES do `append` -- entao total/monitoradas sao calculados sobre a lista JA FILTRADA, nao a lista completa de whatsapp_chats. Corrigido no comentario. Nao e uma garantia formal do outputSchema (total continua so `{\"type\": \"integer\"}`), so uma nota de comportamento real para quem for consumir a tool -- mesma categoria das notas ja aceitas para obter_fila_atencao/consultar_elevacoes_sugeridas."
    autoridade: existente_ou_nova
  - id: p03-sub32-chat_name-sem-coercao-risco-aceito
    motivo: "Achado da mesma rodada 1: chat_name (`dados.get(\"chat_name\") or chat_id`) nao tem a coercao `str(...)` explicita que chat_id tem (`str(dados.get(\"chat_id\") or snap.id)`) -- se um documento tiver chat_name PRESENTE mas truthy-e-nao-string, o fallback por `or` nao age e o valor cru passaria direto, violando `{\"type\": \"string\"}`. RISCO ACEITO, documentado no comentario de _OUTPUT_SCHEMAS, nao corrigido: nenhum escritor real conhecido (o worker WhatsApp em Node) grava isso, e adicionar a coercao mudaria COMPORTAMENTO de listar_conversas, fora do escopo de uma fatia so-schema -- mesma categoria de risco ja aceita para outros campos de forma variavel noutras tools deste catalogo."
    autoridade: existente_ou_nova
  - id: p03-sub32-fixture-sucesso-corrigida
    motivo: "A mesma rodada 1 achou que o primeiro teste de integracao (sucesso) misturava argumentos={} (apenas_monitoradas=True implicito) com um item mockado monitorada=False -- combinacao que o handler real NUNCA produziria (ver decisao acima sobre o filtro). Corrigido acrescentando apenas_monitoradas: False explicito aos argumentos do teste, tornando a fixture uma forma real possivel da tool."
    autoridade: existente_ou_nova
  - id: p03-sub32-teste-cadeia-real-novo
    motivo: "A mesma rodada 1 notou que os 3 testes de integracao originais mockavam mcp_server.execute_tool diretamente, entao NENHUM exercitava a cadeia real _whatsapp->listar_conversas -- exatamente o caminho onde o achado do oneOf vive. Acrescentado 1 teste novo que roda essa cadeia de verdade (sem mockar execute_tool), com os fakes _Ctx/_Db reaproveitados de test_whatsapp_tools.py, provando que limite=\"abc\" propaga ValueError ate mcp_server._handle_tools_call e bate com o ramo de erro publicado."
    autoridade: existente_ou_nova
  - id: p03-sub32-diario-31N-estado-corrigido
    motivo: "PR #323 (sub-entrega 31/N) ja estava confirmada mesclada no inicio desta execucao (mcp__github__pull_request_read: merged=true, merged_at=2026-09-24T17:52:42Z), mas o bloco correspondente em execucao.md ainda dizia estado: pronto_para_revisao -- uma execucao anterior abriu e mesclou a PR mas nao voltou para atualizar o diario. Corrigido para validado nesta execucao, sem reescrever mais nada do conteudo do bloco (o bloco nao estava validado antes desta correcao, entao a regra de nunca reescrever um bloco ja validado nao se aplica)."
    autoridade: existente_ou_nova
rodadas_revisao_adversarial:
  - rodada: 1
    foco: "Revisao completa da implementacao inicial (Agent tool, general-purpose, sem contexto da implementacao -- so o diff e o objetivo)."
    achados_reais: 3
    detalhe: "(1) forma unica estava ERRADA, faltava oneOf para o ramo de erro de ValueError (ver decisao p03-sub32-oneof-corrigido); (2) comentario sobre total/monitoradas 'lista completa' estava impreciso (ver decisao p03-sub32-total-monitoradas-comentario-corrigido); (3) chat_name sem coercao str() explicita, risco aceito nao corrigido (ver decisao p03-sub32-chat_name-sem-coercao-risco-aceito). Tambem achou a inconsistencia de fixture do teste de sucesso (ver decisao p03-sub32-fixture-sucesso-corrigida) e a lacuna de nenhum teste exercitar a cadeia real (ver decisao p03-sub32-teste-cadeia-real-novo). Todos corrigidos antes da rodada 2."
  - rodada: 2
    foco: "Fix da rodada 1 (Agent tool, general-purpose, sem contexto da rodada 1 alem do diff e do objetivo -- incluindo o novo teste de cadeia real)."
    achados_reais: 0
    detalhe: "Verificou de forma independente: exaustao de excecoes alcancaveis (incluindo TypeError/OverflowError para limite nao-string, confirmando que nenhum dos dois produz structuredContent -- mcp_server nunca atribui `estruturado` no except externo), exatidao do branch de sucesso e do branch de erro, correcao do teste de cadeia real (tracou que os fakes _Ctx/_Db fornecem tudo que o caminho real le antes de chegar no handler, e que o ValueError acontece ANTES do loop de stream(), entao nao ha falso-positivo por outra excecao mascarada), exatidao do risco aceito de chat_name (nenhum outro campo tem a mesma lacuna), exatidao do comentario de total/monitoradas, e ausencia de outro consumidor de output_schema() que assuma forma plana sem oneOf. Nao achou nada -- criterio de parada atingido, PR aberta em seguida."
testes:
  comandos:
    - "repositorio ja clonado no inicio da execucao (branch main, commit eaa6a66, gmail webhook fix PR #324 ja mesclada e fora do escopo desta rotina -- mcp__github__list_pull_requests devolveu lista vazia para estado aberto)"
    - "python3 -m venv /tmp/hermes_venv_32 + pip install -r requirements.txt (venv fresco nesta execucao, instalou corretamente na 1a tentativa)"
    - "python3 -m unittest test_output_schema -v (isolado, apos a implementacao inicial e novamente apos os fixes da rodada 1)"
    - "python3 -m unittest discover -s . -p 'test_*.py' -q (suite completa -- baseline medido num git worktree separado em origin/main, SEM git stash, seguindo a licao de PROCESSO registrada no bloco da sub-entrega 31/N sobre nao rodar stash durante revisao adversarial; suite completa novamente apos a implementacao inicial e apos os fixes da rodada 1)"
  resultados:
    - "Baseline (origin/main, commit eaa6a66, via git worktree separado): 2327/2327, 0 falhas, 0 erros."
    - "Apos a implementacao inicial (schema + testes, antes do achado da rodada 1): 116/116 no escopo isolado (test_output_schema) e 2332/2332 na suite completa."
    - "Apos os fixes da rodada 1 (oneOf + 2 comentarios corrigidos + fixture corrigida + 2 testes novos -- erro mockado e cadeia real): 116/116 no escopo isolado (confirmado por mim e, de forma independente, pela 2a rodada de revisao adversarial) e 2334/2334 na suite completa -- delta final de +7 tests sobre o baseline de 2327 (1 teste de forma renomeado sem mudar de contagem + 1 novo em TestOutputSchema pela paridade + 1 novo em TestHandleToolsListOutputSchema + 5 novos em TestIntegracaoHandleToolsCallStructuredContent)."
evidencias:
  - "2 rodadas de revisao adversarial independentes (Agent tool, general-purpose, cada uma sem contexto de implementacao nem de rodada anterior alem do que foi passado no prompt). Ver rodadas_revisao_adversarial acima para o detalhe -- resumo: rodada 1 achou 3 problemas reais + 2 problemas de qualidade de teste, todos corrigidos antes da PR; rodada 2 (focada nos fixes) nao achou nada, criterio de parada atingido."
  - "PR #325 aberta por esta execucao (https://github.com/andre-martiini/Hermes/pull/325)."
migracao:
  dry_run: null
  executada: false
flags:
  antes: {}
  depois: {}
pendencias:
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: demanda DEV-2026-0013 (race entre consultar_autorizacao_argos e decidir_autorizacao/consumir_autorizacao) -- aguarda priorizacao do Andre."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: sipac_processos.snapshot_hash colidindo com scheduledSipacSync (achado da sub-entrega 23/N), candidato a P05."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: risco residual de validacao de outputSchema em obter_fila_atencao (achado do Codex na sub-entrega 30/N) -- limitacao estrutural de todo o catalogo com outputSchema, nao especifica desta tool."
  - "NOVA, achado de processo desta execucao: uma execucao anterior (31/N) abriu e mesclou a PR #323 mas nao voltou para atualizar o diario de pronto_para_revisao para validado -- corrigido nesta execucao (ver decisao p03-sub32-diario-31N-estado-corrigido). Para a proxima execucao: ao terminar de mesclar uma PR, atualizar o `estado` do bloco correspondente ANTES de encerrar a execucao, nao deixar essa atualizacao para a proxima."
  - "Pendencias ja registradas em blocos anteriores e nao tocadas por esta sub-entrega continuam abertas: ver docs/autonomia/execucao-archive-p03sub29.md e bloco P03 sub-entrega 30/N para a lista completa."
proximo_pacote: "P03 esgota a lista original de 5 candidatas da sub-entrega 27/N -- listar_conversas_whatsapp (esta sub-entrega) era a ULTIMA. outputSchema chega a 16 de 108 tools do catalogo. RECOMENDACAO FORTE desta execucao para a proxima, confirmando o que a sub-entrega 31/N ja vinha sinalizando: nao ha mais candidata facil e conhecida de outputSchema restando do levantamento original -- a proxima execucao DEVE avaliar seriamente migrar para P04 (pedidos duraveis/lease/ledger, ver docs/plano-hermes-autonomo-2026-09-06.md secao 9) como o proximo compromisso de varias sub-entregas, em vez de procurar mais uma tool avulsa para outputSchema por conta propria. Isso nao impede uma outputSchema pontual se uma tool nova/candidata clara surgir organicamente (ex.: por pedido do Andre), mas nao deve ser o objetivo padrao da proxima execucao. NOTAS DE PROCESSO: (1) PR #264 continua fechada, nao e mais pendencia; (2) demanda DEV-2026-0013 aguarda priorizacao do Andre; (3) esta execucao encontrou e corrigiu um diario desatualizado de uma execucao anterior (bloco 31/N com estado: pronto_para_revisao apesar da PR #323 ja estar mesclada) -- ver pendencia NOVA acima sobre atualizar o diario ANTES de encerrar, nao depois; (4) esta e a PRIMEIRA sub-entrega da serie recente em que o achado real da revisao adversarial veio da PROPRIA rodada 1 (Agent tool), nao do Codex na PR -- a disciplina de verificar explicitamente toda excecao alcancavel pelo handler (nao so os campos nullable, licao das sub-entregas 30/N e 31/N) evitou um achado do Codex depois do merge; (5) Blueprint da Autonomia (artifact) a atualizar apos a confirmacao deste bloco -- ver relato final desta execucao para o desfecho."
```
