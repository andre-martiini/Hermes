# Arquivo de execução — P04 sub-entrega 1/N

Bloco movido de docs/autonomia/execucao.md em 25/09/2026, pela tarefa agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega P04 2/N (o arquivo ativo passaria de ~52KB, acima do limite de ~50KB, sem re-arquivamento desde o vigésimo nono arquivo, execucao-archive-p03sub32.md). Nenhum conteúdo foi alterado — o bloco já estava com `estado: validado` (confirmado pela própria sub-entrega, que verificou o merge da PR #326 via git fetch antes de encerrar). Cobre: P04 sub-entrega 1/N (passo 1 do pacote — estados, leases e gerações da seção 4, novo módulo functions/autonomy/requests.py).

---

```yaml
plano: plano-hermes-autonomo-2026-09-06
base_commit: 32971416f4d8abf230f87d1e8628836329edb376
pacote: "P04 sub-entrega 1/N -- passo 1 do pacote (\"Implementar os estados, leases e geracoes da secao 4\"). PRIMEIRA fatia de P04 (docs/plano-hermes-autonomo-2026-09-06.md, secao 4.4 \"Pedido\" e secao 4.5 \"Reserva, retomada e idempotencia\"), apos a lista original de 5 candidatas de outputSchema (P03) se esgotar na sub-entrega 32/N -- recomendacao FORTE do proprio bloco 32/N para migrar para P04 como proximo compromisso de varias sub-entregas, ja que P04 e pre-requisito de quase todo o resto do plano (P05-P18). Confirmado no inicio desta execucao (mcp__github__list_pull_requests, estado aberto) que havia 1 PR pendente de uma execucao anterior desta MESMA rotina -- PR #325 (sub-entrega 32/N), aberta as 08:43 UTC e ainda nao mesclada quando esta execucao comecou (11:19 UTC, ~2h30 depois -- bem alem da janela de espera de 5-10 min do fluxo). Verificado: CI verde nos 3 checks, revisao do Codex concluida SEM achados (so o comentario-resumo de rastreamento, nenhum finding real), 0 threads de revisao nao resolvidos, mergeable_state=clean. Mesclada por esta execucao (squash, mesmo padrao dos merges anteriores) antes de iniciar trabalho novo -- retomada de trabalho pela metade de uma execucao anterior, em vez de duplicar. Em seguida, main atualizado localmente e nova branch criada a partir dele para a fatia de P04 abaixo. Mesmo padrao incremental ja usado em P02 sub-entrega 1/N (autonomy/contracts.py): so tipos e logica pura, SEM tocar em agent_requests.py/Firestore ainda -- wiring fica para uma sub-entrega seguinte."
estado: validado
inicio: "2026-09-25T11:24:00Z"
fim: "2026-09-25T12:20:00Z"
arquivos_alterados:
  - "functions/autonomy/requests.py (NOVO -- RequestStatus, enum de 11 estados incluindo pendente/reservado/em_andamento/verificando/concluido da secao 4.4 mais os caminhos adicionais (aguardando_aprovacao, aguardando_externo, retentativa_agendada, resultado_desconhecido, falha_final, cancelado) e ERRO_LEGADO so para normalizar registros antigos na leitura -- nenhum estado novo transiciona PARA ele. Maquina de transicoes permitidas (_TRANSICOES_PERMITIDAS) -- interpretacao desta sub-entrega das arestas implicitas na secao 4.4 (o plano nomeia os estados mas nao desenha o grafo completo), documentada como tal no docstring do modulo para revisao futura. Lease (dataclass frozen: lease_token/generation/executor_id/expires_at) com validacao tz-aware obrigatoria em __post_init__; gerar_lease_token (secrets.token_urlsafe); nova_lease (emite lease incrementando generation); lease_expirada (limite inclusivo); lease_valida_para_acao (fencing por token+generation, secrets.compare_digest para comparacao em tempo constante); calcular_backoff_segundos (jitter +-20% nos patamares de 1/5/20 min da secao 4.5, DEFAULT_MAX_TENTATIVAS=3)."
  - "functions/test_autonomy_requests.py (NOVO -- 65 testes: maquina de estados (terminais nunca transicionam, nada transiciona para ERRO_LEGADO, conjunto EXATO de transicoes permitidas para cada um dos 7 estados nao-terminais -- nao so PENDENTE -- incluindo a checagem explicita de que RESERVADO/EM_ANDAMENTO nao pulam direto para CONCLUIDO, e que RESULTADO_DESCONHECIDO nao volta direto para EM_ANDAMENTO sem reconciliar), lease/fencing (token certo/errado/vazio/nao-ASCII, generation certa/errada/nao-numerica/None/infinita positiva e negativa/Decimal infinito, expiracao inclusiva), tz-awareness (datetime naive em agora/expires_at levanta ValueError cedo), DST (expires_at sempre UTC, fallback e avanco de DST reproduzidos com zoneinfo real -- achado do Codex na PR #326), e backoff (patamares exatos, jitter deterministico via RNG fake, piso de 1s realmente exercitado com jitter extremo)."
  - "docs/autonomia/execucao.md (este bloco; arquivo estava em 51999 bytes, acima do limite de ~50KB -- bloco da sub-entrega 31/N movido para docs/autonomia/execucao-archive-p03sub31.md antes de acrescentar esta entrada, mesmo criterio ja usado nos 27 arquivos de arquivo anteriores. Depois, ao registrar o fix de DST do Codex, o arquivo passou de ~50KB de novo -- bloco da sub-entrega 32/N tambem movido, para execucao-archive-p03sub32.md. Nenhuma reescrita de bloco ja validado -- so relocacao)."
  - "docs/autonomia/execucao-archive-p03sub31.md (NOVO, vigesimo oitavo arquivo de arquivo)"
  - "docs/autonomia/execucao-archive-p03sub32.md (NOVO, vigesimo nono arquivo de arquivo)"
decisoes:
  - id: p04-sub1-retomada-pr325-em-vez-de-trabalho-novo
    motivo: "Esta execucao encontrou a PR #325 (sub-entrega 32/N) ainda aberta e nao mesclada ao iniciar, ~2h30 depois de criada -- bem alem da janela de espera de 5-10 min do fluxo padrao. CI verde, revisao do Codex concluida sem achados, sem threads de revisao pendentes, sem conflito de merge. Mesclada por esta execucao antes de iniciar qualquer trabalho novo, seguindo a instrucao explicita de retomar trabalho pela metade em vez de duplicar."
    autoridade: existente_ou_nova
  - id: p04-sub1-escopo-so-logica-pura-sem-wiring
    motivo: "P04 e pacote G (grande complexidade), com 10 passos que incluem wiring em agent_requests.py/agent_runs.py/mcp_jobs.py existentes, indices Firestore e migracao de registros legados. Uma unica PR pequena e revisavel nao cobre tudo isso de uma vez -- escolhido o passo 1 sozinho (\"implementar os estados, leases e geracoes\") como primeira fatia, mesmo padrao ja usado com sucesso em P02 sub-entrega 1/N (autonomy/contracts.py: so tipos, nenhum canal religado ainda). Nada em producao foi tocado; agent_requests.py continua exatamente como estava."
    autoridade: existente_ou_nova
  - id: p04-sub1-maquina-de-estados-interpretacao-propria
    motivo: "A secao 4.4 do plano nomeia os estados do Pedido e alguns invariantes (terminais nao regridem; operacao externa incerta vai para reconciliacao) mas nao desenha o grafo completo de transicoes. As arestas em _TRANSICOES_PERMITIDAS sao uma modelagem desta sub-entrega, documentada explicitamente como tal (nao uma citacao literal do plano) para que quem vier depois possa revisar/discordar com base no texto real da secao 4.4/4.5, nao veja como fato estabelecido."
    autoridade: existente_ou_nova
  - id: p04-sub1-erro-legado-nunca-transicionavel
    motivo: "Secao 4.4: 'o status legado erro e preservado para registros antigos e normalizado na leitura; novos erros distinguem recuperavel e terminal'. RequestStatus.ERRO_LEGADO existe só para essa normalizacao de leitura -- nenhuma aresta em _TRANSICOES_PERMITIDAS tem ERRO_LEGADO como destino (testado explicitamente: test_nenhum_estado_transiciona_para_erro_legado), entao codigo novo nunca pode produzi-lo, so FALHA_FINAL/RETENTATIVA_AGENDADA distinguem recuperavel/terminal como o plano pede."
    autoridade: existente_ou_nova
  - id: p04-sub1-linha-valueerror-vs-false-motivo
    motivo: "lease_valida_para_acao recebe token_apresentado/generation_apresentada de um CONSUMIDOR (secao 4.5, item 1) -- entrada nao confiavel por definicao, entao um valor malformado (nao-ASCII, nao-numerico, infinito) e tratado como '(False, motivo)', nunca uma excecao. Ja agora/generation_anterior/executor_id/duracao_segundos em nova_lease sao responsabilidade de QUEM CHAMA a funcao (codigo interno, nao o consumidor externo), entao continuam levantando ValueError -- falha cedo e clara para um erro de programacao, em vez de mascarar como 'so mais um pedido invalido'. Linha confirmada consistente pela 2a rodada de revisao adversarial."
    autoridade: existente_ou_nova
  - id: p04-sub1-rodada1-achados-reais-corrigidos
    motivo: "1a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto da implementacao) achou 3 problemas reais, todos corrigidos antes da 2a rodada: (1) lease_valida_para_acao levantava TypeError para token nao-ASCII (secrets.compare_digest recusa comparar string nao-ASCII); (2) a mesma funcao levantava ValueError/TypeError para generation nao coercivel a int; (3) datetime naive em agora/expires_at podia crashar bem longe da causa raiz (TypeError ao comparar naive com aware). Tambem apontou 2 lacunas de teste: (4) so PENDENTE tinha o conjunto EXATO de transicoes coberto por teste -- nada impedia silenciosamente alguem adicionar CONCLUIDO como atalho de RESERVADO/EM_ANDAMENTO sem quebrar teste nenhum; (5) o teste que alegava verificar o piso max(1.0, ...) do backoff nunca disparava esse piso de verdade (jitter real da faixa declarada nunca chega la)."
    autoridade: existente_ou_nova
  - id: p04-sub1-rodada2-achado-real-overflowerror
    motivo: "2a rodada de revisao adversarial (Agent tool, general-purpose, foco exclusivo nos fixes da rodada 1, sem contexto dela alem do que foi passado no prompt) confirmou os 5 fixes anteriores e achou 1 problema NOVO real: int(float(\"inf\"))/int(Decimal(\"Infinity\")) levantam OverflowError, nao capturado pelo except (TypeError, ValueError) do fix anterior -- json.loads aceita \"Infinity\"/\"-Infinity\" como extensao por padrao, entao um consumidor podia mandar isso num payload e crashar a funcao do mesmo jeito que o fix deveria ter fechado. Tambem achou que um teste se chamava '...com_fracao' mas passava a STRING \"3.5\", nao um float de verdade -- renomeado, e adicionados 3 testes com o valor real (inf positivo, inf negativo, Decimal infinito)."
    autoridade: existente_ou_nova
  - id: p04-sub1-rodada3-nao-achou-nada-alem-de-risco-documentado
    motivo: "3a rodada de revisao adversarial (Agent tool, general-purpose, foco exclusivo no fix do OverflowError, incluindo verificacao empirica com numpy -- dependencia real do projeto -- e Decimal/complex/int de precisao arbitraria) confirmou o fix como correto e SUFICIENTE, e argumentou explicitamente que capturar excecoes de um __int__ malicioso customizado seria mudar de modelo de ameaca (execucao de codigo adversario, nao dado malformado) e sairia do escopo -- recomendacao explicita de NAO alargar mais o except. Apontou 2 notas informativas nao-bloqueantes (int() trunca silenciosamente uma fracao NUMERICA -- float/Decimal -- mas rejeita uma fracao em STRING; bool e aceito como inteiro por ser subclasse de int em Python) -- nenhuma das duas enfraquece o fencing (quem ja sabe a generation/token corretos nao ganha nada mandando 3.9 em vez de 3), documentadas como RISCO ACEITO em comentario no codigo, nao corrigidas por serem mudanca de comportamento (round-trip de igualdade) fora do escopo desta fatia. Criterio de parada atingido -- nenhuma rodada seguinte disparada."
    autoridade: existente_ou_nova
  - id: p04-sub1-codex-pr326-dst-corrigido
    motivo: "Achado REAL do Codex (P2) na PR #326, apos abertura: nova_lease calculava expires_at = agora_resolvido + timedelta(...) com agora_resolvido podendo ser qualquer datetime tz-aware, nao necessariamente UTC. datetime + timedelta num fuso com DST (zoneinfo) faz aritmetica de RELOGIO DE PAREDE, nao de tempo decorrido -- reproduzido antes do fix: lease de 300s criada as 01:58 America/New_York (antes do fallback de 2026-11-01) expirava as 02:03 do lado de ca do fallback, 3900s reais depois (mais de 1h), nao 300s. Corrigido normalizando agora_resolvido para UTC (.astimezone(timezone.utc)) ANTES de somar a duracao -- UTC nao observa DST, entao a soma vira aritmetica de tempo decorrido de verdade qualquer que seja o fuso de entrada. 3 testes de regressao novos: expires_at sempre UTC, fallback de DST (novembro) e avanco de DST (marco, direcao oposta do erro) reproduzidos com zoneinfo.ZoneInfo real, delta absoluto conferido bater exatamente com a duracao pedida."
    autoridade: existente_ou_nova
  - id: p04-sub1-rodada4-achado-qualidade-teste-avanco-dst
    motivo: "4a rodada de revisao adversarial (Agent tool, general-purpose, foco exclusivo no fix de DST da decisao anterior) confirmou o fix de UTC como correto e suficiente (unico ponto de soma datetime+timedelta no arquivo; comparacoes de datetime aware ja sao seguras por timezone em Python; confirmado que pytz -- dependencia real do projeto -- tambem e coberto por .astimezone()) e achou 1 problema real de QUALIDADE DE TESTE: o teste de avanco de DST (marco) usava duracao_segundos=300, mas 01:58 America/New_York + 300s de relogio de parede (02:03) cai DENTRO do proprio buraco de DST daquele dia (02:00-03:00 nao existe) -- por PEP 495/fold=0, esse instante e resolvido com o MESMO offset de antes da transicao, entao o codigo com bug e o codigo corrigido produziam o MESMO delta (300s). O teste passava igual com ou sem o fix, sem discriminar nada -- apesar do proprio docstring alegar cobrir 'a direcao oposta do erro'. Corrigido trocando para duracao_segundos=65*60 (65 min), que ultrapassa o buraco de 1h -- verificado empiricamente (e depois confirmado pela 5a rodada com um teste de MUTACAO: reintroduziu o bug de proposito e confirmou que os 2 testes de DST falham, restaurou o arquivo original em seguida) que o codigo com bug perde exatamente 1h (delta de so 5 min para uma lease pedida de 65 min) enquanto o corrigido acerta os 65 min exatos. Tambem apontou 1 risco residual de baixa severidade, EXPLICITAMENTE fora do escopo desta fatia (confirmado como enquadramento correto pela propria rodada): Lease.__post_init__ so exige tz-AWARENESS, nao identidade UTC -- um construtor futuro de Lease fora de nova_lease (nenhum existe hoje, wiring fica para sub-entrega seguinte) poderia reintroduzir o mesmo bug se fizesse aritmetica direta sobre expires_at sem re-derivar de datetime.now(utc); registrado como pendencia para quem fizer o wiring."
    autoridade: existente_ou_nova
  - id: p04-sub1-rodada5-mutacao-nao-achou-nada
    motivo: "5a rodada de revisao adversarial (Agent tool, general-purpose, foco exclusivo no fix do teste de avanco de DST) verificou com um TESTE DE MUTACAO real (reintroduziu o bug pre-fix numa copia de nova_lease, rodou a suite TestNovaLease inteira contra ela, confirmou que os 2 testes de DST falham com a mensagem exata esperada e todos os demais continuam passando, restaurou o arquivo original e confirmou via git diff que ficou byte-identico) que o teste agora discrimina genuinamente buggy de corrigido, e fez um sweep de 1 a 74 minutos de duracao no mesmo instante para confirmar que 65 min tem margem confortavel (divergencia comeca em 62 min), nao e um caso de borda fragil. Nao achou nada -- criterio de parada atingido, PR pronta para merge."
    autoridade: existente_ou_nova
testes:
  comandos:
    - "python3 -m venv /tmp/hermes_venv_p04 + pip install -r requirements.txt (venv fresco nesta execucao)"
    - "python3 -m unittest test_autonomy_requests -v (isolado, apos cada rodada de fix)"
    - "python3 -m unittest discover -s . -p 'test_*.py' -q (suite completa -- apos merge da PR #325, e apos cada rodada de fix)"
  resultados:
    - "Baseline (main pos-merge da PR #325, commit 3297141): 2334/2334, 0 falhas."
    - "Apos a implementacao inicial (41 testes novos): 41/41 isolado; 2375/2375 na suite completa."
    - "Apos os fixes da rodada 1 (+18 testes, 59 no total): 59/59 isolado; 2393/2393 na suite completa."
    - "Apos o fix da rodada 2 (+3 testes, 62 no total): 62/62 isolado; 2396/2396 na suite completa."
    - "Apos o fix de DST da rodada do Codex (+3 testes, 65 no total): 65/65 isolado; 2399/2399 na suite completa."
    - "Apos o fix do teste de avanco de DST da rodada 4 (contagem inalterada, so valores de um teste existente): 65/65 isolado; 2399/2399 na suite completa -- delta final de +65 sobre o baseline de 2334."
evidencias:
  - "5 rodadas de revisao adversarial independentes (Agent tool, general-purpose, cada uma sem contexto de implementacao nem de rodada anterior alem do que foi passado no prompt) -- ver decisoes p04-sub1-rodada1/2/3/4/5 acima para o detalhe completo de cada achado e correcao. A rodada 4 verificou um achado do proprio Codex na PR (ver decisao p04-sub1-codex-pr326-dst-corrigido); a rodada 5 usou teste de MUTACAO (reintroduziu o bug de proposito, confirmou falha, restaurou o arquivo) para provar que o teste de regressao final discrimina buggy de corrigido de verdade."
  - "PR #326 aberta por esta execucao (https://github.com/andre-martiini/Hermes/pull/326), mesclada apos CI verde nos 3 checks, revisao do Codex com 1 achado real (P2, DST -- corrigido e respondido no proprio thread, resolvido) e nenhum outro comentario/thread pendente. Merge (squash) confirmado no commit ad7aca90a178092cfa766fe047388bde5c321635, verificado via git fetch origin main -- estado deste bloco atualizado para validado NESTA MESMA execucao, antes de encerrar (licao da pendencia registrada no bloco da sub-entrega 32/N, ver docs/autonomia/execucao-archive-p03sub32.md)."
migracao:
  dry_run: null
  executada: false
flags:
  antes: {}
  depois: {}
pendencias:
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: demanda DEV-2026-0013 (race entre consultar_autorizacao_argos e decidir_autorizacao/consumir_autorizacao) -- aguarda priorizacao do Andre."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: sipac_processos.snapshot_hash colidindo com scheduledSipacSync (achado da sub-entrega 23/N), candidato a P05."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: risco residual de validacao de outputSchema em obter_fila_atencao/consultar_elevacoes_sugeridas -- limitacao estrutural do catalogo com outputSchema."
  - "NOVA, RISCO ACEITO documentado em comentario no proprio codigo (functions/autonomy/requests.py, achado da 3a rodada de revisao adversarial): int() em lease_valida_para_acao trunca silenciosamente uma fracao NUMERICA (int(3.9)==3, int(Decimal(\"3.9\"))==3) mas rejeita a mesma fracao em STRING (\"3.5\" -> invalido); bool tambem e aceito como inteiro (int(True)==1) por ser subclasse de int. Nenhum dos dois enfraquece o fencing em si -- nao corrigido por ser mudanca de comportamento (round-trip de igualdade) fora do escopo desta fatia."
  - "NOVA, risco residual de baixa severidade apontado pela 4a rodada de revisao adversarial (achado do Codex, PR #326): Lease.__post_init__ so valida tz-AWARENESS de expires_at, nao identidade UTC -- nova_lease sempre normaliza para UTC antes de construir a Lease, mas o construtor publico do dataclass nao impede alguem de criar uma Lease diretamente com expires_at aware-porem-nao-UTC. Hoje inofensivo (nenhum codigo fora deste modulo/seus testes constroi Lease -- wiring ainda nao existe). Para quem fizer o wiring (proxima fatia de P04): qualquer funcao futura de RENOVACAO de lease deve re-derivar expires_at de datetime.now(timezone.utc) + duracao, nunca fazer aritmetica direta sobre um Lease.expires_at existente sem reconfirmar que already-UTC, ou o mesmo bug de DST desta sub-entrega reaparece por outra porta."
  - "NOVA, achado de processo desta execucao: uma execucao anterior (32/N) escreveu o proprio bloco com estado: validado ANTES de a PR #325 estar de fato mesclada (contradiz o padrao estabelecido desde a sub-entrega 27/N, em que validado so e escrito pela execucao SEGUINTE apos confirmar o merge via git log/mcp__github__pull_request_read). Coincidencia: esta execucao mesclou a PR #325 de qualquer forma, entao o campo acabou correspondendo a realidade -- mas por sorte, nao por disciplina da execucao anterior. Nao corrigido retroativamente (bloco ja arquivado, nao reescrever). Para a proxima execucao: so escrever estado: validado depois de CONFIRMAR o merge de verdade (git log origin/main ou mcp__github__pull_request_read com merged=true), nunca antecipando."
proximo_pacote: "P04 -- primeira fatia (passo 1: estados/leases/geracoes) implementada como logica pura, SEM wiring em producao. RECOMENDACAO desta execucao para a proxima: (1) proxima fatia natural e o passo 3 do pacote (\"criar ledger de operacoes e checkpoints com limites de tamanho\", secao 4.5 itens 6/9/10 -- idempotency_key, hash canonico, conflito quando o mesmo idempotency_key vem com payload diferente) -- tambem pura logica primeiro, mesmo padrao desta fatia, ANTES de comecar a religar agent_requests.py/mcp_jobs.py de verdade; (2) alternativa igualmente valida: comecar o wiring desta fatia (RequestStatus/Lease) em agent_requests.py agora, criando um caminho novo em paralelo ao legado (schema_version novo exige lease; executor legado nao pode concluir um pedido reservado por outro -- secao 9, P04 passo 5) -- decisao de qual vir primeiro (ledger puro vs. wiring desta fatia) fica para a proxima execucao avaliar com mais contexto de quanto tempo/orcamento tem disponivel, ja que P04 e pacote G e nao cabe inteiro em poucas sub-entregas. NOTAS DE PROCESSO: (1) PR #264 continua fechada, nao e mais pendencia; (2) demanda DEV-2026-0013 aguarda priorizacao do Andre; (3) esta execucao encontrou e mesclou uma PR pela metade de uma execucao anterior (PR #325, sub-entrega 32/N) antes de iniciar trabalho novo -- ver decisao p04-sub1-retomada-pr325-em-vez-de-trabalho-novo; (4) achado de processo sobre uma execucao anterior ter escrito estado: validado antes da confirmacao real do merge -- ver pendencia NOVA acima; (5) bloco P03 sub-entrega 31/N arquivado nesta execucao (ver docs/autonomia/execucao-archive-p03sub31.md) por aproximacao do limite de ~50KB; (6) Blueprint da Autonomia (artifact) a atualizar apos a confirmacao deste bloco -- ver relato final desta execucao para o desfecho."
```
