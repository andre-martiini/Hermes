# Arquivo de execução — P04 sub-entrega 3/N

Bloco movido de docs/autonomia/execucao.md em 26/09/2026, pela tarefa agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega P04 4/N (o arquivo ativo passaria de ~64KB, acima do limite de ~50KB, sem re-arquivamento desde o trigésimo arquivo de arquivo, execucao-archive-p04sub1.md). Nenhum conteúdo foi alterado — o bloco já estava com `estado: validado` (confirmado pela própria sub-entrega, que verificou o merge da PR #330 antes de encerrar). Cobre: P04 sub-entrega 3/N (passo 4 do pacote — assumir/renovar/registrar progresso/registrar resultado observado, novo módulo functions/autonomy/execution.py, costurando requests.py + ledger.py).

---

```yaml
plano: plano-hermes-autonomo-2026-09-06
base_commit: 82c110b447f5de4a4653a37b804b8c2ac8f1e985
pacote: "P04 sub-entrega 3/N -- passo 4 do pacote (\"Implementar assumir, renovar, registrar progresso e registrar resultado observado\", secao 4.5 \"Reserva, retomada e idempotencia\" e secao 6.2). Novo modulo functions/autonomy/execution.py, costurando autonomy/requests.py (sub-entrega 1/N) e autonomy/ledger.py (sub-entrega 2/N) num agregado PedidoDuravel, seguindo a RECOMENDACAO explicita do bloco da sub-entrega 2/N (opcao 1: passo 2 ou passo 4 do pacote, ainda logica pura, antes do wiring real). Ao inicio desta execucao, mcp__github__list_pull_requests (estado aberto) nao encontrou nenhuma PR pendente -- nenhuma retomada de trabalho pela metade foi necessaria; branch nova criada a partir do main atual (commit acima)."
estado: validado
inicio: "2026-09-25T17:00:00Z"
fim: "2026-09-25T18:05:00Z"
arquivos_alterados:
  - "functions/autonomy/execution.py (NOVO -- PedidoDuravel (dataclass frozen: request_id/status/lease/ledger_entry), LeaseInvalida (excecao de fencing), assumir_pedido (transiciona para RESERVADO, emite lease nova, exige lease anterior expirada quando existe), renovar_pedido (fencing, estende expires_at mantendo token/geracao, restrito a _STATUS_PERMITE_RENOVACAO_DE_LEASE), registrar_progresso (fencing, abre/reusa entrada do ledger via criar_ou_reusar_entrada, acrescenta checkpoint, transiciona para EM_ANDAMENTO), registrar_resultado_observado (fencing, exige ledger aberto, so aceita um novo_status TERMINAL -- ESTADOS_TERMINAIS --, transicao implicita via VERIFICANDO quando necessario, sela o resultado no ledger; bypass de identidade-sem-expiracao para reentrega de um resultado terminal ja selado e identico), pedido_esta_ativo. _transicionar/_transicionar_para_resultado: duas variantes deliberadamente diferentes da validacao de transicao (a 2a aceita novo_status==status atual como no-op e tenta o caminho implicito por VERIFICANDO)."
  - "functions/autonomy/requests.py (+renovar_lease -- estende expires_at mantendo token/geracao, ao contrario de nova_lease que sempre incrementa geracao; +lease_pertence_ao_apresentante -- identidade token/geracao extraida de lease_valida_para_acao, SEM checar expiracao, para o bypass de reentrega de registrar_resultado_observado; lease_valida_para_acao vira wrapper fino que soma a checagem de expiracao)."
  - "functions/test_autonomy_execution.py (NOVO -- 36 testes: assumir (reserva, geracao incrementa ao reassumir, lease anterior ainda valida bloqueia reassumir), renovar (mantem token/geracao, rejeita token/geracao/lease errados, rejeita status fora de _STATUS_PERMITE_RENOVACAO_DE_LEASE inclusive RETENTATIVA_AGENDADA/RESULTADO_DESCONHECIDO/terminais), progresso (abre e reusa ledger, dedup de checkpoint, conflito de payload, fencing), resultado observado (conclui, aceita FALHA_FINAL, rejeita status nao-terminal sem selar o ledger, ponta-a-ponta confirmando que a rejeicao nao trava uma retentativa real, reentrega idempotente mesmo com lease expirada mas exigindo token correto e rejeitando resultado diferente), pedido_esta_ativo."
decisoes:
  - id: p04-sub3-execution-como-agregado-puro
    motivo: "PedidoDuravel e o agregado que o wiring real (Firestore) vai precisar montar a partir de um documento -- mas ainda sem nenhum I/O aqui, mesmo padrao incremental das sub-entregas 1/N e 2/N. As quatro operacoes (assumir/renovar/progresso/resultado) correspondem as quatro ferramentas MCP propostas na secao 6.2 (assumir_pedido_agente, renovar_pedido_agente, registrar_progresso_agente, registrar_resultado_observado)."
    autoridade: existente_ou_nova
  - id: p04-sub3-rodada1-critico-selagem-de-ledger-para-status-nao-terminal
    motivo: "1a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto da implementacao) achou 3 problemas reais. O mais grave: registrar_resultado_observado selava o ledger da operacao PERMANENTEMENTE (autonomy.ledger.registrar_resultado nunca sobrescreve) para QUALQUER novo_status, inclusive nao-terminais (RETENTATIVA_AGENDADA, AGUARDANDO_APROVACAO, AGUARDANDO_EXTERNO, RESULTADO_DESCONHECIDO) -- como adicionar_checkpoint recusa checkpoint novo numa entrada selada, isso travava qualquer retentativa da MESMA operacao num beco sem saida (reproduzido concretamente pelo revisor: reassumir apos RETENTATIVA_AGENDADA e tentar registrar_progresso de novo, com a MESMA chave ou uma nova, falhava dos dois jeitos). Corrigido: novo_status agora so aceita um estado TERMINAL do pedido (ESTADOS_TERMINAIS), rejeitado ANTES de tocar o ledger -- com um teste de ponta a ponta confirmando que a rejeicao nao impede a retentativa real de terminar depois."
    autoridade: existente_ou_nova
  - id: p04-sub3-rodada1-renovar-pedido-terminal
    motivo: "2o achado real da 1a rodada: renovar_pedido nao verificava se o pedido ja tinha chegado a um estado terminal -- nenhuma funcao limpa pedido.lease ao concluir, entao uma lease emitida antes da conclusao continuava 'renovavel' ate seu proprio vencimento original mesmo com o pedido ja CONCLUIDO/FALHA_FINAL, um estado internamente contraditorio. Corrigido (nesta rodada, depois generalizado pelo achado do Codex -- ver decisao p04-sub3-codex-pr330-p1)."
    autoridade: existente_ou_nova
  - id: p04-sub3-rodada1-assumir-pedido-lease-ainda-valida
    motivo: "3o achado real da 1a rodada: assumir_pedido nao verificava se uma lease existente ainda estava valida antes de reatribui-la a outro executor -- dependia so da maquina de estados permitir a transicao para RESERVADO, entao um segundo executor podia 'roubar' silenciosamente uma lease ainda com minutos de validade sempre que o status permitisse (ex.: RETENTATIVA_AGENDADA). Corrigido: assumir_pedido agora exige lease_expirada(pedido.lease) quando ja existe uma lease anterior, antes de emitir a nova."
    autoridade: existente_ou_nova
  - id: p04-sub3-rodada2-nao-achou-nada
    motivo: "2a rodada de revisao adversarial (foco exclusivo nos 3 fixes da rodada 1, execucao direta e reproducao manual de casos-limite) confirmou os 3 fixes como corretos, a ordem de validacao correta em cada funcao, e os testes novos como nao-vacuosos -- NAO achou nada real. Criterio de parada atingido, PR #330 aberta."
    autoridade: existente_ou_nova
  - id: p04-sub3-codex-pr330-p1-lease-zumbi-bloqueia-retentativa
    motivo: "Revisao automatica do Codex (chatgpt-codex-connector, bot configurado no repositorio) na PR #330 achou 2 problemas reais (P1 e P2), ambos verificados por execucao direta e corrigidos. P1: o fix p04-sub3-rodada1-renovar-pedido-terminal so excluia ESTADOS_TERMINAIS, mas nao RETENTATIVA_AGENDADA/RESULTADO_DESCONHECIDO -- quando outro mecanismo move um pedido para RETENTATIVA_AGENDADA preservando a lease (o proprio fluxo de retentativa construido pelos testes desta sub-entrega), um executor 'zumbi' podia continuar renovando essa lease indefinidamente, e como assumir_pedido corretamente recusa reassumir uma lease ainda valida, isso impedia PARA SEMPRE que a retentativa fosse reivindicada por qualquer outro executor. Corrigido: renovar_pedido agora usa uma lista de PERMISSAO (_STATUS_PERMITE_RENOVACAO_DE_LEASE = RESERVADO/EM_ANDAMENTO/VERIFICANDO/AGUARDANDO_APROVACAO/AGUARDANDO_EXTERNO) em vez de uma lista de bloqueio (nao-terminal) -- verificado que AGUARDANDO_APROVACAO/AGUARDANDO_EXTERNO nao tem o mesmo problema porque nenhuma transicao de _TRANSICOES_PERMITIDAS leva desses dois estados a RESERVADO nem a RETENTATIVA_AGENDADA (rodada de revisao seguinte confirmou isso por leitura direta da tabela)."
    autoridade: existente_ou_nova
  - id: p04-sub3-codex-pr330-p2-reentrega-pos-lease-expirada
    motivo: "P2 do Codex na PR #330: se a resposta da 1a chamada de registrar_resultado_observado se perdesse (timeout de rede) e o consumidor retentasse com o MESMO token/geracao depois do vencimento natural da lease (5 minutos por padrao), a reentrega idempotente do item 9 da secao 4.5 falhava com 'lease expirada' numa operacao cujo resultado JA estava definitivamente selado -- contradizendo a propria garantia de reentrega do item 9. Corrigido: extraida autonomy.requests.lease_pertence_ao_apresentante (identidade token/geracao, SEM checar expiracao) de lease_valida_para_acao (que continua fazendo as duas checagens para todo o resto); registrar_resultado_observado usa a nova funcao SO no caminho especifico de reentrega de um resultado terminal ja selado e identico (pedido.status in ESTADOS_TERMINAIS AND novo_status == pedido.status AND ledger_entry.resultado_registrado_em is not None), preservando a exigencia de identidade correta (LeaseInvalida se token/geracao nao conferem, mesmo com lease expirada) e a garantia de nunca sobrescrever (autonomy.ledger.registrar_resultado continua arbitro final, ValueError para resultado diferente)."
    autoridade: existente_ou_nova
  - id: p04-sub3-rodada3-nao-achou-nada
    motivo: "3a rodada de revisao adversarial (foco exclusivo nos 2 fixes do Codex, incluindo diff direto contra a versao pre-refatoracao de lease_valida_para_acao, execucao de inputs adversariais contra lease_pertence_ao_apresentante, e leitura de _TRANSICOES_PERMITIDAS para confirmar que AGUARDANDO_APROVACAO/AGUARDANDO_EXTERNO nao tem caminho ate RESERVADO ou RETENTATIVA_AGENDADA) confirmou os 2 fixes como corretos e NAO achou nada real -- criterio de parada atingido."
    autoridade: existente_ou_nova
  - id: p04-sub3-escopo-registrar-resultado-so-terminal
    motivo: "registrar_resultado_observado (apos o fix do achado critico da rodada 1) so aceita novo_status em ESTADOS_TERMINAIS (CONCLUIDO/FALHA_FINAL/CANCELADO). Isso significa que NENHUMA funcao deste modulo consegue hoje transicionar um pedido para RETENTATIVA_AGENDADA ou RESULTADO_DESCONHECIDO -- esses dois estados intermediarios (retentativa agendada, resultado ainda desconhecido aguardando reconciliacao) ficam para outro mecanismo (o sweep do passo 7 do pacote P04, ou uma futura funcao de transicao de status que nao toque o ledger), deliberadamente fora do escopo desta fatia. O teste de ponta-a-ponta desta sub-entrega simula esse estado intermediario construindo um PedidoDuravel manualmente (nao existe hoje um caminho de producao que o produza)."
    autoridade: existente_ou_nova
testes:
  comandos:
    - "/tmp/hermes_venv_p04sub3 (venv fresco nesta execucao) + pip install -r requirements.txt"
    - "python3 -m unittest test_autonomy_execution test_autonomy_requests -v (isolado, apos cada rodada de fix)"
    - "python3 -m unittest discover -s . -p 'test_*.py' -q (suite completa -- apos cada rodada de fix)"
  resultados:
    - "Baseline (main pos-merge da PR #329, commit 82c110b): 2481/2481, 0 falhas."
    - "Apos a implementacao inicial (27 testes novos em execution.py): 27/27 isolado; 2508/2508 na suite completa."
    - "Apos os 3 fixes da 1a rodada de revisao adversarial (+4 testes, 31 no total): 31/31 isolado; 2512/2512 na suite completa."
    - "Apos os 2 fixes dos achados do Codex na PR #330 (+5 testes em execution.py, 36 no total; requests.py sem mudanca de contagem, 65/65 continuam passando apos a refatoracao de lease_valida_para_acao): 101/101 (execution+requests) isolado; 2517/2517 na suite completa -- delta final de +36 sobre o baseline de 2481."
evidencias:
  - "3 rodadas de revisao adversarial independentes (Agent tool, general-purpose, cada uma sem contexto de implementacao nem de rodada anterior alem do que foi passado no prompt): rodada 1 achou 3 problemas reais (1 critico), rodada 2 (foco nos fixes da rodada 1) nao achou nada, rodada 3 (foco nos fixes dos achados do Codex) nao achou nada. Mais a revisao automatica do Codex (bot do repositorio) na propria PR #330, com 2 achados reais (P1 e P2) -- ver decisoes p04-sub3-codex-pr330-p1/p2 acima."
  - "PR #330 aberta por esta execucao (https://github.com/andre-martiini/Hermes/pull/330), CI verde nos 3 checks (GitGuardian, Testes e build, Testes das Functions Python) no commit final (ec626c4). 2 threads de revisao do Codex, ambas respondidas (explicando o fix e apontando o commit ec626c4) e resolvidas via mcp__github__resolve_review_thread antes do merge. Mesclada (squash) no commit 6c84f12431b9955f444ea39d611bc58b9a6c640c, confirmado via git fetch origin main -- estado deste bloco escrito como validado NESTA MESMA execucao, apos confirmar o merge de verdade."
  - "git push --delete da branch claude/p04-sub3-execucao-orquestracao apos o merge falhou com HTTP 403 (mesma limitacao ja registrada nas sub-entregas 1/N e 2/N -- permissao do token/proxy nao cobre delete de branch remota). Nao bloqueia nada -- so um item de limpeza manual."
migracao:
  dry_run: null
  executada: false
flags:
  antes: {}
  depois: {}
pendencias:
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: demanda DEV-2026-0013 (race entre consultar_autorizacao_argos e decidir_autorizacao/consumir_autorizacao) -- aguarda priorizacao do Andre."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: sipac_processos.snapshot_hash colidindo com scheduledSipacSync (achado da sub-entrega 23/N), candidato a P05."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: risco residual de validacao de outputSchema em obter_fila_atencao/consultar_elevacoes_sugeridas -- limitacao estrutural do catalogo com outputSchema."
  - "ABERTA, ja registrada no bloco da sub-entrega 1/N e nao tocada por esta: Lease.__post_init__ so valida tz-AWARENESS de expires_at, nao identidade UTC -- risco residual de baixa severidade para quem fizer o wiring de renovacao de lease (autonomy/requests.py)."
  - "NOVA, item de limpeza manual (nao bloqueia nada): branch claude/p04-sub3-execucao-orquestracao nao pode ser deletada remotamente por esta execucao (git push --delete -> HTTP 403). Mesma limitacao ja registrada nas sub-entregas 1/N e 2/N -- pode ser deletada manualmente pelo Andre pela UI do GitHub."
  - "NOVA, observacao nao-bloqueante da 3a rodada de revisao adversarial (fora do escopo desta fatia): AGUARDANDO_APROVACAO nao tem transicao de _TRANSICOES_PERMITIDAS para nenhum caminho de retentativa/falha (RETENTATIVA_AGENDADA/FALHA_FINAL) se uma aprovacao for rejeitada ou expirar -- so para EM_ANDAMENTO ou CANCELADO. Uma questao de design da maquina de estados em autonomy/requests.py, nao algo que esta sub-entrega tenha introduzido ou precise resolver; candidato a revisao quando o pacote P04 tratar aprovacao de verdade (a fila de atencao/outbox_aprovacao ja existe, mas o vinculo com RequestStatus.AGUARDANDO_APROVACAO ainda nao foi desenhado)."
  - "NOVA, escopo explicito desta fatia (nao e pendencia, e decisao deliberada -- ver decisao p04-sub3-escopo-registrar-resultado-so-terminal): nenhuma funcao deste modulo produz RETENTATIVA_AGENDADA ou RESULTADO_DESCONHECIDO hoje. Fica para o sweep do passo 7 do pacote P04 ('Criar sweep de leases vencidos, resultado desconhecido, retry agendado e falha final, com fila de diagnostico') ou uma funcao de transicao de status dedicada que nao toque o ledger."
proximo_pacote: "P04 -- passo 4 do pacote (orquestracao de assumir/renovar/progresso/resultado, seções 4.5 e 6.2) implementado como logica pura, SEM wiring em producao -- soma-se aos passos 1 (estados/leases/geracoes, sub-entrega 1/N) e 3 (ledger de operacoes/checkpoints, sub-entrega 2/N). RECOMENDACAO desta execucao para a proxima: (1) proxima fatia natural e o passo 2 do pacote ('servidor cria agent_run ao reservar/iniciar; conclusao e timeout sao transicoes verificadas, nao simples add de resumo') -- ainda logica pura, fecha o ultimo passo 'facil' antes do wiring real; (2) alternativa igualmente valida: comecar o passo 7 do sweep (leases vencidos, resultado desconhecido, retry agendado, falha final) agora que ha uma pendencia explicita registrada nesta sub-entrega mostrando que RETENTATIVA_AGENDADA/RESULTADO_DESCONHECIDO nao tem NENHUM produtor ainda -- o sweep seria o primeiro; (3) alternativa igualmente valida: comecar o wiring de verdade agora (passo 5: preservar consultar/concluir legados, pedidos schema_version novo exigem lease) -- decisao de qual vir primeiro fica para a proxima execucao avaliar com mais contexto de orcamento disponivel, ja que P04 e pacote G (10 passos) e ainda faltam varios. NOTAS DE PROCESSO: (1) esta execucao NAO encontrou PR pendente de execucao anterior ao iniciar (list_pull_requests vazio) -- trabalho novo iniciado diretamente; (2) 3 rodadas de revisao adversarial interna + 2 achados reais do Codex (bot do repositorio) na PR, todos corrigidos e verificados por rodadas adicionais -- ver decisoes acima, exemplo concreto de por que vale sempre rodar mais uma rodada focada no PROPRIO fix; (3) branch remota nao pode ser deletada por esta execucao (HTTP 403, mesma limitacao recorrente) -- ver pendencia NOVA acima; (4) Blueprint da Autonomia (artifact) a atualizar apos a confirmacao deste bloco -- ver relato final desta execucao para o desfecho."
```
