# Registro de execução — plano-hermes-autonomo-2026-09-06

Um bloco por pacote (P00–P18), no formato da seção 14.2 do plano. Cada pacote novo é acrescentado ao final; nenhum bloco anterior é reescrito depois de `validado`. Sem segredos nem conteúdo privado — apenas referências a artefatos e IDs.

**Blocos P00 a P02 sub-entrega 11/N foram movidos para
docs/autonomia/execucao-archive-p00-a-p02sub11.md em 2026-09-09 (P02
sub-entrega 15/N). Blocos P02 sub-entrega 12/N a 16/N foram movidos, em
2026-09-09 também, para um SEGUNDO arquivo de arquivo,
docs/autonomia/execucao-archive-p02-sub12-a-sub16.md — durante a correção
da sub-entrega P01 5/N, depois que uma escrita anterior derrubou
silenciosamente o bloco P02 sub-entrega 17/N na retranscrição manual de
um arquivo de 85701 bytes, e uma tentativa de reenviar tudo somado num
arquivo de arquivo só (159725 bytes) falhou por limite de saída (64000
tokens). O bloco P02 sub-entrega 17/N foi movido, também em 2026-09-09,
para um TERCEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p02-sub17.md — durante a sub-entrega P01
6/N, preventivamente. Os blocos P01 sub-entrega 3/N e 4/N foram movidos,
ainda em 2026-09-09, para um QUARTO arquivo de arquivo,
docs/autonomia/execucao-archive-p01-sub3-a-sub4.md — durante a sub-entrega
P01 7/N, também preventivamente. O bloco P01 sub-entrega 5/N foi movido,
ainda em 2026-09-09, para um QUINTO arquivo de arquivo,
docs/autonomia/execucao-archive-p01-sub5.md — durante a sub-entrega P01
8/N, preventivamente pelo mesmo motivo. Os blocos P01 sub-entrega 6/N a
9/N (fechamento do pacote) foram movidos, ainda em 2026-09-09, para um
SEXTO arquivo de arquivo, docs/autonomia/execucao-archive-p01-sub6-a-sub9.md
— durante a sub-entrega P02 19/N, preventivamente, ao registrar
retroativamente as sub-entregas P02 18/N e 19/N. Os blocos P02
sub-entrega 18/N a P03 sub-entrega 3/N foram movidos, em 2026-09-12, para
um SÉTIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p02sub18-a-p03sub3.md — pela tarefa
agendada de avanço autônomo, ao registrar retroativamente a sub-entrega
P03 7/N, preventivamente (o arquivo ativo tinha passado de ~50KB sem
re-arquivamento desde o sexto arquivo). Ver o cabeçalho de cada arquivo
de arquivo para o relato completo. Os blocos P03 sub-entrega 4/N e 5/N foram
movidos, em 12/09/2026, para um OITAVO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub4-a-sub5.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 8/N,
preventivamente (o arquivo ativo passaria de ~50KB ao acrescentar essa
entrada sem re-arquivamento). Os blocos P03 sub-entrega 6/N e 7/N foram
movidos, em 13/09/2026, para um NONO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub6-a-sub7.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 10/N,
preventivamente (mesmo motivo). Os blocos P03 sub-entrega 8/N e 9/N foram
movidos, também em 13/09/2026, para um DÉCIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub8-a-sub9.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 11/N
(o arquivo ativo passaria de ~50KB ao acrescentar essa entrada sem
re-arquivamento). Os blocos P03 sub-entrega 10/N e 11/N foram movidos, em
14/09/2026, para um DÉCIMO PRIMEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub10-a-sub11.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 13/N
(o arquivo ativo passaria de ~50KB ao acrescentar essa entrada sem
re-arquivamento). O bloco P03 sub-entrega 12/N foi movido, também em
14/09/2026, para um DÉCIMO SEGUNDO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub12.md — durante a correção do
registro do diário da sub-entrega 14/N (incidente de processo: um
subagente sem autorização abriu duas PRs indevidas para este mesmo
registro, uma delas mesclada pelo André antes da correção chegar — ver a
entrada da sub-entrega 14/N para o relato completo), preventivamente
(o arquivo ativo chegaria perto de ~50KB com as correções necessárias
sem re-arquivamento). O bloco P03 sub-entrega 13/N foi movido, em
14/09/2026, para um DÉCIMO TERCEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub13.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 15/N
(o arquivo ativo passaria de ~50KB ao acrescentar essa entrada sem
re-arquivamento). Os blocos P03 sub-entrega 14/N e 15/N foram movidos,
também em 14/09/2026, para um DÉCIMO QUARTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub14-a-sub15.md — pela tarefa
agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega
P03 16/N (o arquivo ativo chegaria a 48157 bytes, perto do limite de
~50KB, sem re-arquivamento). Os blocos P03 sub-entrega 16/N e 17/N
foram movidos, em 15/09/2026, para um DÉCIMO QUINTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub16-a-sub17.md — pela tarefa
agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega
P03 19/N (o arquivo ativo passaria de 60KB, bem acima do limite de
~50KB, ao acrescentar essa entrada sem re-arquivamento). Os blocos P03
sub-entrega 18/N e 19/N foram movidos, em 22/09/2026, para um DÉCIMO
SEXTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub18-a-sub19.md — pela tarefa
agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega
P03 20/N (o arquivo ativo passaria de ~50KB ao acrescentar essa
entrada sem re-arquivamento). O bloco P03 sub-entrega 20/N foi movido,
também em 22/09/2026, para um DÉCIMO SÉTIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub20.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 22/N (o
arquivo ativo tinha chegado a 52198 bytes, acima do limite de ~50KB,
sem re-arquivamento desde o décimo sexto arquivo). Nenhum
conteúdo foi perdido; é uma relocação, não uma edição (o campo `estado`
desse bloco foi atualizado de `pronto_para_revisao` para `validado` no
arquivamento, já que o próprio bloco P03 sub-entrega 21/N confirma o
merge das PRs #304/#305 — ver o cabeçalho do arquivo de arquivo). O
bloco P03 sub-entrega 21/N foi movido, em 23/09/2026, para um DÉCIMO
OITAVO arquivo de arquivo, docs/autonomia/execucao-archive-p03sub21.md
— pela tarefa agendada de avanço autônomo, ao acrescentar a entrada da
sub-entrega P03 23/N (o arquivo ativo tinha chegado a 54816 bytes,
acima do limite de ~50KB, sem re-arquivamento desde o décimo sétimo
arquivo). Mesmo critério de relocação + confirmação de validação (o
campo `estado` desse bloco foi atualizado de `pronto_para_revisao` para
`validado`, já que o próprio bloco P03 sub-entrega 22/N confirma o
merge das PRs #306/#307 — ver o cabeçalho do arquivo de arquivo). Este
arquivo continua sendo a fonte de verdade para tudo a partir da
sub-entrega P03 22/N em diante.

---

```yaml
plano: plano-hermes-autonomo-2026-09-06
base_commit: 50ccb1eda4cfa85158894118b8e11ddb09030b94
pacote: "P03 sub-entrega 22/N -- idempotentHint por handler (passo 3 do plano), setima fatia: campo `idempotencia` classificado por leitura direta do handler real para `confirmar_edicao_acao`, a versao singular do par edicao/edicao-em-lote ja identificada como candidata obvia pela sub-entrega 21/N -- MUDANCA DE PROCESSO NESTA EXECUCAO: o Andre autorizou explicitamente, em 22/09/2026, que esta rotina passe a dar push direto no GitHub (sem intermediario de escrita de arquivo) e a mesclar as proprias PRs sem aprovacao humana previa, substituindo o fluxo anterior de 'abrir PR e aguardar merge manual' -- ver decisao correspondente abaixo. Confirmado no inicio desta execucao, via clone git direto, que as PRs #306 (codigo da sub-entrega 21/N) e #307 (diario da 21/N) estavam abertas e limpas (CI verde, revisao Codex sem achados, sem comentarios pendentes) -- mescladas por esta propria execucao, sob a nova autoridade, em vez de aguardar o Andre, retomando o trabalho da sessao anterior em vez de duplica-lo"
estado: pronto_para_revisao
inicio: "2026-09-22T19:47:00Z"
fim: "2026-09-22T20:35:00Z"
arquivos_alterados:
  - "functions/tools/inventory.py (1 classificacao nova: confirmar_edicao_acao, NAO_IDEMPOTENTE -- chama a MESMA callable main.py::confirmarEdicaoAcao que editar_acao (sub-entrega 16/N), mesma raiz: ArrayUnion incondicional a cada chamada bem-sucedida. Nuance documentada: ao contrario de editar_acao, esta tool repassa snapshot_ts (parametro OPCIONAL do schema, devolvido por preparar_edicao_acao) para a callable, que recusa a gravacao com status:'invalidated' SEM escrever nada se o valor nao bater com data_atualizacao atual -- como a propria gravacao ja reescreve esse campo, uma segunda chamada identica com o MESMO snapshot_ts falha em vez de duplicar a nota, uma auto-limitacao real que o grupo em lote da sub-entrega 21/N nao tem. Nao e idempotencia de verdade (a segunda chamada devolve um resultado DIFERENTE da primeira -- erro, nao o mesmo sucesso -- e a protecao so existe quando o chamador de fato envia snapshot_ts, campo opcional que um cliente MCP pode omitir); classificacao conservadora mantida em NAO_IDEMPOTENTE, mesmo criterio de 'hint errado e pior que omissao' ja usado neste inventario)"
  - "functions/tools/registry.py (docstring de mcp_annotations() atualizado: contagem 47->48 de ~61 tools elegiveis nas DUAS ocorrencias do numero no arquivo; nova secao descrevendo a fatia 22/N; paragrafo final atualizado para as ~5 tools realmente nao investigadas, confirmar_edicao_acao removida da lista)"
  - "functions/test_mcp_annotations.py (confirmar_edicao_acao acrescentada na lista parametrizada NAO_IDEMPOTENTE; contagem no docstring do modulo corrigida de 47 para 48, sub-entregas 16/N-22/N)"
  - "functions/test_confirmar_edicao_acao_nao_idempotente.py (NOVO -- 3 testes por HANDLER REAL: chama main.confirmarEdicaoAcao de verdade via inspect.unwrap (mesma tecnica de test_confirmar_lote_nao_idempotente.py, sub-entrega 21/N) com um FakeDB que suporta .get() alem de .update() (diferente do FakeDB grava-cega da sub-entrega 21/N, porque esta callable LE antes de escrever); prova (1) que sem snapshot_ts, repetir a mesma chamada grava DUAS notas de diario; (2) que COM snapshot_ts, a segunda chamada identica falha com status:'invalidated' SEM gravar nada -- a nuance da auto-limitacao real; (3) que o wrapper MCP (_HANDLERS['confirmar_edicao_acao']) repassa snapshot_ts->snapshotTs corretamente, inclusive a ausencia (vira string vazia)"
decisoes:
  - id: p03-sub22-nova-autoridade-push-direto-e-merge-autonomo
    motivo: "o prompt desta rotina foi atualizado pelo Andre em 22/09/2026 para autorizar push direto no GitHub e merge das proprias PRs sem aprovacao humana previa, substituindo o fluxo anterior (Argos como intermediario de escrita de arquivo + 'nao mesclar automaticamente, merge manual do Andre' no corpo de cada PR). Interpretacao desta execucao: a nova autoridade cobre PRs desta mesma linha de trabalho (o plano de autonomia do Hermes), incluindo as duas PRs #306/#307 que uma execucao anterior tinha aberto sob o fluxo antigo e deixado sem merge -- nao apenas PRs abertas a partir desta execucao especifica. Fundamentacao: o proprio prompt da rotina instrui explicitamente checar 'se a sessao anterior desta MESMA rotina ja deixou algo pela metade (branch aberto sem PR, PR aberta sem merge...) -- retome de onde parou em vez de duplicar', o que so faz sentido sob a nova autoridade se ela se aplica retroativamente as PRs em voo desta rotina. A excecao explicita (PR #264) e para uma PR de terceiro/erro de sessao anterior nao relacionada a este fluxo normal, nao para PRs regulares do plano. Antes de mesclar, confirmado que ambas estavam limpas: CI verde (3/3 checks), revisao Codex sem achados postados, mergeable_state=clean, nenhum comentario aberto do Andre."
    autoridade: nova
  - id: p03-sub22-fluxo-de-escrita-mudou-de-argos-para-git-direto
    motivo: "diferente das sub-entregas 1/N-21/N (que usavam mcp__Argos__argos_escrever_arquivo_repositorio com verificacao de sha), esta execucao roda com o repositorio ja clonado localmente e faz commit/push diretos via git -- conforme a nova instrucao da rotina. Integridade da escrita verificada de outro jeito: leitura dos arquivos de volta do proprio working tree apos a edicao (Read/Edit tools operam no mesmo arquivo que sera commitado, sem intermediario de rede) e suite de testes completa rodando contra o working tree local antes do commit."
    autoridade: nova
  - id: p03-sub22-classificacao-nao-idempotente-mesma-raiz-do-editar-acao
    motivo: "leitura direta de main.py::confirmarEdicaoAcao confirma ArrayUnion incondicional a cada chamada bem-sucedida, mesmo padrao ja estabelecido para editar_acao (sub-entrega 16/N) e o grupo em lote (sub-entrega 21/N). O valor final dos campos editados converge, mas o diario cresce a cada repeticao -- efeito adicional, logo NAO_IDEMPOTENTE, mesmo com a nuance do snapshot_ts (ver proximo item)."
    autoridade: existente_ou_nova
  - id: p03-sub22-achado-snapshot-ts-e-auto-limitacao-real-mas-nao-idempotencia
    motivo: "leitura de tools/hermes_tools.py::_map_confirmar_edicao_acao (repassa snapshot_ts->snapshotTs, diferente de editar_acao que nunca envia esse campo) e do schema confirmar_edicao_acao.json (snapshot_ts e OPCIONAL, nao required) revela uma diferenca real desta tool em relacao ao resto do grupo: quando o chamador usa o fluxo normal (snapshot_ts devolvido por preparar_edicao_acao), a callable recusa a SEGUNDA chamada identica com status:'invalidated' ANTES de escrever (nao so um status de erro depois de gravar mesmo assim -- confirmado por teste e por mutacao na revisao adversarial). Decisao desta execucao: isso NAO e idempotencia segundo a definicao da especificacao MCP (repetir a chamada deveria produzir o MESMO efeito observavel da primeira -- aqui produz um efeito DIFERENTE, um erro), entao a classificacao continua NAO_IDEMPOTENTE, mas a nuance foi documentada em detalhe na nota do inventario e no docstring de registry.py, em vez de omitida ou simplificada para bater com o padrao das outras 4 tools do grupo em lote (que nao tem protecao nenhuma)."
    autoridade: existente_ou_nova
  - id: p03-sub22-rodada1-nao-achou-nada
    motivo: "1a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto da implementacao): releu de forma independente o handler real (main.py::confirmarEdicaoAcao), o schema, o wrapper MCP e o teste novo; confirmou as duas claims centrais (diario duplica sem snapshot_ts; segunda chamada falha ANTES de escrever com snapshot_ts) por MUTACAO real no codigo (quebrou deliberadamente o ArrayUnion e depois a guarda do snapshot_ts, confirmou que os testes relevantes falhavam exatamente como esperado, reverteu, confirmou git status limpo); rodou a suite completa (2126/2126); conferiu que as DUAS ocorrencias da contagem em registry.py foram atualizadas. Nao achou nada de errado -- resultado relatado aqui como esta, sem forcar um achado. Criterio de parada atingido (uma rodada limpa e suficiente quando a primeira ja nao acha nada)."
    autoridade: existente_ou_nova
testes:
  comandos:
    - "repositorio ja clonado no inicio da execucao (branch main, sem uso de conector Argos para leitura/escrita de arquivo)"
    - "python3 -m venv + pip install -r requirements.txt"
    - "python3 -m unittest test_confirmar_edicao_acao_nao_idempotente test_mcp_annotations test_tool_inventory -v (arquivos isolados)"
    - "python3 -m unittest discover -s . -p 'test_*.py' -q (suite completa, antes da implementacao e depois da revisao adversarial, incluindo durante a mutacao/reversao da revisao)"
  resultados:
    - "Baseline (origin/main, commit 50ccb1e, PRs #306/#307 ja mescladas por esta propria execucao antes de comecar a implementacao): 2123/2123, 0 falhas, 0 erros."
    - "Apos a implementacao completa + a rodada de revisao adversarial: 2126/2126, 0 falhas, 0 erros, sem regressao (2123 baseline + 3 testes novos)."
    - "test_confirmar_edicao_acao_nao_idempotente isolado: 3/3 OK. test_mcp_annotations isolado: 24/24 OK. test_tool_inventory isolado: 15/15 OK (42 no total dos 3 arquivos)."
evidencias:
  - "1 rodada de revisao adversarial independente (Agent tool, general-purpose, sem contexto da implementacao) -- nao achou nada de errado, incluindo verificacao por MUTACAO real (nao so leitura) das duas claims centrais. Ver decisoes para o relato completo. Parada aqui, conforme a disciplina (escalar so enquanto uma rodada acha algo real)."
  - "PRs #306 e #307 (sub-entrega 21/N, herdadas de uma execucao anterior desta mesma rotina que as tinha deixado abertas sob o fluxo antigo) confirmadas limpas e mescladas por esta execucao ANTES de comecar a implementacao desta sub-entrega -- squash merge, mesmo padrao ja usado pelo historico do repositorio (commits anteriores com sufixo (#N), sem merge commits)."
  - "PR desta sub-entrega (22/N) sera aberta e mesclada por esta mesma execucao apos o registro deste diario, sob a nova autoridade -- ver decisao p03-sub22-nova-autoridade-push-direto-e-merge-autonomo. Numero da PR e confirmacao de merge ficam para o relato final desta execucao (nao disponivel no momento de escrever este bloco, que e commitado JUNTO com o codigo na mesma PR)."
migracao:
  dry_run: null
  executada: false
flags:
  antes: {}
  depois: {}
pendencias:
  - "ABERTA: PR #264 (aberta por engano durante a correcao do diario da sub-entrega 14/N) continua aberta com um bug real de escaping YAML -- deve ser fechada sem mesclar pelo Andre; fora do escopo desta rotina (excecao explicita no prompt)."
  - "ABERTA, ja registrada no bloco da sub-entrega 20/N e nao tocada por esta: demanda DEV-2026-0013 (race entre consultar_autorizacao_argos e decidir_autorizacao/consumir_autorizacao) -- aguarda priorizacao do Andre."
  - "ABERTA, deliberadamente: idempotentHint para as ~5 tools de escrita/leitura_e_escrita ainda nunca investigadas continua fora de escopo -- acompanhar_processo_sipac, consolidar_whatsapp, consultar_contatos_prioritarios_secretario, registrar_inscricao_bolsa_publica, schedule_whatsapp_message. Mais 8 tools seguem investigadas e deliberadamente sem classificacao por ambiguidade genuina -- ver notas de cada uma em tools/inventory.py."
  - "Pendencias ja registradas em blocos anteriores e nao tocadas por esta sub-entrega continuam abertas: ver blocos P03 sub-entrega 16/N a 21/N para a lista completa (religar inventario tipado a decisao de politica real; ambiguidades de classificacao; card do Telegram de rascunho degradado; observabilidade de claim pendente; TTL do Firestore para mcp_jobs; limite de 200KB do Argos (pode nao se aplicar mais, ja que o fluxo de escrita mudou de Argos para git direto -- a validar); passo 2 do P02 como hardening futuro; salvar_memoria_global sem teste de handler dedicado; caveat de concorrencia em registrar_saude/consultar_investimentos; outputSchema para as tools restantes do catalogo; a fatia envelope restante do passo 3 do P03)."
proximo_pacote: "P03 -- sete fatias de idempotentHint entregues (16/N-22/N), 48 de ~61 tools elegiveis classificadas, ~5 tools de escrita nunca investigadas restantes (acompanhar_processo_sipac, consolidar_whatsapp, consultar_contatos_prioritarios_secretario, registrar_inscricao_bolsa_publica, schedule_whatsapp_message). Sob a NOVA autoridade (push direto + merge autonomo), a proxima execucao NAO precisa mais esperar confirmacao de merge do Andre -- deve ler o estado real do repositorio (git log/gh pr list) para confirmar se esta sub-entrega 22/N foi mesclada por esta propria execucao (deveria ter sido, ver evidencias) e decidir entre: (a) mais uma fatia de idempotentHint para as ~5 tools restantes; (b) a fatia envelope restante do passo 3 do P03 (outputSchema/structuredContent para as tools sem contrato ainda); (c) um levantamento novo do catalogo para outputSchema (nenhuma candidata nova investigada desde a sub-entrega 15/N); ou (d) avancar para P04 (pedidos duraveis/lease/ledger) -- com sete fatias de idempotentHint entregues e o passo 3 do P03 ainda com trabalho de outputSchema/envelope pendente, P03 NAO esta fechado ainda (varios passos do plano -- normalizador, expected_version/idempotency_key, paginacao, paralelismo -- seguem sem cobertura desta serie de sub-entregas focadas so em idempotentHint); recomendacao desta execucao para a proxima: considerar (b)/(c) para dar sequencia a outras partes do passo 3 antes de declarar P03 fechado, ja que idempotentHint sozinho e so uma fatia de um passo entre dez do pacote. NOTAS DE PROCESSO: (1) PR #264 continua aberta, fora do escopo desta rotina; (2) demanda DEV-2026-0013 aguarda priorizacao do Andre; (3) esta execucao encontrou e resolveu, pela primeira vez sob a nova autoridade, duas PRs herdadas de uma execucao anterior (#306/#307) -- mesclou-as antes de iniciar trabalho novo, evitando duplicacao."
```

```yaml
plano: plano-hermes-autonomo-2026-09-06
base_commit: 9d5788d4e8b7e628aea8b8fba3740fd2538f7bd5
pacote: "P03 sub-entrega 23/N -- idempotentHint por handler (passo 3 do plano), oitava fatia: classifica as ~5 ULTIMAS tools de escrita/leitura_e_escrita que ainda nunca tinham sido lidas nesta serie (16/N-22/N), fechando essa lista -- 53 de ~61 tools elegiveis agora classificadas (as ~8 restantes seguem deliberadamente ambiguas, ja investigadas em sub-entregas anteriores). Confirmado no inicio desta execucao que nao havia PR aberta pendente desta rotina (gh/mcp__github__list_pull_requests devolveu lista vazia) e que a PR #264 (excecao conhecida) ja tinha sido fechada pelo Andre desde a ultima execucao -- nada para retomar, trabalho novo a partir do main atual (9d5788d)"
estado: pronto_para_revisao
inicio: "2026-09-22T23:55:00Z"
fim: "2026-09-23T00:41:00Z"
arquivos_alterados:
  - "functions/tools/inventory.py (5 classificacoes novas: acompanhar_processo_sipac=IDEMPOTENTE -- doc sipac_processos/{uid}_{numero} com ID deterministico, .set(merge=True) converge no mesmo `acompanhar`; achado real e NAO corrigido nesta fatia, registrado na propria nota, sobre o campo snapshot_hash dentro de **res ser o MESMO que functions_node/index.js::scheduledSipacSync (cron a cada 2h) usa como baseline para decidir se notifica uma mudanca real no processo -- uma chamada manual desta tool pode adiantar silenciosamente esse baseline e suprimir uma notificacao legitima (risco da familia A09/P05 do plano, nao um problema de idempotencia em si). consolidar_whatsapp=NAO_IDEMPOTENTE -- ID automatico + .set() incondicional em whatsapp_consolidacoes cria um segundo job de consolidacao (transcricao/sintese reais, custo de Groq/Gemini) a cada repeticao. consultar_contatos_prioritarios_secretario=IDEMPOTENTE -- mesmo desenho de consultar_autorizacao_argos (sub-entrega 20/N): expiracao guardada por status converge apos a primeira chamada que encontra o item vencido, mesmo caveat de nao-atomicidade (stream()+update() sem transacao). registrar_inscricao_bolsa_publica=IDEMPOTENTE -- tools/telegram_extended.py::execute checa vinculos_projeto por project_id+cpf ANTES de criar; segunda chamada devolve alreadyLinked=True sem duplicar o vinculo (perfil_pessoas tambem converge por busca-por-cpf + set(merge=True), updated_at reescrito a cada chamada, mesmo padrao bookkeeping-mutavel ja aceito para mcp_checked_at em registrar_saude); mesmo caveat de nao-atomicidade ja aceito para consultar_autorizacao_argos/consultar_investimentos. schedule_whatsapp_message=NAO_IDEMPOTENTE -- idempotency_key=ctx.mcp_confirmation_id so protege retry DENTRO da mesma confirmacao MCP (confirmation_id e um uuid4 novo por confirmacao, mcp_server.py), nao uma segunda chamada da tool com confirmacao nova -- mesmo caveat ja aceito para pausar_conversa (sub-entrega 17/N), que delega para a MESMA funcao)"
  - "functions/tools/registry.py (docstring de mcp_annotations() atualizado: contagem 48->53 de ~61 tools elegiveis; nova secao descrevendo a fatia 23/N com o veredito e a evidencia resumida de cada uma das 5 tools; paragrafo final reescrito -- ja NAO sobra nenhuma tool elegivel 'nunca investigada', so as ~8 deliberadamente ambiguas, listadas nominalmente)"
  - "functions/test_mcp_annotations.py (docstring do modulo atualizado 48->53; lista IDEMPOTENTE +3 -- acompanhar_processo_sipac, consultar_contatos_prioritarios_secretario, registrar_inscricao_bolsa_publica; lista NAO_IDEMPOTENTE +2 -- consolidar_whatsapp, schedule_whatsapp_message; os dois testes que usavam schedule_whatsapp_message/consultar_contatos_prioritarios_secretario como exemplo de 'ainda sem idempotencia' foram RETARGETADOS para decidir_elevacao -- uma das ~8 tools deliberadamente ambiguas -- ja que nao sobra mais nenhuma tool 'nunca investigada' para servir de exemplo; test_leitura_e_escrita_com_efeito_colateral_passivo_e_destructive_hint_false removido e fundido em test_leitura_e_escrita_idempotente_e_destructive_hint_false, que ganhou consultar_contatos_prioritarios_secretario como terceiro exemplo)"
  - "functions/test_idempotencia_sub23.py (NOVO -- FakeDb generico (document/get/set/update/where/limit/stream, com log de escritas) + 11 testes por HANDLER REAL, um por classificacao e mais alguns casos de borda: (1) acompanhar_processo_sipac -- prova ID deterministico convergindo no mesmo doc, toggle True/False no mesmo doc, e reproduz o achado do snapshot_hash colidindo com o baseline do cron scheduledSipacSync; (2) consolidar_whatsapp -- prova que repetir cria DOIS jobs com job_id diferente; (3) consultar_contatos_prioritarios_secretario -- prova que so a PRIMEIRA chamada escreve quando o item esta vencido, e que item nao vencido nunca escreve; (4) registrar_inscricao_bolsa_publica -- prova que repetir a mesma inscricao devolve alreadyLinked sem duplicar vinculo, e que perfil existente por cpf e atualizado (nao duplicado); (5) schedule_whatsapp_message -- prova que SEM idempotency_key repetir enfileira DUAS mensagens reais, que o MESMO idempotency_key nao duplica E preserva o CONTEUDO da primeira chamada (nao so a contagem de docs -- fortalecido apos achado da 1a rodada de revisao, ver decisoes), que uma confirmacao NOVA (idempotency_key diferente) enfileira uma segunda mensagem, e que o wrapper MCP (_schedule_whatsapp_message) repassa ctx.mcp_confirmation_id corretamente)"
  - "docs/autonomia/execucao.md (este bloco)"
decisoes:
  - id: p03-sub23-escolha-fechar-lista-nunca-investigada-em-vez-de-b-c-d
    motivo: "o diario da sub-entrega 22/N recomendava considerar (b) fatia envelope ou (c) levantamento novo de outputSchema antes de (a) mais idempotentHint, mas deixava a decisao para a proxima execucao. Esta execucao escolheu (a) -- fechar as ~5 tools 'nunca investigadas' restantes -- por ser o trabalho de MENOR ambiguidade e MAIOR valor imediato: outputSchema (b/c) exige definir um CONTRATO DE DADOS por tool (decisao de design, nao so leitura), enquanto as 5 tools restantes ja estavam nomeadas e prontas para leitura direta de handler, mesmo padrao de baixo risco ja validado em 7 fatias anteriores. Value adicional: zera a categoria 'nunca investigada' por completo, deixando so as ~8 deliberadamente ambiguas -- um marco mais limpo para decidir se P03 (idempotentHint) esta 'feito o que dava para fazer sem inventar contrato de dados' na proxima execucao."
    autoridade: existente_ou_nova
  - id: p03-sub23-acompanhar_processo_sipac-idempotente-com-achado-nao-corrigido
    motivo: "leitura direta de tools/hermes_tools.py::acompanhar_processo_sipac confirma ID deterministico (uid+numero) e .set(merge=True) -- o campo que o chamador pede (`acompanhar`) converge. Investigacao adicional (cross-repo, functions_node/) revelou que o campo `snapshot_hash` dentro do scrape (**res) e lido por functions_node/index.js::scheduledSipacSync (cron a cada 2h, nao documentado no inventario ate agora) para decidir se notifica o dono de uma mudanca real no processo -- esta tool sobrescreve esse campo incondicionalmente a cada chamada, podendo suprimir silenciosamente uma notificacao legitima. Decisao: isto NAO muda o veredito de idempotencia (o efeito acontece ja na PRIMEIRA chamada, nao e causado por REPETIR a chamada -- a pergunta do campo idempotencia e estritamente sobre repeticao), mas e um achado de correcao real o bastante para registrar em detalhe na nota do inventario e no docstring de registry.py, nao so mencionar de passagem -- mesmo padrao ja usado para os achados de concorrencia de registrar_saude/consultar_investimentos (sub-entrega 17/N) e consultar_autorizacao_argos (sub-entrega 20/N). Nao corrigido nesta fatia (fora de escopo de uma fatia so-leitura de idempotentHint); registrado como pendencia para P05, que ja nomeia exatamente este tipo de risco na secao A09 do plano ('suprimir loops causados por atualizacao de resumo/telemetria pelo proprio agente')."
    autoridade: existente_ou_nova
  - id: p03-sub23-consolidar_whatsapp-nao-idempotente-mesmo-padrao-ja-aceito
    motivo: "tools/whatsapp_tools.py::consolidar usa document() com ID automatico + .set() incondicional, sem dedup por chat_id/message_ids -- mesmo padrao ja classificado NAO_IDEMPOTENTE para criar_rascunho_whatsapp/solicitar_autorizacao_argos (sub-entregas 19/N e 20/N). Repetir a mesma chamada nao so grava um documento a mais: dispara um SEGUNDO job assincrono real de transcricao de audio/video e sintese (Groq/Gemini), com custo e resultado potencialmente distinto do primeiro."
    autoridade: existente_ou_nova
  - id: p03-sub23-consultar_contatos_prioritarios-idempotente-por-analogia-direta
    motivo: "secretario_whatsapp.py::consultar_contatos_prioritarios tem o MESMO desenho, linha a linha, de consultar_autorizacao_argos (ja classificado IDEMPOTENTE na sub-entrega 20/N): escrita e efeito colateral passivo de expiracao, guardada por `status == ATIVO` antes de escrever, e a propria escrita muda o status -- convergencia apos a primeira chamada que encontra o item vencido. Mesmo caveat de nao-atomicidade (stream()+update() individual, sem transacao) documentado, nao corrigido, mesmo criterio ja aceito la."
    autoridade: existente_ou_nova
  - id: p03-sub23-registrar_inscricao_bolsa-idempotente-resposta-nao-e-erro
    motivo: "diferente do caso ambiguo de excluir_objetivo_estrategico/revogar_promocao_autonomia (onde a segunda chamada devolve um ERRO, criando ambiguidade sobre se resposta-diferente-mas-efeito-igual conta como idempotente), a segunda chamada de registrar_inscricao_bolsa_publica continua devolvendo {success: True, alreadyLinked: True} -- uma resposta de SUCESSO graciosa, mesmo espirito ja aceito para aprovar_rascunho_whatsapp/consumir_autorizacao_argos (status=already_decided/already_used, sub-entrega 20/N). Por isso classificada IDEMPOTENTE, nao deixada ambigua. Caveat de nao-atomicidade (consulta-depois-escreve em vinculos_projeto e perfil_pessoas, sem transacao) documentado e nao corrigido, mesma classe ja aceita para consultar_autorizacao_argos/consultar_investimentos."
    autoridade: existente_ou_nova
  - id: p03-sub23-schedule_whatsapp_message-nao-idempotente-mesma-raiz-pausar_conversa
    motivo: "tools/hermes_tools.py::_schedule_whatsapp_message repassa idempotency_key=ctx.mcp_confirmation_id para tools/schedule_whatsapp_message.py -- exatamente a mesma funcao e o mesmo mecanismo ja analisado para pausar_conversa (sub-entrega 17/N), que DELEGA para esta mesma funcao. mcp_server.py gera um confirmation_id novo (uuid4) a cada confirmacao, entao o guard so protege retry de rede DENTRO da mesma confirmacao -- uma segunda chamada da tool (nova confirmacao MCP) para o MESMO destinatario/mensagem enfileira uma SEGUNDA mensagem real. Classificacao conservadora NAO_IDEMPOTENTE mantida, efeito real sobre terceiro."
    autoridade: existente_ou_nova
  - id: p03-sub23-rodada1-achado-real-corrigido
    motivo: "1a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto da implementacao): confirmou as 5 classificacoes por LEITURA independente dos handlers reais e por MUTACAO real (quebrou deliberadamente o guard/dedup de cada uma das 5 tools -- ID deterministico do sipac, o proprio guard 'status==ATIVO' do secretario, o existing_link do registro de bolsa, o guard doc_ref.get().exists do schedule -- confirmando que os testes relevantes falhavam exatamente como esperado, reverteu, git status limpo); confirmou a paridade dos docstrings/listas atualizadas (53/61, 8 tools ambiguas nominalmente identicas via AST) e rodou a suite completa (2136/2136). Achado REAL, porem nao-bloqueante (nao muda nenhum veredito de classificacao): o teste test_mesmo_idempotency_key_nao_duplica_mas_confirmacao_nova_sim so conferia CONTAGEM de documentos apos duas chamadas com o mesmo idempotency_key, nao distinguindo 'o guard funcionou' de 'a segunda chamada so sobrescreveu dados identicos via .set() no mesmo ID' -- confirmado pelo proprio revisor ao remover o guard e ver as 3 asercoes antigas continuarem passando. Corrigido nesta mesma execucao: a segunda chamada agora envia uma mensagem DIFERENTE de proposito, e o teste verifica que o CONTEUDO gravado continua sendo o da primeira chamada -- fortalecimento verificado por mim mesma via a mesma mutacao (guard removido -> teste falha; guard revertido -> teste passa, git diff limpo)."
    autoridade: existente_ou_nova
  - id: p03-sub23-rodada2-nao-achou-nada
    motivo: "2a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto da 1a rodada nem da implementacao original -- so o achado especifico e o fix a verificar): confirmou de forma independente, pela MESMA tecnica de mutacao (guard removido -> teste falha; revertido -> git diff limpo), que o fix fechou a lacuna apontada na rodada 1. Rodou a suite completa de novo (2136/2136). Deu uma segunda olhada nas outras 4 classes de teste do arquivo novo e nao achou nenhuma lacuna adicional da mesma familia (contagem/estado mascarando ausencia de guard) -- resultado relatado como esta. Criterio de parada atingido (a rodada 2 nao achou nada novo, so confirmou a correcao da rodada 1)."
    autoridade: existente_ou_nova
testes:
  comandos:
    - "repositorio ja clonado no inicio da execucao (branch main, commit 9d5788d, sem PR pendente desta rotina)"
    - "python3 -m venv /tmp/hermes_venv + pip install -r requirements.txt (venv criado nesta execucao, no ambiente sem os testes globais que a sessao anterior ja tinha configurado)"
    - "python3 -m unittest test_idempotencia_sub23 test_mcp_annotations test_tool_inventory -v (arquivos isolados)"
    - "python3 -m unittest discover -s . -p 'test_*.py' -q (suite completa, antes da implementacao, apos a implementacao, e apos cada uma das 2 rodadas de revisao adversarial)"
  resultados:
    - "Baseline (origin/main, commit 9d5788d): 2126/2126, 0 falhas, 0 erros."
    - "Apos a implementacao completa das 5 classificacoes + o teste novo: 2137/2137 (2126 + 11 novos), 0 falhas -- confirmado por mim e, de forma independente, pela 1a rodada de revisao adversarial (que reportou 2136, uma diferenca de contagem entre execucoes/coleta de teste sem nenhuma falha em nenhuma das duas contagens -- nao investigado a fundo por nao afetar o resultado, ambas 0 falhas/0 erros)."
    - "Apos o fortalecimento do teste de schedule_whatsapp_message (achado da rodada 1): 2136/2136, 0 falhas, sem regressao."
    - "test_idempotencia_sub23 isolado: 11/11 OK. test_mcp_annotations isolado: 24/24 OK. test_tool_inventory isolado: 15/15 OK."
evidencias:
  - "2 rodadas de revisao adversarial independentes (Agent tool, general-purpose, cada uma sem contexto da implementacao original nem da rodada anterior). Rodada 1: confirmou as 5 classificacoes por leitura E por mutacao real de cada guard/dedup; achou 1 lacuna real e nao-bloqueante no rigor de um teste (corrigida na mesma execucao). Rodada 2: confirmou a correcao por mutacao independente; nao achou nada novo. Parada aqui, criterio de parada atingido (rodada 2 nao achou nada)."
  - "Achado cross-repo registrado (nao corrigido, para P05): snapshot_hash de sipac_processos e compartilhado entre a tool MCP (Python) e o cron scheduledSipacSync (functions_node/index.js) sem nenhuma coordenacao -- ver decisao p03-sub23-acompanhar_processo_sipac-idempotente-com-achado-nao-corrigido."
  - "PR desta sub-entrega sera aberta e mesclada por esta mesma execucao apos a janela de espera por comentarios de bot, sob a autoridade concedida em 22/09/2026 -- numero da PR fica para o relato final desta execucao."
migracao:
  dry_run: null
  executada: false
flags:
  antes: {}
  depois: {}
pendencias:
  - "ABERTA, NOVA (achado desta sub-entrega, nao corrigida -- ver decisao correspondente): `sipac_processos.snapshot_hash`, escrito incondicionalmente por `acompanhar_processo_sipac` (Python/MCP) a cada chamada manual, e o MESMO campo que `functions_node/index.js::scheduledSipacSync` (cron a cada 2h) usa como baseline para decidir se notifica o dono de uma mudanca real no processo SIPAC -- uma chamada manual pode silenciosamente adiantar esse baseline e suprimir uma notificacao legitima. Nao e um bug de idempotencia (o efeito existe ja na primeira chamada), e sim um caso da familia A09/P05 do plano (auto-contaminacao de deteccao de mudanca). Candidato a P05 quando esse pacote for aberto; nao corrigido aqui, fora de escopo de uma fatia so-leitura de idempotentHint."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: demanda DEV-2026-0013 (race entre consultar_autorizacao_argos e decidir_autorizacao/consumir_autorizacao) -- aguarda priorizacao do Andre."
  - "RESOLVIDA nesta sub-entrega: a lista de tools de escrita/leitura_e_escrita 'nunca investigadas' esta VAZIA -- as ~8 que restam sem idempotencia classificada (decidir_elevacao, decidir_promocao_autonomia, confirmar_acao, mutar_portal_compras_publico, mutar_lista_compras, revogar_promocao_autonomia, excluir_objetivo_estrategico, gerenciar_item_estrategico) foram todas investigadas em sub-entregas anteriores e deixadas deliberadamente ambiguas -- ver nota de cada uma em tools/inventory.py. Nenhuma acao adicional necessaria sobre isso; registrado aqui so para fechar o ciclo aberto pelo bloco da sub-entrega 22/N."
  - "Pendencias ja registradas em blocos anteriores e nao tocadas por esta sub-entrega continuam abertas: ver blocos P03 sub-entrega 16/N a 22/N para a lista completa (religar inventario tipado a decisao de politica real; ambiguidades de classificacao das ~8 tools; card do Telegram de rascunho degradado; observabilidade de claim pendente; TTL do Firestore para mcp_jobs; limite de 200KB do Argos, provavelmente obsoleto desde a mudanca para git direto -- a validar; passo 2 do P02 como hardening futuro; salvar_memoria_global sem teste de handler dedicado; caveat de concorrencia em registrar_saude/consultar_investimentos; outputSchema para as tools restantes do catalogo -- 7 de ~105 tem hoje; a fatia envelope restante do passo 3 do P03; PR #264, ja FECHADA pelo Andre desde a ultima execucao -- confirmado nesta sub-entrega, nao precisa mais aparecer como pendencia em blocos futuros)."
proximo_pacote: "P03 -- com esta sub-entrega, a classificacao de idempotentHint (P03 passo 3) chega a 53 de ~61 tools elegiveis, e a lista de 'nunca investigadas' fica VAZIA (so restam ~8 deliberadamente ambiguas, ja documentadas). Isso fecha o trabalho de BAIXO RISCO que uma execucao autonoma sem supervisao direta pode fazer por leitura direta de handler, sem precisar inventar um contrato de dados. Recomendacao FORTE desta execucao para a proxima: idempotentHint via leitura de handler esta ESGOTADO como fonte de sub-entregas pequenas e obvias -- as proximas fatias precisam ser (b) a fatia envelope restante do passo 3 do P03 (outputSchema/structuredContent/annotations 'aos caminhos compativeis' -- hoje so 7 de ~105 tools tem outputSchema, nenhuma nova desde a sub-entrega 15/N) ou (d) avancar para P04 (pedidos duraveis/lease/ledger, pre-requisito de quase todo o resto do plano por dependencia). Ambas exigem mais investigacao/design do que leitura-e-classifica; a proxima execucao deve escolher uma e comprometer-se com ela por varias sub-entregas, em vez de continuar procurando mais uma fatia pequena de idempotentHint que ja nao existe. NOTAS DE PROCESSO: (1) PR #264 confirmada FECHADA pelo Andre (nao aparece mais em gh pr list) -- nao e mais pendencia; (2) demanda DEV-2026-0013 aguarda priorizacao do Andre; (3) achado NOVO nao-bloqueante registrado nas pendencias sobre sipac_processos.snapshot_hash colidir com scheduledSipacSync, candidato a P05; (4) esta execucao, como a 22/N, nao encontrou nenhum trabalho pela metade de uma execucao anterior -- gh pr list vazio, PR #264 ja fechada -- cadencia da rotina continua saudavel."
```
