# Registro de execução — plano-hermes-autonomo-2026-09-06

Um bloco por pacote (P00–P18), no formato da seção 14.2 do plano. Cada pacote novo é acrescentado ao final; nenhum bloco anterior é reescrito depois de `validado`. Sem segredos nem conteúdo privado — apenas referências a artefatos e IDs.

**Blocos P00 a P02 sub-entrega 11/N foram movidos para
docs/autonomia/execucao-archive-p00-a-p02sub11.md em 2026-09-09 (P02
sub-entrega 15/N). Blocos P02 sub-entrega 12/N a 16/N foram movidos, em
2026-09-09 também, para um SEGUNDO arquivo de arquivo,
docs/autonomia/execucao-archive-p02-sub12-a-sub16.md — durante a correção
da sub-entrega P01 5/N, depois que uma escrita anterior derrubou
silenciosamente o bloco P02 sub-entrega 17/N na retranscrição manual de
um arquivo de 85701 bytes, e uma tentativa de reenviar tudo somado num
arquivo de arquivo só (159725 bytes) falhou por limite de saída (64000
tokens). O bloco P02 sub-entrega 17/N foi movido, também em 2026-09-09,
para um TERCEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p02-sub17.md — durante a sub-entrega P01
6/N, preventivamente. Os blocos P01 sub-entrega 3/N e 4/N foram movidos,
ainda em 2026-09-09, para um QUARTO arquivo de arquivo,
docs/autonomia/execucao-archive-p01-sub3-a-sub4.md — durante a sub-entrega
P01 7/N, também preventivamente. O bloco P01 sub-entrega 5/N foi movido,
ainda em 2026-09-09, para um QUINTO arquivo de arquivo,
docs/autonomia/execucao-archive-p01-sub5.md — durante a sub-entrega P01
8/N, preventivamente pelo mesmo motivo. Os blocos P01 sub-entrega 6/N a
9/N (fechamento do pacote) foram movidos, ainda em 2026-09-09, para um
SEXTO arquivo de arquivo, docs/autonomia/execucao-archive-p01-sub6-a-sub9.md
— durante a sub-entrega P02 19/N, preventivamente, ao registrar
retroativamente as sub-entregas P02 18/N e 19/N. Os blocos P02
sub-entrega 18/N a P03 sub-entrega 3/N foram movidos, em 2026-09-12, para
um SÉTIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p02sub18-a-p03sub3.md — pela tarefa
agendada de avanço autônomo, ao registrar retroativamente a sub-entrega
P03 7/N, preventivamente (o arquivo ativo tinha passado de ~50KB sem
re-arquivamento desde o sexto arquivo). Ver o cabeçalho de cada arquivo
de arquivo para o relato completo. Os blocos P03 sub-entrega 4/N e 5/N foram
movidos, em 12/09/2026, para um OITAVO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub4-a-sub5.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 8/N,
preventivamente (o arquivo ativo passaria de ~50KB ao acrescentar essa
entrada sem re-arquivamento). Os blocos P03 sub-entrega 6/N e 7/N foram
movidos, em 13/09/2026, para um NONO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub6-a-sub7.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 10/N,
preventivamente (mesmo motivo). Os blocos P03 sub-entrega 8/N e 9/N foram
movidos, também em 13/09/2026, para um DÉCIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub8-a-sub9.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 11/N
(o arquivo ativo passaria de ~50KB ao acrescentar essa entrada sem
re-arquivamento). Os blocos P03 sub-entrega 10/N e 11/N foram movidos, em
14/09/2026, para um DÉCIMO PRIMEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub10-a-sub11.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 13/N
(o arquivo ativo passaria de ~50KB ao acrescentar essa entrada sem
re-arquivamento). O bloco P03 sub-entrega 12/N foi movido, também em
14/09/2026, para um DÉCIMO SEGUNDO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub12.md — durante a correção do
registro do diário da sub-entrega 14/N (incidente de processo: um
subagente sem autorização abriu duas PRs indevidas para este mesmo
registro, uma delas mesclada pelo André antes da correção chegar — ver a
entrada da sub-entrega 14/N para o relato completo), preventivamente
(o arquivo ativo chegaria perto de ~50KB com as correções necessárias
sem re-arquivamento). O bloco P03 sub-entrega 13/N foi movido, em
14/09/2026, para um DÉCIMO TERCEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub13.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 15/N
(o arquivo ativo passaria de ~50KB ao acrescentar essa entrada sem
re-arquivamento). Os blocos P03 sub-entrega 14/N e 15/N foram movidos,
também em 14/09/2026, para um DÉCIMO QUARTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub14-a-sub15.md — pela tarefa
agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega
P03 16/N (o arquivo ativo chegaria a 48157 bytes, perto do limite de
~50KB, sem re-arquivamento). Os blocos P03 sub-entrega 16/N e 17/N
foram movidos, em 15/09/2026, para um DÉCIMO QUINTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub16-a-sub17.md — pela tarefa
agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega
P03 19/N (o arquivo ativo passaria de 60KB, bem acima do limite de
~50KB, ao acrescentar essa entrada sem re-arquivamento). Os blocos P03
sub-entrega 18/N e 19/N foram movidos, em 22/09/2026, para um DÉCIMO
SEXTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub18-a-sub19.md — pela tarefa
agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega
P03 20/N (o arquivo ativo passaria de ~50KB ao acrescentar essa
entrada sem re-arquivamento). O bloco P03 sub-entrega 20/N foi movido,
também em 22/09/2026, para um DÉCIMO SÉTIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub20.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 22/N (o
arquivo ativo tinha chegado a 52198 bytes, acima do limite de ~50KB,
sem re-arquivamento desde o décimo sexto arquivo). Nenhum
conteúdo foi perdido; é uma relocação, não uma edição (o campo `estado`
desse bloco foi atualizado de `pronto_para_revisao` para `validado` no
arquivamento, já que o próprio bloco P03 sub-entrega 21/N confirma o
merge das PRs #304/#305 — ver o cabeçalho do arquivo de arquivo). O
bloco P03 sub-entrega 21/N foi movido, em 23/09/2026, para um DÉCIMO
OITAVO arquivo de arquivo, docs/autonomia/execucao-archive-p03sub21.md
— pela tarefa agendada de avanço autônomo, ao acrescentar a entrada da
sub-entrega P03 23/N (o arquivo ativo tinha chegado a 54816 bytes,
acima do limite de ~50KB, sem re-arquivamento desde o décimo sétimo
arquivo). Mesmo critério de relocação + confirmação de validação (o
campo `estado` desse bloco foi atualizado de `pronto_para_revisao` para
`validado`, já que o próprio bloco P03 sub-entrega 22/N confirma o
merge das PRs #306/#307 — ver o cabeçalho do arquivo de arquivo). O
bloco P03 sub-entrega 22/N foi movido, também em 23/09/2026, para um
DÉCIMO NONO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub22.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 24/N (o
arquivo ativo tinha chegado a 56906 bytes, acima do limite de ~50KB,
sem re-arquivamento desde o décimo oitavo arquivo). Mesmo critério de
relocação + confirmação de validação (o campo `estado` desse bloco foi
atualizado de `pronto_para_revisao` para `validado`, já que o histórico
de commits do `main` — commit `9d5788d`, "P03 sub-entrega 22/N... (#308)"
— confirma o merge da PR desta sub-entrega 22/N, exatamente como suas
próprias evidências previam). O bloco P03 sub-entrega 23/N foi movido, em
23/09/2026, para um VIGÉSIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub23.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 25/N (o
arquivo ativo teria passado de ~50KB sem re-arquivamento desde o décimo
nono arquivo). Mesmo critério de relocação + confirmação de validação (o
campo `estado` desse bloco foi atualizado de `pronto_para_revisao` para
`validado`, já que o próprio bloco P03 sub-entrega 24/N tem
`base_commit: ce56aab` — o commit exato do merge da PR #309 desta
sub-entrega 23/N, confirmado via `git log origin/main` — ver o cabeçalho
do arquivo de arquivo). Este arquivo continua sendo a fonte de verdade
para tudo a partir da sub-entrega P03 24/N em diante.

---

```yaml
plano: plano-hermes-autonomo-2026-09-06
base_commit: ce56aabaf93e64c13cfe908660cac17ba279541d
pacote: "P03 sub-entrega 24/N -- outputSchema/structuredContent por handler (passo 3 do plano), oitava tool: listar_rascunhos_pendentes, backed por outbox_aprovacao.listar_rascunhos. Confirmado no inicio desta execucao (mcp__github__list_pull_requests, estado aberto) que nao havia PR pendente desta rotina -- trabalho novo a partir do main atual. Seguindo a recomendacao FORTE do bloco da sub-entrega 23/N (idempotentHint esgotado como fonte de sub-entregas pequenas), esta execucao escolheu a opcao (b) -- outputSchema/structuredContent -- em vez de (d) P04: outputSchema permite uma fatia pequena e verificavel (uma tool por vez, mesmo padrao das 7 anteriores), enquanto P04 (pedidos duraveis/lease/ledger) e trabalho arquitetural de varios passos que nao cabe com seguranca numa unica execucao sem supervisao direta -- decisao registrada abaixo. GRANDE PARTE DO TRABALHO desta sub-entrega foi processo de revisao, nao codigo: o codigo/schema ficou estavel desde a 1a implementacao (nenhuma mudanca funcional em nenhuma das 4 rodadas de revisao adversarial), mas o COMENTARIO de justificativa precisou de 3 correcoes sucessivas -- ver decisoes e a licao de processo registrada no proprio comentario de tools/registry.py."
estado: pronto_para_revisao
inicio: "2026-09-23T08:01:00Z"
fim: "2026-09-23T08:57:00Z"
arquivos_alterados:
  - "functions/tools/registry.py (1 outputSchema novo: listar_rascunhos_pendentes, oitava tool com contrato publicado -- 98 de 106 tools hoje sem contrato, deliberadamente. status e enum de 2 valores [aguardando_aprovacao, aguardando_janela] pela MESMA garantia ja usada em consultar_pedidos_agente -- filtro da propria query .where('status','in',[...]), nao escritor unico (a colecao whatsapp_outbox tem MUITOS escritores, em Python e em Node -- ver decisao sobre a licao de processo). ACHADO: telegram_message_id e inteiro (message_id da API do Telegram), nao string como o padrao _to_iso dos dois campos de data faria supor. foi_editado/trecho sao coagidos na PROPRIA LEITURA (bool()/str()[:120]); acao_id/item_atencao_id nullable por desenho. docstring de output_schema() atualizado 99->98 tools sem contrato, apos a oitava entrada.)"
  - "functions/test_output_schema.py (docstring do modulo atualizado para citar a oitava tool; TestOutputSchema ganhou test_listar_rascunhos_pendentes_tem_schema_com_campos_obrigatorios + parity test renomeado para 8 tools; TestHandleToolsListOutputSchema ganhou test_listar_rascunhos_pendentes_publica_output_schema + as duas listas de paridade atualizadas; TestIntegracaoHandleToolsCallStructuredContent ganhou 3 testes novos com mcp_server.execute_tool mockado -- sucesso, lista vazia, e bate-com-o-schema-publicado, mesmo padrao das 7 tools anteriores)"
  - "functions/test_outbox_aprovacao.py (4 testes novos por HANDLER REAL contra o _MockDb ja existente no arquivo: as 14 chaves exatas do item incluindo campos ausentes no Firestore -> None; telegram_message_id inteiro quando presente; trecho truncado em 120 caracteres; e -- fruto da 1a rodada de revisao adversarial -- coercao de foi_editado/trecho PROVADA por mutacao com tipo BRUTO gravado no Firestore, string/int em vez de bool/string, porque o teste anterior so cobria o default False que ja e bool sem a coercao)"
  - "docs/autonomia/execucao.md (este bloco)"
decisoes:
  - id: p03-sub24-escolha-outputschema-em-vez-de-p04
    motivo: "seguindo a recomendacao explicita do bloco da sub-entrega 23/N ('a proxima execucao deve escolher uma [opcao b ou d] e comprometer-se com ela'), esta execucao escolheu (b) outputSchema/structuredContent em vez de (d) P04. Razao: P04 (pedidos duraveis/lease/ledger, 10 passos, modulos novos autonomy/requests.py+execution.py+verifiers.py) e trabalho arquitetural de alto risco -- exige decisoes de design que afetam quase todo o resto do plano por dependencia, mais adequado para ser iniciado com supervisao direta do Andre ou pelo menos numa sub-entrega dedicada so a desenhar o contrato antes de implementar. outputSchema permite continuar a mesma disciplina de fatia pequena e verificavel (uma tool por vez, leitura de handler real, sem inventar contrato) que ja rendeu 8 sub-entregas de baixo risco em P03."
    autoridade: existente_ou_nova
  - id: p03-sub24-listar_rascunhos_pendentes-escolhida-por-forma-unica-sem-oneof
    motivo: "outbox_aprovacao.listar_rascunhos tem uma forma de retorno so (sem try/except proprio, sem ramo de erro alternativo), diferente de consultar_historico_acoes/obter_acao (sub-entregas 14/N e 15/N) que precisaram de oneOf -- candidata de menor risco entre as tools de leitura ainda sem contrato. consultar_dados_cadastrais foi descartada nesta sub-entrega pelo mesmo motivo ja usado para obter_portal_compras_publico/obter_projeto_bolsas_publico/consultar_pedidos_agente (sub-entrega 12/N): devolve STRING via json.dumps, nao dict."
    autoridade: existente_ou_nova
  - id: p03-sub24-telegram_message_id-e-inteiro-achado-real
    motivo: "leitura de telegram_utils.py::_send_telegram_message_with_keyboard confirma que o valor devolvido (e gravado como telegram_message_id) vem de resp.json().get('result',{}).get('message_id') -- message_id e sempre um inteiro na API do Telegram, nunca string, ao contrario do padrao _to_iso (sempre string-ou-null) que os outros dois campos de data desta tool seguem. Confirmado tambem por fixture ja existente em test_outbox_aprovacao.py (telegram_message_id: 999, literal inteiro) escrita por uma sub-entrega anterior sem relacao com esta. outputSchema publicado como ['integer','null'], nao ['string','null']."
    autoridade: existente_ou_nova
  - id: p03-sub24-status-enum-por-filtro-de-query-nao-por-escritor-unico
    motivo: "mesma garantia ja estabelecida para consultar_pedidos_agente (sub-entrega 12/N): o enum de 2 valores de status nao depende de a colecao ter poucos escritores (whatsapp_outbox tem MUITOS, ao contrario de agent_requests), depende so do filtro .where('status','in',[STATUS_AGUARDANDO, STATUS_AGUARDANDO_JANELA]) da propria query em outbox_aprovacao.listar_rascunhos -- nenhum documento com outro valor pode aparecer no resultado, seja qual for o numero de escritores ou o que cada um escreva."
    autoridade: existente_ou_nova
  - id: p03-sub24-licao-de-processo-enumeracao-exaustiva-abandonada
    motivo: "a implementacao original tentava provar a garantia do enum por ENUMERACAO COMPLETA de todo escritor de whatsapp_outbox (mesmo padrao usado com sucesso para consultar_pedidos_agente/agent_requests, sub-entrega 12/N, onde a colecao tinha um UNICO escritor). Para whatsapp_outbox isso nao funcionou: TRES rodadas seguidas de revisao adversarial (ver evidencias) acharam, cada uma, pelo menos um escritor real que a versao anterior do comentario tinha omitido -- rodada 1: aplicar_edicao_rascunho/_degradar_rascunho_promovido_sem_mandato; rodada 2: ai_notification_planner.py (que a nota entao dizia, erradamente, ser so-leitura) e hermes-voice-bridge/tools.py (subsistema fora de functions/); rodada 3: aprovar_rascunho/descartar_rascunho/expirar_rascunhos_pendentes (dentro do PROPRIO outbox_aprovacao.py, o arquivo que a nota estava auditando) e services/whatsapp-capture/index.js (o worker Node, um runtime diferente de Python). Em NENHUMA das tres rodadas o escritor omitido mudava o veredito do enum -- nenhum deles escreve aguardando_aprovacao/aguardando_janela fora de um gate que ja exige o documento estar num desses dois estados. Decisao desta execucao, apos a 3a rodada: abandonar a enumeracao completa (um alvo que nao para de crescer numa colecao com escritores em multiplos runtimes) e reescrever o comentario para depender SO da garantia estrutural real -- o filtro da query, que nunca precisou de enumeracao nenhuma para ser correto. A 4a rodada de revisao adversarial validou essa garantia estrutural diretamente (nao a enumeracao) e nao achou mais nada. LICAO para sub-entregas futuras de outputSchema: a tecnica de 'enumerar todo escritor e provar cada um inofensivo' so e viavel quando a colecao tem POUCOS escritores confirmados (agent_requests, agent_runs); para colecoes com muitos escritores em multiplos modulos/runtimes (como whatsapp_outbox), preferir desde o inicio a garantia estrutural do filtro da propria query, sem tentar enumerar."
    autoridade: existente_ou_nova
  - id: p03-sub24-destinatario_resolvido-caveat-corrigido-nao-se-aplica-a-esta-tool
    motivo: "a 1a rodada de revisao adversarial apontou que o caveat original (risco residual de tipo cru vazando via o parametro destinatario_resolvido de criar_rascunho) estava mal atribuido a esta tool: o unico call site (secretario_whatsapp.py::enviar_resposta_via_outbox) sempre passa envio_imediato=True, que forca status_inicial=STATUS_PENDING incondicionalmente -- um documento criado por esse caminho NUNCA pode aparecer no resultado de listar_rascunhos (o filtro da query exclui PENDING). Corrigido para deixar claro que o caveat continua real para criar_rascunho (a funcao) e para uma eventual outputSchema futura de criar_rascunho_whatsapp, mas nao se aplica ao contrato desta tool especifica."
    autoridade: existente_ou_nova
  - id: p03-sub24-rodada1-2-achados-reais-corrigidos-rodadas-3-4-nao-acharam-nada
    motivo: "4 rodadas de revisao adversarial (Agent tool, general-purpose, cada uma sem contexto de nenhuma rodada anterior, so o diff e o objetivo). Rodada 1: achou a lista de escritores incompleta/errada (ver licao de processo) e um teste que so provava a coercao de foi_editado/trecho no caso default (ja bool sem a coercao) -- ambos corrigidos nesta mesma execucao, incluindo um teste novo verificado por mutacao real (remover bool()/str() e ver o teste falhar; reverter e ver passar; git status limpo). Rodada 2: achou que a correcao da rodada 1 AINDA estava incompleta (2 escritores novos omitidos) -- corrigido de novo. Rodada 3: achou MAIS 4 escritores omitidos (3 dentro do proprio outbox_aprovacao.py) -- decisao de abandonar a enumeracao nesta mesma execucao, apos a 3a vez que a tentativa de lista completa falhava. Rodada 4 (sobre a garantia estrutural, nao a enumeracao): nao achou nada -- confirmou o filtro da query, a cadeia completa MCP tool -> handler -> query como passthrough puro, a honestidade do comentario reescrito, e a suite completa. Criterio de parada atingido na rodada 4 (a 1a rodada, desde a mudanca de estrategia, que nao achou nada)."
    autoridade: existente_ou_nova
testes:
  comandos:
    - "repositorio ja clonado no inicio da execucao (branch main, commit ce56aab, sem PR pendente desta rotina -- mcp__github__list_pull_requests devolveu lista vazia)"
    - "python3 -m venv /tmp/hermes_venv + pip install -r requirements.txt"
    - "python3 -m unittest test_output_schema test_outbox_aprovacao test_mcp_annotations test_tool_inventory -v (arquivos isolados, 168 testes)"
    - "python3 -m unittest discover -s . -p 'test_*.py' -q (suite completa -- antes da implementacao/baseline via git stash, apos a implementacao, e apos cada uma das 4 rodadas de revisao adversarial)"
    - "mutacao manual (nao a suite): remover bool(...)/str(...) de outbox_aprovacao.listar_rascunhos, confirmar teste novo falhando, reverter, confirmar git diff limpo -- feito por mim e replicado de forma independente por 2 das 4 rodadas de revisao adversarial"
  resultados:
    - "Baseline (origin/main, commit ce56aab, via git stash): 2136/2136, 0 falhas, 0 erros."
    - "Apos a 1a implementacao (schema + testes iniciais): 2144/2144 (2136 + 8 novos), 0 falhas."
    - "Apos a correcao da rodada 1 (comentario + 1 teste novo de mutacao): 2145/2145 (2144 + 1 novo), 0 falhas."
    - "Apos a correcao da rodada 2 (so comentario, sem novo teste): 2145/2145, sem regressao."
    - "Apos a reescrita do comentario para abandonar a enumeracao (decisao pos-rodada-3): 2145/2145, sem regressao (mudanca so de comentario)."
    - "Confirmado pela rodada 4, de forma independente: 2145/2145."
    - "168 testes isolados (test_output_schema + test_outbox_aprovacao + test_mcp_annotations + test_tool_inventory): todos OK, em cada rodada."
evidencias:
  - "4 rodadas de revisao adversarial independentes (Agent tool, general-purpose, cada uma sem contexto de nenhuma rodada anterior). Rodadas 1-3 acharam achados reais e sucessivos sobre a EXAUSTIVIDADE da lista de escritores de whatsapp_outbox (nunca sobre a correcao do schema/enum em si, que se manteve intacta as 4 rodadas); rodada 4 nao achou nada, sobre a garantia estrutural que substituiu a enumeracao. Ver a decisao p03-sub24-licao-de-processo-enumeracao-exaustiva-abandonada para o relato completo e a licao para sub-entregas futuras."
  - "Nenhuma mudanca de codigo funcional (so comentarios e testes) entre a 1a implementacao e a versao final -- confirmado explicitamente pela rodada 4 via git show em cada commit do branch."
  - "PR desta sub-entrega sera aberta e mesclada por esta mesma execucao apos a janela de espera por comentarios de bot, sob a autoridade concedida em 22/09/2026."
migracao:
  dry_run: null
  executada: false
flags:
  antes: {}
  depois: {}
pendencias:
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: demanda DEV-2026-0013 (race entre consultar_autorizacao_argos e decidir_autorizacao/consumir_autorizacao) -- aguarda priorizacao do Andre."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: sipac_processos.snapshot_hash colidindo com scheduledSipacSync (achado da sub-entrega 23/N), candidato a P05."
  - "Pendencias ja registradas em blocos anteriores e nao tocadas por esta sub-entrega continuam abertas: ver blocos P03 sub-entrega 16/N a 23/N para a lista completa (religar inventario tipado a decisao de politica real; ambiguidades de classificacao das ~8 tools de idempotentHint; card do Telegram de rascunho degradado; observabilidade de claim pendente; TTL do Firestore para mcp_jobs; limite de 200KB do Argos, provavelmente obsoleto desde a mudanca para git direto -- a validar; passo 2 do P02 como hardening futuro; salvar_memoria_global sem teste de handler dedicado; caveat de concorrencia em registrar_saude/consultar_investimentos; a fatia envelope restante do passo 3 do P03 -- expected_version/idempotency_key, paginacao, paralelismo, normalizador -- outputSchema e so uma fatia entre dez passos do P03)."
proximo_pacote: "P03 -- outputSchema chega a 8 de 106 tools do catalogo (calculadora, buscar_contato, consultar_lista_compras, consultar_execucoes_agente, consultar_pedidos_agente, consultar_historico_acoes, obter_acao, listar_rascunhos_pendentes). Candidatas de FORMA UNICA (sem oneOf) ainda plausíveis para a proxima fatia, nao investigadas nesta sub-entrega: tools de leitura simples do catalogo (ex. consultar_saude, consultar_investimentos, consultar_dados_cadastrais [JA DESCARTADA -- devolve string via json.dumps], consultar_job) -- a proxima execucao deve LER o handler real antes de escolher, nao assumir pela descricao. RECOMENDACAO desta execucao para a proxima: (1) continuar outputSchema por mais 1-2 fatias enquanto houver candidatas de forma unica obvias, OU (2) se a proxima leitura mostrar que as tools restantes exigem oneOf ou contrato ambiguo (mesmo padrao que fez consultar_historico_acoes/obter_acao serem mais trabalhosas), considerar migrar para P04 (pedidos duraveis/lease/ledger) como o proximo compromisso de varias sub-entregas, conforme a recomendacao ja acumulada desde a sub-entrega 23/N. LICAO DE PROCESSO A PROPAGAR (ver decisao completa acima): para colecoes Firestore com MUITOS escritores em multiplos runtimes, nao tentar enumerar todo escritor para provar um enum -- usar a garantia estrutural do filtro da propria query, mais robusta e mais rapida de verificar. NOTAS: (1) PR #264 continua fechada (nao e mais pendencia, ja confirmado na sub-entrega 23/N); (2) demanda DEV-2026-0013 aguarda priorizacao do Andre; (3) esta execucao gastou a maior parte do tempo em revisao adversarial (4 rodadas) por causa da tentativa de enumeracao exaustiva -- a licao registrada aqui deve evitar o mesmo padrao de retrabalho numa sub-entrega futura de outputSchema sobre outra colecao com muitos escritores."
```

```yaml
plano: plano-hermes-autonomo-2026-09-06
base_commit: 4fbe896194e9a13e92ed57774e79bb0e42c9c93b
pacote: "P03 sub-entrega 25/N -- outputSchema/structuredContent por handler (passo 3 do plano), nona tool: consultar_job, backed por mcp_jobs.ler_job. Confirmado no inicio desta execucao (mcp__github__list_pull_requests, estado aberto) que nao havia PR pendente desta rotina -- trabalho novo a partir do main atual (4fbe896). Seguindo a recomendacao da sub-entrega 24/N (continuar outputSchema enquanto houver candidatas de forma unica obvias, mas LER o handler real antes de escolher, nao assumir pela descricao): consultar_saude foi descartada pelo mesmo motivo ja usado para consultar_dados_cadastrais (devolve STRING via json.dumps, nao dict); consultar_investimentos tambem foi descartada por ter multiplas formas de retorno genuinamente distintas (erro/sem-carteira/com-carteira, com campos extras condicionais) que exigiriam investigacao mais profunda; consultar_job foi escolhida apesar de ter 5 `return` no handler (a maior contagem de branches ate agora) porque mcp_jobs.py e o UNICO escritor da colecao mcp_jobs em todo o repositorio -- ao contrario de whatsapp_outbox (sub-entrega 24/N), isso tornou viavel enumerar os 4 pontos de escrita de status por completo, sem repetir o padrao de retrabalho da enumeracao exaustiva ja abandonado naquela sub-entrega."
estado: validado
inicio: "2026-09-23T11:05:00Z"
fim: "2026-09-23T11:53:00Z"
arquivos_alterados:
  - "functions/tools/registry.py (1 outputSchema novo: consultar_job, nona tool com contrato publicado -- 99 de 108 tools hoje sem contrato, deliberadamente. 4 branches de oneOf, a maior contagem deste catalogo ate agora: (1) branch de erro combinado -- sem job_id (so `erro`) OU job inexistente/uid divergente (`erro`+`status:not_found`), mesmo padrao de status opcional ja usado em obter_acao; (2) status=done, com `resultado` documentado como STRING HOJE como contrato de SNAPSHOT -- as 3 unicas tools de _TOOLS_LONGAS (gerar_relatorio, ler_documento_na_integra, buscar_e_analisar_email) sempre devolvem string (confirmado lendo os 3 handlers por completo), mas o mecanismo (_preparar_resultado) e generico, entao uma quarta tool futura em _TOOLS_LONGAS que devolva dict/lista quebraria essa garantia; (3) status=error, com erro_tipo (enum fechado de 4 valores) e bloqueio_politica opcionais; (4) status=processing, com mensagem literal fixa. ACHADO registrado, nao corrigido: o campo truncado (gravado pelo trigger on_mcp_job_created junto com resultado) nunca e devolvido por ler_job -- fora do escopo de uma fatia so-schema, candidato a P05/mcp_jobs. docstring de output_schema() corrigido de 98/106 (numero ja desatualizado antes desta sub-entrega) para 99/108, apos achado da revisao adversarial rodada 1.)"
  - "functions/test_output_schema.py (docstring do modulo atualizado para citar a nona tool; TestOutputSchema ganhou test_consultar_job_tem_schema_com_quatro_branches + paridade renomeada para 9 tools (test_paridade_nove_tools_tem_output_schema_hoje, tambem corrigindo 106->108 no comentario); TestHandleToolsListOutputSchema ganhou test_consultar_job_publica_output_schema + as listas de paridade atualizadas; TestIntegracaoHandleToolsCallStructuredContent ganhou 6 testes novos com mcp_server.execute_tool mockado -- um por branch (sem_job_id, nao_encontrado, done, error, processing) + um de paridade campo a campo contra os 4 branches do oneOf, mesmo padrao de obter_acao)"
  - "docs/autonomia/execucao.md (este bloco; arquivamento do bloco 23/N para docs/autonomia/execucao-archive-p03sub23.md, ver cabecalho do arquivo)"
decisoes:
  - id: p03-sub25-escolha-consultar_job-sobre-consultar_saude-e-consultar_investimentos
    motivo: "consultar_saude (tools/hermes_tools.py::_consultar_saude) devolve json.dumps(...) -- uma STRING, nao dict -- mesmo motivo de descarte ja usado para consultar_dados_cadastrais (sub-entrega 12/N). consultar_investimentos (_consultar_investimentos) tem 3+ formas de retorno genuinamente distintas: erro puro de investimentos.carteira(), sem-carteira-ainda (status+carteira_registrada:False+observacao) e com-carteira (dict completo + ate 2 campos extras condicionais de sincronizacao de rebalanceamento) -- exigiria investigacao mais profunda de investimentos.py/investimentos_sync.py do que cabia nesta fatia. consultar_job foi escolhida apesar do maior numero de branches porque a colecao mcp_jobs tem UM UNICO escritor (mcp_jobs.py), tornando a enumeracao de status VIAVEL e definitiva -- ao contrario de whatsapp_outbox (sub-entrega 24/N, multiplos escritores, enumeracao abandonada)."
    autoridade: existente_ou_nova
  - id: p03-sub25-quatro-branches-erro-combinado
    motivo: "ler_job (mcp_jobs.py) tem 5 `return`, mas os dois primeiros (sem job_id / job inexistente ou uid divergente) colapsam num UNICO branch de oneOf com `status` fora de required -- mesmo padrao ja usado para obter_acao (sub-entrega 15/N): a diferenca entre eles e so a PRESENCA opcional do campo `status` (sempre `not_found` quando aparece), nao um formato alternativo genuino. Os outros 3 branches (done/error/processing) compartilham job_id/tool/status mas tem o resto dos campos obrigatorios DISJUNTO -- portanto 4 branches de oneOf no total, nao 5 nem 3."
    autoridade: existente_ou_nova
  - id: p03-sub25-resultado-contrato-de-snapshot-nao-garantia-estrutural
    motivo: "o campo resultado (branch done) e documentado como string HOJE por observacao direta das 3 unicas tools de _TOOLS_LONGAS (mcp_server.py) -- gerar_relatorio e ler_documento_na_integra (tools/telegram_extended.py) e buscar_e_analisar_email (tools/buscar_e_analisar_email.py), todas lidas por completo, TODO `return` de string. Isto NAO e uma garantia estrutural como o enum por filtro de query (listar_rascunhos_pendentes, sub-entrega 24/N): o mecanismo que grava o campo (mcp_jobs._preparar_resultado) e generico (aceita qualquer tipo, serializa para JSON quando nao-string), entao uma QUARTA tool futura adicionada a _TOOLS_LONGAS que devolva dict/lista quebraria esta garantia sem tocar o comentario -- explicitamente registrado como caveat de snapshot, nao de estrutura, para nao repetir a licao de honestidade de comentario ja aprendida na sub-entrega 24/N (reason_code)."
    autoridade: existente_ou_nova
  - id: p03-sub25-truncado-achado-nao-corrigido
    motivo: "o trigger on_mcp_job_created grava `truncado` (bool) no MESMO ref.update() que grava `resultado` quando status=done, mas ler_job so copia job_id/tool/status/resultado para `saida` -- o campo truncado, apesar de existir no Firestore, nunca chega ao consumidor de consultar_job. Um resultado longo cortado por _MAX_RESULTADO_CHARS (120000 chars) fica indistinguivel de um resultado completo. Nao corrigido nesta fatia -- mudar o que ler_job devolve e mudanca de COMPORTAMENTO do handler, nao so documentacao de contrato existente, fora do escopo de uma fatia so-outputSchema; registrado como pendencia para uma fatia futura de mcp_jobs.py ou P05."
    autoridade: existente_ou_nova
  - id: p03-sub25-rodada1-dois-achados-reais-corrigidos
    motivo: "1a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto da implementacao): leu mcp_jobs.py (ler_job e on_mcp_job_created) por completo, confirmou independentemente que mcp_jobs.py e o unico escritor da colecao (grep no repo inteiro), releu os 3 handlers de _TOOLS_LONGAS por completo confirmando que sempre devolvem string, e validou os 4 branches campo a campo. Achou 2 itens reais, corrigidos na mesma execucao: (1) o comentario original afirmava que bloqueio_politica.reason_code 'pode ser None' sem verificacao -- na real, autonomy/contracts.py::PolicyDecision.reason_code e tipado `str` (nao `str | None`) e os 4 pontos de construcao de PolicyDecision em autonomy/policy.py (incluindo _decisao_padrao_por_classe, tipada -> tuple[Decisao, str, bool], 8 `return` todos string) sempre passam literal de string -- schema e comentario corrigidos para nao-nullable, teste estrutural novo trava a garantia; (2) o docstring de output_schema() citava '106' tools no catalogo, desatualizado desde ANTES desta sub-entrega (len(registry.list_tool_names()) e 108 hoje) -- corrigido nas duas linhas que esta sub-entrega ja tocava (registry.py e o comentario do teste de paridade)."
    autoridade: existente_ou_nova
  - id: p03-sub25-rodada2-nao-achou-nada
    motivo: "2a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto de nenhuma rodada anterior -- so a alegacao da correcao a verificar): leu autonomy/contracts.py e autonomy/policy.py por conta propria, confirmou reason_code tipado str sem excecao em nenhum dos 4 sites de construcao nem em _decisao_padrao_por_classe; rodou len(registry.list_tool_names()) == 108 e listou as 9 tools com schema, confirmando 99/108; grepou '106' nos dois arquivos e confirmou que as 2 ocorrencias restantes sao comentarios que DOCUMENTAM a propria correcao, nao referencias esquecidas; confirmou que o teste de reason_code opera sobre o schema real (registry.output_schema), nao um literal desconectado; rodou a suite completa. Nao achou nada. Criterio de parada atingido."
    autoridade: existente_ou_nova
testes:
  comandos:
    - "repositorio ja clonado no inicio da execucao (branch main, commit 4fbe896, sem PR pendente desta rotina -- mcp__github__list_pull_requests devolveu lista vazia)"
    - "python3 -m venv /tmp/hermes_venv + pip install -r requirements.txt (venv criado nesta execucao; primeira tentativa de pip install -r requirements.txt nao instalou firebase-functions por algum motivo transitorio do ambiente -- corrigido com pip install firebase-functions explicito seguido de novo pip install -r requirements.txt, que entao completou corretamente)"
    - "python3 -m unittest test_output_schema -v (arquivo isolado)"
    - "python3 -m unittest discover -s . -p 'test_*.py' -q (suite completa, baseline antes da implementacao, apos a implementacao, e apos cada uma das 2 rodadas de revisao adversarial)"
  resultados:
    - "Baseline (origin/main, commit 4fbe896): 2146/2146, 0 falhas, 0 erros."
    - "Apos a implementacao completa do outputSchema + 8 testes novos: 2154/2154, 0 falhas -- confirmado por mim e, de forma independente, pela 1a rodada de revisao adversarial."
    - "Apos as 2 correcoes da rodada 1 (reason_code + contagem 106->108): 2154/2154, sem regressao (mudanca so de schema/comentario/asserts, nenhum teste novo nem removido)."
    - "test_output_schema.py isolado: 74/74 OK, em cada rodada."
evidencias:
  - "2 rodadas de revisao adversarial independentes (Agent tool, general-purpose, cada uma sem contexto da implementacao original nem da rodada anterior). Rodada 1: achou 2 itens reais e nao-bloqueantes (reason_code nullable sem verificacao; contagem 106 desatualizada), ambos corrigidos nesta mesma execucao. Rodada 2: confirmou as 2 correcoes por leitura propria independente e nao achou nada novo. Parada aqui, criterio de parada atingido (rodada 2 nao achou nada)."
  - "PR #311 aberta, revisada pelo Codex (chatgpt-codex-connector[bot] -- reagiu com resumo de review completo, sem comentarios de achado, sem review threads inline) e pelos 3 checks de CI (GitGuardian Security Checks, Testes das Functions Python, Testes e build -- todos success), mesclada por esta mesma execucao (squash, commit af547d5) apos a janela de espera de ~5 minutos, sob a autoridade concedida em 22/09/2026. Nenhum comentario de bot exigiu correcao -- 0 rodadas de espera-e-correcao usadas das 2 permitidas."
migracao:
  dry_run: null
  executada: false
flags:
  antes: {}
  depois: {}
pendencias:
  - "ABERTA, NOVA (achado desta sub-entrega, nao corrigida -- ver decisao p03-sub25-truncado-achado-nao-corrigido): o campo `truncado` de um job concluido (mcp_jobs.py) nunca e devolvido por `ler_job`/`consultar_job` -- um resultado longo cortado por tamanho fica indistinguivel de um resultado completo para quem consulta o job. Candidato a uma fatia futura de mcp_jobs.py ou P05."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: demanda DEV-2026-0013 (race entre consultar_autorizacao_argos e decidir_autorizacao/consumir_autorizacao) -- aguarda priorizacao do Andre."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: sipac_processos.snapshot_hash colidindo com scheduledSipacSync (achado da sub-entrega 23/N), candidato a P05."
  - "Pendencias ja registradas em blocos anteriores e nao tocadas por esta sub-entrega continuam abertas: ver blocos P03 sub-entrega 16/N a 24/N para a lista completa (religar inventario tipado a decisao de politica real; ambiguidades de classificacao das ~8 tools de idempotentHint; card do Telegram de rascunho degradado; observabilidade de claim pendente; TTL do Firestore para mcp_jobs; limite de 200KB do Argos, provavelmente obsoleto desde a mudanca para git direto -- a validar; passo 2 do P02 como hardening futuro; salvar_memoria_global sem teste de handler dedicado; caveat de concorrencia em registrar_saude/consultar_investimentos; a fatia envelope restante do passo 3 do P03 -- expected_version/idempotency_key, paginacao, paralelismo, normalizador -- outputSchema e so uma fatia entre dez passos do P03; consultar_investimentos permanece candidata descartada nesta sub-entrega por precisar de investigacao mais profunda antes de outputSchema, nao permanentemente fora de escopo)."
proximo_pacote: "P03 -- outputSchema chega a 9 de 108 tools do catalogo (calculadora, buscar_contato, consultar_lista_compras, consultar_execucoes_agente, consultar_pedidos_agente, consultar_historico_acoes, obter_acao, listar_rascunhos_pendentes, consultar_job). Candidatas de forma unica plausiveis para a proxima fatia, nao investigadas nesta sub-entrega: consultar_job esgotou as candidatas 'obvias pela descricao' que a sub-entrega 24/N tinha apontado -- consultar_saude e consultar_investimentos foram INVESTIGADAS e descartadas nesta sub-entrega (ver decisoes), nao ficam mais como candidatas pendentes de leitura. A proxima execucao precisa LER o handler de outras tools de leitura do catalogo (ex. consultar_processo_sipac, acompanhar_processo_sipac, buscar_arquivos_acervo) antes de escolher, sem assumir forma pela descricao -- mesma disciplina ja estabelecida. RECOMENDACAO desta execucao para a proxima: (1) continuar outputSchema por mais 1 fatia se uma leitura de handler revelar uma candidata de forma unica ou oneOf tratavel (poucos escritores, como mcp_jobs/agent_requests/agent_runs foram), OU (2) se as tools restantes exigirem investigacao desproporcional (como consultar_investimentos, ou colecoes com muitos escritores), considerar migrar para P04 (pedidos duraveis/lease/ledger) como o proximo compromisso de varias sub-entregas -- recomendacao que ja vem se acumulando desde a sub-entrega 23/N e segue valida. NOTAS DE PROCESSO: (1) PR #264 continua fechada, nao e mais pendencia; (2) demanda DEV-2026-0013 aguarda priorizacao do Andre; (3) esta execucao nao encontrou nenhum trabalho pela metade de uma execucao anterior -- gh/mcp__github__list_pull_requests vazio no inicio -- cadencia da rotina continua saudavel; (4) achado de ambiente: pip install -r requirements.txt nao instalou firebase-functions na primeira tentativa nesta execucao (venv fresco) -- contornado com instalacao explicita do pacote seguida de nova tentativa do requirements.txt completo, sem impacto no resultado final, mas vale registrar caso se repita; (5) Blueprint da Autonomia (artifact) atualizado nesta execucao com o pacote/PR desta sub-entrega -- ver evidencias/relato final desta execucao para o link."
```
