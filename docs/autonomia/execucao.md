# Registro de execução — plano-hermes-autonomo-2026-09-06

Um bloco por pacote (P00–P18), no formato da seção 14.2 do plano. Cada pacote novo é acrescentado ao final; nenhum bloco anterior é reescrito depois de `validado`. Sem segredos nem conteúdo privado — apenas referências a artefatos e IDs.

**Blocos P00 a P02 sub-entrega 11/N foram movidos para
docs/autonomia/execucao-archive-p00-a-p02sub11.md em 2026-09-09 (P02
sub-entrega 15/N). Blocos P02 sub-entrega 12/N a 16/N foram movidos, em
2026-09-09 também, para um SEGUNDO arquivo de arquivo,
docs/autonomia/execucao-archive-p02-sub12-a-sub16.md — durante a correção
da sub-entrega P01 5/N, depois que uma escrita anterior derrubou
silenciosamente o bloco P02 sub-entrega 17/N na retranscrição manual de
um arquivo de 85701 bytes, e uma tentativa de reenviar tudo somado num
arquivo de arquivo só (159725 bytes) falhou por limite de saída (64000
tokens). O bloco P02 sub-entrega 17/N foi movido, também em 2026-09-09,
para um TERCEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p02-sub17.md — durante a sub-entrega P01
6/N, preventivamente. Os blocos P01 sub-entrega 3/N e 4/N foram movidos,
ainda em 2026-09-09, para um QUARTO arquivo de arquivo,
docs/autonomia/execucao-archive-p01-sub3-a-sub4.md — durante a sub-entrega
P01 7/N, também preventivamente. O bloco P01 sub-entrega 5/N foi movido,
ainda em 2026-09-09, para um QUINTO arquivo de arquivo,
docs/autonomia/execucao-archive-p01-sub5.md — durante a sub-entrega P01
8/N, preventivamente pelo mesmo motivo. Os blocos P01 sub-entrega 6/N a
9/N (fechamento do pacote) foram movidos, ainda em 2026-09-09, para um
SEXTO arquivo de arquivo, docs/autonomia/execucao-archive-p01-sub6-a-sub9.md
— durante a sub-entrega P02 19/N, preventivamente, ao registrar
retroativamente as sub-entregas P02 18/N e 19/N. Os blocos P02
sub-entrega 18/N a P03 sub-entrega 3/N foram movidos, em 2026-09-12, para
um SÉTIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p02sub18-a-p03sub3.md — pela tarefa
agendada de avanço autônomo, ao registrar retroativamente a sub-entrega
P03 7/N, preventivamente (o arquivo ativo tinha passado de ~50KB sem
re-arquivamento desde o sexto arquivo). Ver o cabeçalho de cada arquivo
de arquivo para o relato completo. Os blocos P03 sub-entrega 4/N e 5/N foram
movidos, em 12/09/2026, para um OITAVO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub4-a-sub5.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 8/N,
preventivamente (o arquivo ativo passaria de ~50KB ao acrescentar essa
entrada sem re-arquivamento). Os blocos P03 sub-entrega 6/N e 7/N foram
movidos, em 13/09/2026, para um NONO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub6-a-sub7.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 10/N,
preventivamente (mesmo motivo). Os blocos P03 sub-entrega 8/N e 9/N foram
movidos, também em 13/09/2026, para um DÉCIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub8-a-sub9.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 11/N
(o arquivo ativo passaria de ~50KB ao acrescentar essa entrada sem
re-arquivamento). Os blocos P03 sub-entrega 10/N e 11/N foram movidos, em
14/09/2026, para um DÉCIMO PRIMEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub10-a-sub11.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 13/N
(o arquivo ativo passaria de ~50KB ao acrescentar essa entrada sem
re-arquivamento). O bloco P03 sub-entrega 12/N foi movido, também em
14/09/2026, para um DÉCIMO SEGUNDO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub12.md — durante a correção do
registro do diário da sub-entrega 14/N (incidente de processo: um
subagente sem autorização abriu duas PRs indevidas para este mesmo
registro, uma delas mesclada pelo André antes da correção chegar — ver a
entrada da sub-entrega 14/N para o relato completo), preventivamente
(o arquivo ativo chegaria perto de ~50KB com as correções necessárias
sem re-arquivamento). O bloco P03 sub-entrega 13/N foi movido, em
14/09/2026, para um DÉCIMO TERCEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub13.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 15/N
(o arquivo ativo passaria de ~50KB ao acrescentar essa entrada sem
re-arquivamento). Os blocos P03 sub-entrega 14/N e 15/N foram movidos,
também em 14/09/2026, para um DÉCIMO QUARTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub14-a-sub15.md — pela tarefa
agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega
P03 16/N (o arquivo ativo chegaria a 48157 bytes, perto do limite de
~50KB, sem re-arquivamento). Os blocos P03 sub-entrega 16/N e 17/N
foram movidos, em 15/09/2026, para um DÉCIMO QUINTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub16-a-sub17.md — pela tarefa
agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega
P03 19/N (o arquivo ativo passaria de 60KB, bem acima do limite de
~50KB, ao acrescentar essa entrada sem re-arquivamento). Os blocos P03
sub-entrega 18/N e 19/N foram movidos, em 22/09/2026, para um DÉCIMO
SEXTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub18-a-sub19.md — pela tarefa
agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega
P03 20/N (o arquivo ativo passaria de ~50KB ao acrescentar essa
entrada sem re-arquivamento). O bloco P03 sub-entrega 20/N foi movido,
também em 22/09/2026, para um DÉCIMO SÉTIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub20.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 22/N (o
arquivo ativo tinha chegado a 52198 bytes, acima do limite de ~50KB,
sem re-arquivamento desde o décimo sexto arquivo). Nenhum
conteúdo foi perdido; é uma relocação, não uma edição (o campo `estado`
desse bloco foi atualizado de `pronto_para_revisao` para `validado` no
arquivamento, já que o próprio bloco P03 sub-entrega 21/N confirma o
merge das PRs #304/#305 — ver o cabeçalho do arquivo de arquivo). O
bloco P03 sub-entrega 21/N foi movido, em 23/09/2026, para um DÉCIMO
OITAVO arquivo de arquivo, docs/autonomia/execucao-archive-p03sub21.md
— pela tarefa agendada de avanço autônomo, ao acrescentar a entrada da
sub-entrega P03 23/N (o arquivo ativo tinha chegado a 54816 bytes,
acima do limite de ~50KB, sem re-arquivamento desde o décimo sétimo
arquivo). Mesmo critério de relocação + confirmação de validação (o
campo `estado` desse bloco foi atualizado de `pronto_para_revisao` para
`validado`, já que o próprio bloco P03 sub-entrega 22/N confirma o
merge das PRs #306/#307 — ver o cabeçalho do arquivo de arquivo). O
bloco P03 sub-entrega 22/N foi movido, também em 23/09/2026, para um
DÉCIMO NONO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub22.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 24/N (o
arquivo ativo tinha chegado a 56906 bytes, acima do limite de ~50KB,
sem re-arquivamento desde o décimo oitavo arquivo). Mesmo critério de
relocação + confirmação de validação (o campo `estado` desse bloco foi
atualizado de `pronto_para_revisao` para `validado`, já que o histórico
de commits do `main` — commit `9d5788d`, "P03 sub-entrega 22/N... (#308)"
— confirma o merge da PR desta sub-entrega 22/N, exatamente como suas
próprias evidências previam). O bloco P03 sub-entrega 23/N foi movido, em
23/09/2026, para um VIGÉSIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub23.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 25/N (o
arquivo ativo teria passado de ~50KB sem re-arquivamento desde o décimo
nono arquivo). Mesmo critério de relocação + confirmação de validação (o
campo `estado` desse bloco foi atualizado de `pronto_para_revisao` para
`validado`, já que o próprio bloco P03 sub-entrega 24/N tem
`base_commit: ce56aab` — o commit exato do merge da PR #309 desta
sub-entrega 23/N, confirmado via `git log origin/main` — ver o cabeçalho
do arquivo de arquivo). O bloco P03 sub-entrega 24/N foi movido, em 23/09/2026, para um VIGÉSIMO
PRIMEIRO arquivo de arquivo, docs/autonomia/execucao-archive-p03sub24.md
— pela tarefa agendada de avanço autônomo, ao acrescentar a entrada da
sub-entrega P03 26/N (o arquivo ativo passaria de ~50KB ao acrescentar
essa entrada sem re-arquivamento). Mesmo critério de relocação +
confirmação de validação já usado nos arquivos anteriores (o campo
`estado` desse bloco foi atualizado de `pronto_para_revisao` para
`validado`, e a última entrada de `evidencias` reescrita para confirmar
o número da PR e o commit do merge, já que o próprio bloco P03
sub-entrega 25/N tem `base_commit: 4fbe896194e9a13e92ed57774e79bb0e42c9c93b`
— o commit exato do merge da PR #310 desta sub-entrega 24/N, confirmado
via `git log origin/main` — ver o cabeçalho do arquivo de arquivo). O
bloco P03 sub-entrega 25/N foi movido, em 23/09/2026, para um VIGÉSIMO
SEGUNDO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub25.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 27/N (o
arquivo ativo tinha chegado a 52261 bytes, acima do limite de ~50KB, sem
re-arquivamento desde o vigésimo primeiro arquivo). Nenhum conteúdo foi
alterado — o bloco já estava com `estado: validado` (confirmado pelo
próprio bloco P03 sub-entrega 26/N, cujo `base_commit` é o commit exato
do merge desta sub-entrega 25/N). O bloco P03 sub-entrega 26/N foi
movido, em 24/09/2026, para um VIGÉSIMO TERCEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub26.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 28/N (o
arquivo ativo tinha chegado a 54462 bytes, acima do limite de ~50KB, sem
re-arquivamento desde o vigésimo segundo arquivo). Mesmo critério de
relocação + confirmação de validação já usado nos arquivos anteriores (o
bloco já estava com `estado: validado`, confirmado pelo próprio bloco
P03 sub-entrega 27/N, cujo `base_commit` é o commit exato do merge desta
sub-entrega 26/N). O bloco P03 sub-entrega 27/N foi movido, em
24/09/2026, para um VIGÉSIMO QUARTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub27.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 29/N (o
arquivo ativo estava em 42331 bytes, se aproximando de ~50KB sem
re-arquivamento desde o vigésimo terceiro arquivo). Mesmo critério de
relocação + confirmação de validação já usado nos arquivos anteriores (o
bloco já estava com `estado: validado`, confirmado pelo próprio bloco
P03 sub-entrega 28/N, cujo `base_commit` é o commit exato do merge desta
sub-entrega 27/N). O bloco P03 sub-entrega 28/N foi movido, também em
24/09/2026, para um VIGÉSIMO QUINTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub28.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 30/N (o
arquivo ativo estava em 42952 bytes, se aproximando de ~50KB sem
re-arquivamento desde o vigésimo quarto arquivo). Mesmo critério de
relocação + confirmação de validação já usado nos arquivos anteriores (o
bloco já estava com `estado: validado`, confirmado pelo próprio bloco
P03 sub-entrega 29/N, cujo `base_commit` é o commit exato do merge desta
sub-entrega 28/N). O bloco P03 sub-entrega 29/N foi movido, em
24/09/2026, para um VIGÉSIMO SEXTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub29.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 31/N (o
arquivo ativo estava em 45164 bytes, passaria de ~50KB ao acrescentar
essa entrada sem re-arquivamento desde o vigésimo quinto arquivo). Mesmo
critério de relocação + confirmação de validação já usado nos arquivos
anteriores (o bloco já estava com `estado: validado`, confirmado pelo
próprio bloco P03 sub-entrega 30/N, cujo `base_commit` é o commit exato
do merge desta sub-entrega 29/N). O bloco P03 sub-entrega 30/N foi
movido, em 24/09/2026, para um VIGÉSIMO SÉTIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub30.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 31/N (o
arquivo ativo tinha chegado a 52516 bytes, acima do limite de ~50KB, sem
re-arquivamento desde o vigésimo sexto arquivo). Nenhum conteúdo foi
alterado — o bloco já estava com `estado: validado` (confirmado pelo
histórico de commits do `main`, commit `0ff339c`, que confirma o merge
da PR #322 desta sub-entrega 30/N). O bloco P03 sub-entrega 31/N foi
movido, em 25/09/2026, para um VIGÉSIMO OITAVO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub31.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P04 1/N (o
arquivo ativo estava em 51999 bytes, acima do limite de ~50KB, sem
re-arquivamento desde o vigésimo sétimo arquivo). Nenhum conteúdo foi
alterado — o bloco já estava com `estado: validado` (corrigido pela
própria sub-entrega 32/N, que confirmou o merge da PR #323 desta
sub-entrega 31/N). O bloco P03 sub-entrega 32/N foi movido, também em
25/09/2026, para um VIGÉSIMO NONO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub32.md — pela mesma execução, ao
corrigir o fix de DST desta entrada (o arquivo ativo tinha passado de
~50KB de novo, sem re-arquivamento desde o vigésimo oitavo arquivo).
Nenhum conteúdo foi alterado — o bloco já estava com `estado: validado`
(a mesclagem da PR #325 desta sub-entrega 32/N foi confirmada
diretamente pela sub-entrega P04 1/N seguinte, que a mesclou). Este
arquivo continua sendo a fonte de verdade para tudo a partir da
sub-entrega P04 1/N em diante. O bloco P04 sub-entrega 1/N foi movido,
em 25/09/2026, para um TRIGÉSIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p04sub1.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P04 2/N (o
arquivo ativo passaria de ~52KB, acima do limite de ~50KB, sem
re-arquivamento desde o vigésimo nono arquivo). Nenhum conteúdo foi
alterado — o bloco já estava com `estado: validado` (confirmado pela
própria sub-entrega, que verificou o merge da PR #326 via git fetch
antes de encerrar).

---

```yaml
plano: plano-hermes-autonomo-2026-09-06
base_commit: 78414295a44bdd10b4ae3d0d3191c83ec38ab62d
pacote: "P04 sub-entrega 2/N -- passo 3 do pacote (\"Criar ledger de operacoes e checkpoints com limites de tamanho\", secao 4.5 \"Reserva, retomada e idempotencia\", itens 6/9/10), seguindo a RECOMENDACAO explicita do bloco da sub-entrega 1/N (opcao 1: ledger puro antes do wiring de agent_requests.py). Novo modulo functions/autonomy/ledger.py, mesmo padrao incremental das sub-entregas anteriores de P04: so tipos e logica pura, sem I/O, sem wiring em producao. Ao inicio desta execucao, mcp__github__list_pull_requests (estado aberto) nao encontrou nenhuma PR pendente -- nenhuma retomada de trabalho pela metade foi necessaria; branch nova criada a partir do main atual (commit acima, HEAD confirmado apos merge da PR #327)."
estado: validado
inicio: "2026-09-25T14:17:00Z"
fim: "2026-09-25T15:03:30Z"
arquivos_alterados:
  - "functions/autonomy/ledger.py (NOVO -- LedgerEntry/Checkpoint (dataclasses frozen), ConflitoIdempotencia/CheckpointMuitoGrande (excecoes), hash_canonico/criar_entrada/criar_ou_reusar_entrada (item 6: criar/reusar operation_ledger com chave unica e hash canonico), consultar_reentrega/ReentregaStatus (item 9: reentrega retorna trabalho em curso ou resultado ja observado), adicionar_checkpoint (limite de tamanho MAX_CHECKPOINT_BYTES=64KiB, retencao FIFO por contagem MAX_CHECKPOINTS_POR_OPERACAO=20 E por orcamento cumulativo MAX_CHECKPOINTS_BYTES_TOTAL=512KiB, dedup de reentrega do mesmo checkpoint), registrar_resultado (item 9/10: registra resultado observado uma unica vez, idempotente para o MESMO resultado reenviado, ValueError para resultado diferente -- nunca sobrescreve). _snapshot_json/_congelar_profundamente: toda entrada guardada (Checkpoint.dados/LedgerEntry.resultado) e uma copia independente E recursivamente imutavel (dict->MappingProxyType, list->tuple) do valor do chamador -- nao o objeto original, nem uma copia ainda mutavel. _chave_no_espirito_json/_canonicalizar_chaves: canonicalizacao de chave de dict replicando a grafia exata que o encoder padrao do json usa para chave nao-string (bool->true/false, None->null, int->repr, float->json.dumps), com deteccao de colisao (ValueError) quando duas chaves distintas do MESMO dict colidem depois de normalizadas."
  - "functions/test_autonomy_ledger.py (NOVO -- 82 testes: hash canonico (ordem de chaves, aninhamento, bool vs int vs float, chaves mistas/colidentes, grafia JSON de bool/None), criar/reusar entrada (reuso por mesmo hash, conflito por hash diferente, validacao de tz-awareness em TODOS os branches), reentrega (trabalho em curso vs resultado observado, resultado None e valido), checkpoint (sequencia monotonica mesmo apos descarte FIFO, limite de tamanho individual E cumulativo, dedup de reentrega comparando so com o ultimo, mutacao do dict original E da referencia ja armazenada nao afeta o checkpoint guardado, copy.copy seguro), registrar_resultado (NaN/bool-int tratados via serializacao canonica nao ==, resultado nao-serializavel rejeitado ja na 1a chamada, mutacao pos-registro nao afeta o resultado guardado, list/tuple equivalente documentado como risco aceito)."
decisoes:
  - id: p04-sub2-ledger-independente-de-requeststatus
    motivo: "O ledger de uma OPERACAO (efeito externo dentro de um pedido, secao 4.2) e um conceito mais estreito que o estado do PEDIDO inteiro (RequestStatus de autonomy/requests.py) -- um pedido pode ter varias operacoes associadas. Este modulo garante so a idempotencia da operacao em si; quem fizer o wiring decide como as duas maquinas se relacionam."
    autoridade: existente_ou_nova
  - id: p04-sub2-rodada1-achados-reais-corrigidos
    motivo: "1a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto da implementacao) achou 5 problemas reais, todos corrigidos: (1) registrar_resultado usava == do Python para checar reenvio identico de resultado -- NaN != NaN fazia um reenvio IDENTICO de NaN ser tratado como conflito; (2) == tambem tratava True/1 como iguais, inconsistente com hash_canonico (que ja distingue bool de int); (3) hash_canonico levantava TypeError em dict aninhado com chaves de tipos mistos ({1: \"a\", \"b\": \"c\"}), mesmo sendo payload serializavel (sort_keys=True nao compara int com str); (4) adicionar_checkpoint nao tinha protecao contra reenvio do mesmo checkpoint -- cada reenvio consumia um slot do limite FIFO e podia derrubar checkpoints antigos distintos para abrir espaco para duplicatas do mais recente; (5) validacao de tz-awareness de agora em registrar_resultado rodava DEPOIS do early-return de \"ja registrado\", mascarando um agora naive no caminho de no-op. Corrigido com _mesmo_valor_canonico (comparacao por serializacao, nao ==), _canonicalizar_chaves (stringifica chave antes de ordenar), dedup de checkpoint comparando com o ultimo, e validacao de tz movida para o topo da funcao."
    autoridade: existente_ou_nova
  - id: p04-sub2-rodada2-achados-reais-corrigidos
    motivo: "2a rodada de revisao adversarial (foco exclusivo nos fixes da rodada 1, sem contexto dela alem do que foi passado no prompt) confirmou os 4 fixes principais e achou 2 problemas NOVOS reais: (1) _canonicalizar_chaves (fix da rodada 1) descartava silenciosamente uma entrada quando duas chaves DISTINTAS do MESMO dict colidiam depois de stringificadas ({1: \"x\", \"1\": \"y\"}) -- pior que o TypeError original, porque dois payloads objetivamente diferentes passavam a produzir o MESMO hash; (2) criar_ou_reusar_entrada so validava tz-awareness de agora no branch de criacao -- os branches de reuso e conflito aceitavam agora naive silenciosamente, mesma classe de bug da rodada 1 na funcao irma. Corrigido: loop explicito com deteccao de colisao (ValueError) em _canonicalizar_chaves, e validacao de tz movida para o topo de criar_ou_reusar_entrada, antes de qualquer branch."
    autoridade: existente_ou_nova
  - id: p04-sub2-rodada3-nao-achou-nada
    motivo: "3a rodada de revisao adversarial (foco exclusivo nos 2 fixes da rodada 2, verificacao por execucao direta, nao so leitura) confirmou os 2 fixes como corretos e completos e NAO achou nada real -- criterio de parada atingido. Observacao nao-bloqueante registrada: _exigir_tz_aware roda duas vezes no branch de criacao de criar_ou_reusar_entrada (uma vez no topo da funcao, outra dentro de criar_entrada) -- redundante mas inofensivo (funcao pura, sem efeito colateral), mesmo padrao ja existente em autonomy/requests.py (lease_valida_para_acao chama lease_expirada, que valida de novo)."
    autoridade: existente_ou_nova
  - id: p04-sub2-codex-pr328-4-achados-corrigidos
    motivo: "Revisao automatica do Codex (chatgpt-codex-connector, bot configurado no repositorio) na PR #328 achou 4 problemas reais (P2), todos verificados por execucao direta e corrigidos: (1) MAX_CHECKPOINTS_POR_OPERACAO (20) * MAX_CHECKPOINT_BYTES (64KiB) sozinho permite ate 1.310.720 bytes, ja excedendo o limite de 1MiB de um documento Firestore antes de contar o resto do documento -- corrigido com MAX_CHECKPOINTS_BYTES_TOTAL (512KiB) e _aplicar_orcamento_cumulativo (descarte FIFO adicional por bytes, mantendo pelo menos 1 checkpoint); (2) um resultado nao-serializavel (ex.: datetime) era aceito silenciosamente na PRIMEIRA chamada de registrar_resultado e so quebrava com TypeError numa REENTREGA -- corrigido validando serializabilidade (via _snapshot_json) em TODA construcao de LedgerEntry, inclusive a primeira; (3) _canonicalizar_chaves usava str(chave), que nao bate com a grafia real que o json usa para chave nao-string (str(True)=\"True\" vs json \"true\"; str(None)=\"None\" vs json \"null\") -- uma reentrega legitima apos round-trip JSON real virava ConflitoIdempotencia; corrigido com _chave_no_espirito_json, que replica a regra exata do encoder padrao; (4) Checkpoint.dados/LedgerEntry.resultado guardavam o objeto mutavel do chamador por referencia -- mutar o dict original depois de guardado mudava o conteudo \"imutavel\" sem validacao -- corrigido com _snapshot_json (copia independente via round-trip JSON)."
    autoridade: existente_ou_nova
  - id: p04-sub2-revisao-pos-codex-achado-real-mutacao-da-referencia-guardada
    motivo: "Uma rodada de revisao adversarial em cima dos 4 fixes do Codex (achado 4 especificamente) achou que o fix so fechava METADE do buraco: a copia guardada por _snapshot_json continuava um dict/list MUTAVEL comum, entao mutar a propria referencia JA ARMAZENADA (entrada.resultado[\"x\"] = ..., devolvida pelo ledger -- nao o dict original do chamador) continuava funcionando sem nenhuma validacao, quebrando a garantia de nunca sobrescrever (item 10) na pratica. frozen=True no dataclass so impede reatribuir o ATRIBUTO, nunca protegeu o CONTEUDO mutavel apontado por ele. Corrigido: nova _congelar_profundamente (dict->MappingProxyType, list->tuple, recursivo), aplicada por _snapshot_json antes de guardar. Efeito colateral desejado, tambem verificado pela mesma rodada: neutraliza copy.copy() num Checkpoint/LedgerEntry (que contorna __post_init__ e compartilharia o mesmo objeto com o original -- inofensivo agora que esse objeto e imutavel)."
    autoridade: existente_ou_nova
  - id: p04-sub2-revisao-final-nao-achou-nada
    motivo: "Rodada de revisao adversarial final (foco exclusivo no fix de congelamento profundo, verificacao por execucao direta incluindo mutacao em 4+ niveis de aninhamento, Mapping subtypes como OrderedDict/Counter/defaultdict, grep no repo inteiro confirmando que nada fora dos testes usa LedgerEntry/Checkpoint ainda) NAO achou nada real -- criterio de parada atingido, PR pronta para merge."
    autoridade: existente_ou_nova
  - id: p04-sub2-risco-aceito-int-float
    motivo: "1 (int) e 1.0 (float) produzem hashes/serializacoes DIFERENTES -- comportamento inerente a JSON (json.dumps distingue \"1\" de \"1.0\"), nao um bug deste modulo. Normalizar tipo numerico exigiria decidir uma politica de coercao que o plano nao especifica. Documentado na docstring de hash_canonico e coberto por teste explicito."
    autoridade: existente_ou_nova
  - id: p04-sub2-risco-aceito-list-tuple
    motivo: "list e tuple convergem para tuple no valor GUARDADO (consequencia de _congelar_profundamente) -- mesma familia do risco aceito de int/float. Nao e uma regressao: hash_canonico/_mesmo_valor_canonico ja tratavam list e tuple como equivalentes para fins de COMPARACAO desde a 1a rodada; agora o valor guardado tambem reflete essa canonicalizacao. Um array JSON de verdade (Firestore incluido) nao distingue os dois de qualquer forma. Documentado na docstring de _congelar_profundamente e coberto por teste explicito."
    autoridade: existente_ou_nova
testes:
  comandos:
    - "/tmp/hermes_venv_p04sub2 (venv fresco nesta execucao) + pip install -r requirements.txt"
    - "python3 -m unittest test_autonomy_ledger -v (isolado, apos cada rodada de fix)"
    - "python3 -m unittest discover -s . -p 'test_*.py' -q (suite completa -- apos cada rodada de fix)"
  resultados:
    - "Baseline (main pos-merge da PR #327, commit 7841429): 2399/2399, 0 falhas."
    - "Apos a implementacao inicial (49 testes novos): 49/49 isolado; 2448/2448 na suite completa."
    - "Apos os fixes da 1a rodada (+11 testes, 60 no total): 60/60 isolado; 2459/2459 na suite completa."
    - "Apos os fixes da 2a rodada (+5 testes, 65 no total): 65/65 isolado; 2464/2464 na suite completa."
    - "Apos os 4 fixes do Codex (+9 testes, 74 no total): 74/74 isolado; 2473/2473 na suite completa."
    - "Apos o fix de congelamento profundo (+8 testes, 82 no total): 82/82 isolado; 2481/2481 na suite completa -- delta final de +82 sobre o baseline de 2399."
evidencias:
  - "6 rodadas de revisao adversarial independentes (Agent tool, general-purpose, cada uma sem contexto de implementacao nem de rodada anterior alem do que foi passado no prompt) -- ver decisoes p04-sub2-rodada1/2/3 e p04-sub2-revisao-pos-codex/final acima para o detalhe completo de cada achado e correcao. Mais a revisao automatica do Codex (bot do repositorio) na propria PR, com 4 achados reais -- ver decisao p04-sub2-codex-pr328-4-achados-corrigidos."
  - "PR #328 aberta por esta execucao (https://github.com/andre-martiini/Hermes/pull/328), CI verde nos 3 checks (GitGuardian, Testes e build, Testes das Functions Python) em todos os commits apos fix. 4 threads de revisao do Codex, todas respondidas (explicando o fix e apontando o commit) e resolvidas via mcp__github__resolve_review_thread antes do merge. Mesclada (squash) no commit 757f10a1e16a64b023a5f29f6323c355d52cdbb2, confirmado via git fetch origin main -- estado deste bloco escrito como validado NESTA MESMA execucao, apos confirmar o merge de verdade (licao da pendencia registrada no bloco da sub-entrega 1/N)."
  - "git push --delete da branch claude/p04-sub2-ledger-operacoes apos o merge falhou com HTTP 403 (permissao do token/proxy nao cobre delete de branch remota) -- branch mesclada deixada no remoto, sem acao de codigo pendente (GitHub normalmente permite deletar depois do merge pela propria UI; nenhuma tool MCP de delete de branch disponivel nesta execucao). Nao bloqueia nada -- so um item de limpeza manual."
migracao:
  dry_run: null
  executada: false
flags:
  antes: {}
  depois: {}
pendencias:
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: demanda DEV-2026-0013 (race entre consultar_autorizacao_argos e decidir_autorizacao/consumir_autorizacao) -- aguarda priorizacao do Andre."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: sipac_processos.snapshot_hash colidindo com scheduledSipacSync (achado da sub-entrega 23/N), candidato a P05."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: risco residual de validacao de outputSchema em obter_fila_atencao/consultar_elevacoes_sugeridas -- limitacao estrutural do catalogo com outputSchema."
  - "ABERTA, ja registrada no bloco da sub-entrega 1/N e nao tocada por esta: Lease.__post_init__ so valida tz-AWARENESS de expires_at, nao identidade UTC -- risco residual de baixa severidade para quem fizer o wiring de renovacao de lease (autonomy/requests.py)."
  - "NOVA, item de limpeza manual (nao bloqueia nada): branch claude/p04-sub2-ledger-operacoes nao pode ser deletada remotamente por esta execucao (git push --delete -> HTTP 403). Pode ser deletada manualmente pelo Andre pela UI do GitHub, ou por uma execucao futura com permissao de token diferente."
  - "NOVA, escopo explicito desta fatia (nao e pendencia, e decisao deliberada): este modulo (autonomy/ledger.py) so cobre os itens 6/9/10 da secao 4.5 (ledger de idempotencia por chave/hash, reentrega, conflito) e checkpoints com limite de tamanho. Os itens 1-5/7/8/11/12 da secao 4.5 (reserva/lease -- ja cobertos por autonomy/requests.py na sub-entrega 1/N -- e reconciliacao de operacao externa incerta) NAO sao cobertos por este modulo; ficam para os proximos passos do pacote P04 (passos 2/4/6/7 do plano)."
proximo_pacote: "P04 -- passo 3 do pacote (ledger de operacoes/checkpoints, itens 6/9/10 da secao 4.5) implementado como logica pura, SEM wiring em producao -- soma-se ao passo 1 (estados/leases/geracoes, sub-entrega 1/N). RECOMENDACAO desta execucao para a proxima: (1) proxima fatia natural e o passo 2 do pacote (\"servidor cria agent_run ao reservar/iniciar; conclusao e timeout sao transicoes verificadas, nao simples add de resumo\") ou o passo 4 (\"implementar assumir, renovar, registrar progresso e registrar resultado observado\" -- ja tem a base pronta em autonomy/requests.py + autonomy/ledger.py, falta so a funcao de orquestracao que costura lease+ledger+RequestStatus juntos) -- ainda logica pura, ANTES de comecar o wiring real em agent_requests.py/agent_runs.py/mcp_jobs.py; (2) alternativa igualmente valida: comecar o wiring de verdade agora (passo 5: preservar consultar/concluir legados, pedidos schema_version novo exigem lease) -- decisao de qual vir primeiro fica para a proxima execucao avaliar com mais contexto de orcamento disponivel, ja que P04 e pacote G (10 passos) e nao cabe inteiro em poucas sub-entregas. NOTAS DE PROCESSO: (1) PR #264 continua fechada, nao e mais pendencia; (2) demanda DEV-2026-0013 aguarda priorizacao do Andre; (3) esta execucao NAO encontrou PR pendente de execucao anterior ao iniciar (list_pull_requests vazio) -- trabalho novo iniciado diretamente; (4) 4 achados reais do Codex (bot do repositorio) corrigidos na propria PR, alem das 3 rodadas de revisao adversarial interna -- ver decisoes acima; (5) uma rodada de revisao em cima do fix do Codex achou que o fix so fechava metade do buraco de mutacao -- ver decisao p04-sub2-revisao-pos-codex-achado-real-mutacao-da-referencia-guardada, exemplo concreto de por que vale sempre rodar mais uma rodada focada no PROPRIO fix, nao so no achado original; (6) branch remota nao pode ser deletada por esta execucao (HTTP 403) -- ver pendencia NOVA acima; (7) Blueprint da Autonomia (artifact) a atualizar apos a confirmacao deste bloco -- ver relato final desta execucao para o desfecho."
```
