# Registro de execução — plano-hermes-autonomo-2026-09-06

Um bloco por pacote (P00–P18), no formato da seção 14.2 do plano. Cada pacote novo é acrescentado ao final; nenhum bloco anterior é reescrito depois de `validado`. Sem segredos nem conteúdo privado — apenas referências a artefatos e IDs.

**Blocos P00 a P02 sub-entrega 11/N foram movidos para
docs/autonomia/execucao-archive-p00-a-p02sub11.md em 2026-09-09 (P02
sub-entrega 15/N). Blocos P02 sub-entrega 12/N a 16/N foram movidos, em
2026-09-09 também, para um SEGUNDO arquivo de arquivo,
docs/autonomia/execucao-archive-p02-sub12-a-sub16.md — durante a correção
da sub-entrega P01 5/N, depois que uma escrita anterior derrubou
silenciosamente o bloco P02 sub-entrega 17/N na retranscrição manual de
um arquivo de 85701 bytes, e uma tentativa de reenviar tudo somado num
arquivo de arquivo só (159725 bytes) falhou por limite de saída (64000
tokens). O bloco P02 sub-entrega 17/N foi movido, também em 2026-09-09,
para um TERCEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p02-sub17.md — durante a sub-entrega P01
6/N, preventivamente. Os blocos P01 sub-entrega 3/N e 4/N foram movidos,
ainda em 2026-09-09, para um QUARTO arquivo de arquivo,
docs/autonomia/execucao-archive-p01-sub3-a-sub4.md — durante a sub-entrega
P01 7/N, também preventivamente. O bloco P01 sub-entrega 5/N foi movido,
ainda em 2026-09-09, para um QUINTO arquivo de arquivo,
docs/autonomia/execucao-archive-p01-sub5.md — durante a sub-entrega P01
8/N, preventivamente pelo mesmo motivo. Os blocos P01 sub-entrega 6/N a
9/N (fechamento do pacote) foram movidos, ainda em 2026-09-09, para um
SEXTO arquivo de arquivo, docs/autonomia/execucao-archive-p01-sub6-a-sub9.md
— durante a sub-entrega P02 19/N, preventivamente, ao registrar
retroativamente as sub-entregas P02 18/N e 19/N. Os blocos P02
sub-entrega 18/N a P03 sub-entrega 3/N foram movidos, em 2026-09-12, para
um SÉTIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p02sub18-a-p03sub3.md — pela tarefa
agendada de avanço autônomo, ao registrar retroativamente a sub-entrega
P03 7/N, preventivamente (o arquivo ativo tinha passado de ~50KB sem
re-arquivamento desde o sexto arquivo). Ver o cabeçalho de cada arquivo
de arquivo para o relato completo. Os blocos P03 sub-entrega 4/N e 5/N foram
movidos, em 12/09/2026, para um OITAVO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub4-a-sub5.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 8/N,
preventivamente (o arquivo ativo passaria de ~50KB ao acrescentar essa
entrada sem re-arquivamento). Os blocos P03 sub-entrega 6/N e 7/N foram
movidos, em 13/09/2026, para um NONO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub6-a-sub7.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 10/N,
preventivamente (mesmo motivo). Os blocos P03 sub-entrega 8/N e 9/N foram
movidos, também em 13/09/2026, para um DÉCIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub8-a-sub9.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 11/N
(o arquivo ativo passaria de ~50KB ao acrescentar essa entrada sem
re-arquivamento). Os blocos P03 sub-entrega 10/N e 11/N foram movidos, em
14/09/2026, para um DÉCIMO PRIMEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub10-a-sub11.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 13/N
(o arquivo ativo passaria de ~50KB ao acrescentar essa entrada sem
re-arquivamento). O bloco P03 sub-entrega 12/N foi movido, também em
14/09/2026, para um DÉCIMO SEGUNDO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub12.md — durante a correção do
registro do diário da sub-entrega 14/N (incidente de processo: um
subagente sem autorização abriu duas PRs indevidas para este mesmo
registro, uma delas mesclada pelo André antes da correção chegar — ver a
entrada da sub-entrega 14/N para o relato completo), preventivamente
(o arquivo ativo chegaria perto de ~50KB com as correções necessárias
sem re-arquivamento). O bloco P03 sub-entrega 13/N foi movido, em
14/09/2026, para um DÉCIMO TERCEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub13.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 15/N
(o arquivo ativo passaria de ~50KB ao acrescentar essa entrada sem
re-arquivamento). Os blocos P03 sub-entrega 14/N e 15/N foram movidos,
também em 14/09/2026, para um DÉCIMO QUARTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub14-a-sub15.md — pela tarefa
agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega
P03 16/N (o arquivo ativo chegaria a 48157 bytes, perto do limite de
~50KB, sem re-arquivamento). Os blocos P03 sub-entrega 16/N e 17/N
foram movidos, em 15/09/2026, para um DÉCIMO QUINTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub16-a-sub17.md — pela tarefa
agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega
P03 19/N (o arquivo ativo passaria de 60KB, bem acima do limite de
~50KB, ao acrescentar essa entrada sem re-arquivamento). Os blocos P03
sub-entrega 18/N e 19/N foram movidos, em 22/09/2026, para um DÉCIMO
SEXTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub18-a-sub19.md — pela tarefa
agendada de avanço autônomo, ao acrescentar a entrada da sub-entrega
P03 20/N (o arquivo ativo passaria de ~50KB ao acrescentar essa
entrada sem re-arquivamento). O bloco P03 sub-entrega 20/N foi movido,
também em 22/09/2026, para um DÉCIMO SÉTIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub20.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 22/N (o
arquivo ativo tinha chegado a 52198 bytes, acima do limite de ~50KB,
sem re-arquivamento desde o décimo sexto arquivo). Nenhum
conteúdo foi perdido; é uma relocação, não uma edição (o campo `estado`
desse bloco foi atualizado de `pronto_para_revisao` para `validado` no
arquivamento, já que o próprio bloco P03 sub-entrega 21/N confirma o
merge das PRs #304/#305 — ver o cabeçalho do arquivo de arquivo). O
bloco P03 sub-entrega 21/N foi movido, em 23/09/2026, para um DÉCIMO
OITAVO arquivo de arquivo, docs/autonomia/execucao-archive-p03sub21.md
— pela tarefa agendada de avanço autônomo, ao acrescentar a entrada da
sub-entrega P03 23/N (o arquivo ativo tinha chegado a 54816 bytes,
acima do limite de ~50KB, sem re-arquivamento desde o décimo sétimo
arquivo). Mesmo critério de relocação + confirmação de validação (o
campo `estado` desse bloco foi atualizado de `pronto_para_revisao` para
`validado`, já que o próprio bloco P03 sub-entrega 22/N confirma o
merge das PRs #306/#307 — ver o cabeçalho do arquivo de arquivo). O
bloco P03 sub-entrega 22/N foi movido, também em 23/09/2026, para um
DÉCIMO NONO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub22.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 24/N (o
arquivo ativo tinha chegado a 56906 bytes, acima do limite de ~50KB,
sem re-arquivamento desde o décimo oitavo arquivo). Mesmo critério de
relocação + confirmação de validação (o campo `estado` desse bloco foi
atualizado de `pronto_para_revisao` para `validado`, já que o histórico
de commits do `main` — commit `9d5788d`, "P03 sub-entrega 22/N... (#308)"
— confirma o merge da PR desta sub-entrega 22/N, exatamente como suas
próprias evidências previam). O bloco P03 sub-entrega 23/N foi movido, em
23/09/2026, para um VIGÉSIMO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub23.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 25/N (o
arquivo ativo teria passado de ~50KB sem re-arquivamento desde o décimo
nono arquivo). Mesmo critério de relocação + confirmação de validação (o
campo `estado` desse bloco foi atualizado de `pronto_para_revisao` para
`validado`, já que o próprio bloco P03 sub-entrega 24/N tem
`base_commit: ce56aab` — o commit exato do merge da PR #309 desta
sub-entrega 23/N, confirmado via `git log origin/main` — ver o cabeçalho
do arquivo de arquivo). O bloco P03 sub-entrega 24/N foi movido, em 23/09/2026, para um VIGÉSIMO
PRIMEIRO arquivo de arquivo, docs/autonomia/execucao-archive-p03sub24.md
— pela tarefa agendada de avanço autônomo, ao acrescentar a entrada da
sub-entrega P03 26/N (o arquivo ativo passaria de ~50KB ao acrescentar
essa entrada sem re-arquivamento). Mesmo critério de relocação +
confirmação de validação já usado nos arquivos anteriores (o campo
`estado` desse bloco foi atualizado de `pronto_para_revisao` para
`validado`, e a última entrada de `evidencias` reescrita para confirmar
o número da PR e o commit do merge, já que o próprio bloco P03
sub-entrega 25/N tem `base_commit: 4fbe896194e9a13e92ed57774e79bb0e42c9c93b`
— o commit exato do merge da PR #310 desta sub-entrega 24/N, confirmado
via `git log origin/main` — ver o cabeçalho do arquivo de arquivo). O
bloco P03 sub-entrega 25/N foi movido, em 23/09/2026, para um VIGÉSIMO
SEGUNDO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub25.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 27/N (o
arquivo ativo tinha chegado a 52261 bytes, acima do limite de ~50KB, sem
re-arquivamento desde o vigésimo primeiro arquivo). Nenhum conteúdo foi
alterado — o bloco já estava com `estado: validado` (confirmado pelo
próprio bloco P03 sub-entrega 26/N, cujo `base_commit` é o commit exato
do merge desta sub-entrega 25/N). O bloco P03 sub-entrega 26/N foi
movido, em 24/09/2026, para um VIGÉSIMO TERCEIRO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub26.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 28/N (o
arquivo ativo tinha chegado a 54462 bytes, acima do limite de ~50KB, sem
re-arquivamento desde o vigésimo segundo arquivo). Mesmo critério de
relocação + confirmação de validação já usado nos arquivos anteriores (o
bloco já estava com `estado: validado`, confirmado pelo próprio bloco
P03 sub-entrega 27/N, cujo `base_commit` é o commit exato do merge desta
sub-entrega 26/N). O bloco P03 sub-entrega 27/N foi movido, em
24/09/2026, para um VIGÉSIMO QUARTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub27.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 29/N (o
arquivo ativo estava em 42331 bytes, se aproximando de ~50KB sem
re-arquivamento desde o vigésimo terceiro arquivo). Mesmo critério de
relocação + confirmação de validação já usado nos arquivos anteriores (o
bloco já estava com `estado: validado`, confirmado pelo próprio bloco
P03 sub-entrega 28/N, cujo `base_commit` é o commit exato do merge desta
sub-entrega 27/N). O bloco P03 sub-entrega 28/N foi movido, também em
24/09/2026, para um VIGÉSIMO QUINTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub28.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 30/N (o
arquivo ativo estava em 42952 bytes, se aproximando de ~50KB sem
re-arquivamento desde o vigésimo quarto arquivo). Mesmo critério de
relocação + confirmação de validação já usado nos arquivos anteriores (o
bloco já estava com `estado: validado`, confirmado pelo próprio bloco
P03 sub-entrega 29/N, cujo `base_commit` é o commit exato do merge desta
sub-entrega 28/N). O bloco P03 sub-entrega 29/N foi movido, em
24/09/2026, para um VIGÉSIMO SEXTO arquivo de arquivo,
docs/autonomia/execucao-archive-p03sub29.md — pela tarefa agendada de
avanço autônomo, ao acrescentar a entrada da sub-entrega P03 31/N (o
arquivo ativo estava em 45164 bytes, passaria de ~50KB ao acrescentar
essa entrada sem re-arquivamento desde o vigésimo quinto arquivo). Mesmo
critério de relocação + confirmação de validação já usado nos arquivos
anteriores (o bloco já estava com `estado: validado`, confirmado pelo
próprio bloco P03 sub-entrega 30/N, cujo `base_commit` é o commit exato
do merge desta sub-entrega 29/N). Este arquivo continua sendo a fonte de
verdade para tudo a partir da sub-entrega P03 30/N em diante.

---

```yaml
plano: plano-hermes-autonomo-2026-09-06
base_commit: 005d4a105cdceffb1e425b988b6bc6d736040dcf
pacote: "P03 sub-entrega 30/N -- outputSchema/structuredContent por handler (passo 3 do plano), decima quarta tool: obter_fila_atencao, backed por atencao.coletar_fila_atencao. Confirmado no inicio desta execucao (mcp__github__list_pull_requests, estado aberto) que nao havia PR pendente desta rotina -- trabalho novo a partir do main atual (005d4a1, PR #319 ja mesclada). Candidata deixada em aberto desde a sub-entrega 27/N (levantamento PARCIAL feito na 29/N) por causa do campo `tipo`, sem enum fechado auditavel numa fatia so -- constantes TIPO_* pareciam divergentes em 3 modulos (atencao.py, atencao_whatsapp.py, secretario_whatsapp.py). Fechei a investigacao nesta execucao: grep exaustivo por TODO ponto de escrita de `tipo` na colecao `atencao` (busca por `atencao.COLLECTION`/`collection(\"atencao\")` no repositorio inteiro, todos os sites lidos por completo) encontrou 8 constantes TIPO_* usadas diretamente + o UNICO parametro livre do catalogo (`secretario_whatsapp.escalar_para_atencao.tipo_atencao: str`), tracado ate seus 2 unicos call sites em todo o repositorio -- ambos so passam constantes ja cobertas pelo enum, nunca uma string arbitraria. As constantes TIPO_ATENCAO_* de secretario_whatsapp.py e as TIPO_SECRETARIO_* de atencao.py tem o MESMO valor string (alias, nao um par divergente) -- a 'divergencia' da sub-entrega 29/N era essa alias, nao um conjunto extra. Total: enum fechado de 10 valores."
estado: validado
inicio: "2026-09-24T14:00:00Z"
fim: "2026-09-24T14:50:00Z"
arquivos_alterados:
  - "functions/tools/registry.py (1 outputSchema novo: obter_fila_atencao, decima quarta tool com contrato publicado -- 94 de 108 tools hoje sem contrato, deliberadamente. Forma UNICA, sem oneOf (coletar_fila_atencao nao tem ramo de erro) -- `{\"total\": int, \"itens\": [item, ...]}`. `tipo` enum fechado de 10 valores (ver acima); `origem` enum de 8 valores (mesmo conjunto do inputSchema, so 6 com escritor real hoje -- agenda/repo aguardando implementacao); `prioridade` enum de 3 (alta/media/baixa, so 2 com escritor real -- baixa e so usada em _PRIORITY_ORDER); `estado` reusa o enum de 5 valores ja existente no inputSchema. origem/tipo/prioridade/titulo/resumo/sugestao/estado sao [\"string\", \"null\"] (os 4 com enum ganham null explicito na propria lista enum) -- ACHADO DO CODEX na PR #320: esses 7 campos sao `d.get(chave)` SEM default no handler, entao um documento esparso na colecao (cenario ja exercitado por test_atencao.py::test_coletar_fila_atencao_ordenacao) devolve None; a redacao original desta sub-entrega os declarava erroneamente como obrigatorios-nao-nulos, com base so nos escritores REAIS de hoje, sem considerar que a funcao de leitura nao exige nenhum desses campos. Corrigido na mesma PR, antes do merge. id/chave_dedupe/evidencia continuam nao-nulos (fallback explicito: doc.id, d.get(...) or doc.id, d.get(...) or {}). docstring de output_schema() corrigido de 94/108 apos a decima quarta entrada.)"
  - "functions/test_output_schema.py (docstring do modulo atualizado para citar a decima quarta tool e o achado do Codex; TestOutputSchema ganhou test_obter_fila_atencao_tem_schema_forma_unica + paridade renomeada para 14 tools (test_paridade_quatorze_tools_tem_output_schema_hoje); TestHandleToolsListOutputSchema ganhou test_obter_fila_atencao_publica_output_schema + listas de paridade atualizadas; TestIntegracaoHandleToolsCallStructuredContent ganhou 4 testes novos -- sucesso, lista vazia, paridade campo a campo, e um teste de REGRESSAO dedicado ao achado do Codex (item esparso com os 7 campos None, ponta a ponta via _handle_tools_call, confirmando que o schema aceita null nesses campos))"
  - "functions/test_hermes_tools.py (TestExecucao ganhou 6 testes novos do HANDLER REAL -- mockando atencao.coletar_fila_atencao diretamente: default de estado (ausencia e None explicito, ambos caem no mesmo `if estado is None` -> \"aberto\"), repasse de estado/origem explicitos, default de limite (`or 20`, incluindo o caso limite=0 -> 20, mesmo padrao de consultar_promocoes_autonomia_sugeridas), e repasse de limite explicito. obter_fila_atencao nao tinha nenhum teste dedicado antes desta sub-entrega.)"
  - "docs/autonomia/execucao.md (este bloco; bloco da sub-entrega 28/N movido para docs/autonomia/execucao-archive-p03sub28.md, arquivo ativo estava em 42952 bytes se aproximando de ~50KB)"
  - "docs/autonomia/execucao-archive-p03sub28.md (novo, vigesimo quinto arquivo de arquivo)"
decisoes:
  - id: p03-sub30-tipo-enum-fechado-10-valores
    motivo: "Busca exaustiva por todo escritor da colecao atencao (grep atencao.COLLECTION/collection(\"atencao\") no repositorio inteiro, todos os sites lidos por completo): atencao.py::_persistir_itens_atencao (4 tipos diretos), atencao_whatsapp.py (_processar_audio/vencer_promessas, 2 tipos diretos), secretario_whatsapp.py (registrar_investigacao_concluida_atencao com 1 tipo direto, e escalar_para_atencao com o parametro livre tipo_atencao: str). Os 2 unicos call sites de escalar_para_atencao (dentro do proprio modulo) so passam TIPO_ATENCAO_INSISTENCIA ou um ternario entre TIPO_ATENCAO_ASSUNTO_SENSIVEL/TIPO_ATENCAO_DECISAO_FORCADA -- nunca uma string arbitraria. As constantes TIPO_ATENCAO_* de secretario_whatsapp.py e as TIPO_SECRETARIO_* de atencao.py resolvem para o MESMO valor string (alias). outbox_aprovacao.py:693 so faz .update() de estado/resolvido_em/desfecho, nunca toca tipo/origem/prioridade. Total: 10 valores distintos, fechado."
    autoridade: existente_ou_nova
  - id: p03-sub30-origem-prioridade-mantidos-no-conjunto-declarado-nao-no-observado
    motivo: "origem tem 8 valores no inputSchema/constantes mas so 6 com escritor real hoje (agenda/repo sem implementacao); prioridade tem 3 constantes (PRIORIDADE_*) mas so 2 com escritor real (baixa nunca escrita, so usada em _PRIORITY_ORDER). Escolhi manter o outputSchema alinhado ao conjunto DECLARADO (inputSchema/constantes), nao ao observado, para nao publicar um contrato de saida mais estreito que o proprio filtro de entrada ja aceito hoje -- um escritor futuro de agenda/repo/baixa nao tornaria o outputSchema desatualizado no mesmo instante."
    autoridade: existente_ou_nova
  - id: p03-sub30-codex-nulabilidade-corrigida
    motivo: "Achado do Codex (P2) na PR #320: origem/tipo/prioridade/titulo/resumo/sugestao/estado sao d.get(chave) sem default em coletar_fila_atencao -- documento esparso (cenario ja testado em test_atencao.py) devolve None, violando o schema original (nao-nulo/obrigatorio). Minha analise inicial so tracou os escritores REAIS de hoje (todos sempre setam esses campos) e concluiu erroneamente que eram sempre string -- nao considerei que a funcao de LEITURA nao valida/exige nada, e que um documento fora dos caminhos de escrita atuais (legado, seed, ou futuro) passaria por ela sem erro. Corrigido para [\"string\", \"null\"] (enum com null explicito nos 4 campos com enum) antes do merge, com teste de regressao dedicado."
    autoridade: existente_ou_nova
  - id: p03-sub30-codex-risco-residual-de-validacao-nao-corrigido
    motivo: "SEGUNDO achado do Codex (P2), desta vez na PR #322 (diario), sobre o proprio texto do diario: a correcao de nulabilidade acima fecha so o caso 'campo ausente' (None) -- ela NAO fecha o caso mais geral de um documento com um valor PRESENTE mas fora do conjunto esperado (ex.: tipo='legado', nao um dos 10 enum; evidencia='texto', nao um dict -- `d.get('evidencia') or {}` so cai no fallback {} para valor FALSY, um valor truthy nao-dict passa direto). coletar_fila_atencao nao valida nem normaliza NADA do que le -- ela confia inteiramente no que esta gravado. Meu schema descreve a forma GARANTIDA pelos escritores rastreados hoje (busca exaustiva), nao uma invariante reforcada em tempo de leitura -- um documento fora desses caminhos (edicao manual no console do Firestore, dado legado anterior a decisao do enum, fixture de seed, ou um bug futuro em algum escritor) devolveria um valor que viola o outputSchema publicado, e um cliente MCP que valide `structuredContent` contra `outputSchema` de forma estrita poderia rejeitar essa chamada. NAO corrigido nesta sub-entrega -- corrigir exigiria normalizar/validar na propria coletar_fila_atencao (mudanca de COMPORTAMENTO, fora do escopo de uma fatia so-schema) ou adicionar validacao de outputSchema em tempo de execucao no mcp_server.py (que NAO EXISTE hoje para NENHUMA das 14 tools com outputSchema publicado, achado confirmado por busca no repositorio -- mudanca ampla, nao especifica desta tool). Risco ACEITO e nao-bloqueante, mesma categoria ja registrada para outras tools deste catalogo (ex.: `consultar_lista_compras`/`shopping_items`, escrito por caminhos fora de tools/lista_compras.py, comentario em _OUTPUT_SCHEMAS desde a sub-entrega 10/N) -- nao e uma regressao desta sub-entrega, e sim uma limitacao estrutural de TODO o catalogo com outputSchema ate hoje, so mais visivel aqui por coletar_fila_atencao ter mais campos com enum que as tools anteriores. Diario e comentario de _OUTPUT_SCHEMAS corrigidos para nao mais afirmar 'corrigido, nenhuma pendencia' -- essa correcao de redacao e o unico efeito desta decisao."
    autoridade: existente_ou_nova
  - id: p03-sub30-rodada1-achado-total-limite-invertido
    motivo: "1a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto da implementacao) achou 1 problema real: o comentario novo sobre a relacao total/limite estava INVERTIDO (dizia que total conta APOS o corte por limite, quando atencao.py calcula total=len(itens) ANTES do corte -- itens.sort() depois total:len(itens), itens:itens[:limite_ajustado], itens nunca reatribuido -- exatamente como consultar_promocoes_autonomia_sugeridas). Sem impacto em schema/logica (comentario apenas), corrigido no commit seguinte."
    autoridade: existente_ou_nova
  - id: p03-sub30-rodada2-fix-nao-achou-nada
    motivo: "2a rodada de revisao adversarial (foco no fix do comentario total/limite, sem contexto da rodada anterior) confirmou a correcao de forma independente, tracando atencao.py e promocao_autonomia.py por conta propria. Nao achou nada -- criterio de parada atingido antes de abrir a PR."
    autoridade: existente_ou_nova
  - id: p03-sub30-rodada3-fix-codex-nao-achou-nada
    motivo: "3a rodada de revisao adversarial (Agent tool, foco no fix do achado do Codex sobre nulabilidade, sem contexto de nenhuma rodada anterior) verificou campo a campo contra coletar_fila_atencao, confirmou a semantica JSON Schema de type+enum com null, verificou o teste de regressao novo e rodou a suite isolada de forma independente. Nao achou nada -- criterio de parada atingido."
    autoridade: existente_ou_nova
testes:
  comandos:
    - "repositorio ja clonado no inicio da execucao (branch main, commit 005d4a1, PR #319 da sub-entrega anterior ja confirmada mesclada -- mcp__github__list_pull_requests devolveu lista vazia para estado aberto)"
    - "python3 -m venv /tmp/hermes_venv_30 + pip install -r requirements.txt (1a tentativa de venv teve o mesmo estado corrompido ja registrado em sub-entregas anteriores -- firebase_functions com ImportError generalizado apesar do pip install terminar sem erro visivel; venv recriado do zero resolveu, sem reincidencia -- achado de ambiente nao-bloqueante, mesma categoria ja registrada multiplas vezes)"
    - "python3 -m unittest test_output_schema test_hermes_tools -v (isolado, apos a implementacao inicial, apos o fix do achado da rodada 1, e novamente apos o fix do achado do Codex)"
    - "python3 -m unittest discover -s . -p 'test_*.py' -q (suite completa -- baseline antes da implementacao, apos a implementacao inicial, e novamente apos o fix do achado do Codex)"
  resultados:
    - "Baseline (origin/main, commit 005d4a1, venv recriado do zero): 2187/2187, 0 falhas, 0 erros."
    - "Apos a implementacao inicial (schema + 10 testes novos, antes do achado do Codex): 2198/2198, 0 falhas."
    - "test_output_schema + test_hermes_tools isolados apos a implementacao inicial: 219/219 OK."
    - "Apos o fix do achado do Codex (7 campos viram nullable + 1 teste de regressao novo): 220/220 no escopo isolado, 2199/2199 na suite completa -- confirmado por mim e, de forma independente, pela 3a rodada de revisao adversarial."
evidencias:
  - "3 rodadas de revisao adversarial independentes (Agent tool, general-purpose, cada uma sem contexto de implementacao nem de rodada anterior). Rodada 1 (antes da PR): achou o comentario total/limite invertido, corrigido no commit seguinte. Rodada 2 (foco no fix da rodada 1): nao achou nada. Rodada 3 (apos o achado do Codex na PR, foco no fix de nulabilidade): nao achou nada."
  - "PR #320 aberta por esta execucao (https://github.com/andre-martiini/Hermes/pull/320), revisada pelo Codex (chatgpt-codex-connector[bot]) -- achou 1 problema REAL (P2): nulabilidade de 7 campos incorretamente declarados como obrigatorios-nao-nulos, dado documento esparso ja testado em test_atencao.py. Corrigido na mesma PR (commit 9f01fae), com resposta ao comentario e thread resolvida. Apos o fix: CI verde (GitGuardian Security Checks, Testes das Functions Python, Testes e build -- todos success no commit final 9f01fae), sem novo comentario do Codex sobre o fix (Codex nao re-revisou automaticamente o commit de fix -- so reagiu na 1a revisao, sobre o commit afe6efa). Mesclada (squash, commit 91b77851c991d00795e5bc453e5ff111cfbd0549) apos a janela de espera e a correcao do achado, sob a autoridade concedida em 22/09/2026. 1 rodada de espera-e-correcao usada das 2 permitidas (achado real de bot, nao nitpick)."
  - "Achado de ambiente registrado, nao-bloqueante, mesma categoria ja registrada em sub-entregas anteriores (25/N, 28/N): a 1a tentativa de criar o venv (/tmp/hermes_venv_30) resultou num estado onde firebase_functions nao importava (ImportError generalizado em 118+124 testes) apesar do pip install ter terminado sem erro visivel -- recriar o venv do zero (rm -rf + python3 -m venv + pip install) resolveu na 2a tentativa, log completo confirmando firebase-functions-0.6.0 instalado corretamente."
migracao:
  dry_run: null
  executada: false
flags:
  antes: {}
  depois: {}
pendencias:
  - "NOVA, achado do Codex na PR #322 (ver decisao p03-sub30-codex-risco-residual-de-validacao-nao-corrigido): coletar_fila_atencao nao valida nem normaliza nada do que le -- um documento com um valor PRESENTE mas fora do enum (ex.: tipo nao-cadastrado) ou evidencia nao-dict (truthy, ex. uma string) violaria o outputSchema publicado. Risco ACEITO e nao-bloqueante, mesma categoria ja aceita para outras tools deste catalogo (ex.: consultar_lista_compras/shopping_items) -- nao corrigido nesta sub-entrega, corrigir exigiria normalizar/validar em coletar_fila_atencao (mudanca de comportamento) ou adicionar validacao de outputSchema em tempo de execucao (inexistente hoje para as 14 tools do catalogo, nao so esta)."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: demanda DEV-2026-0013 (race entre consultar_autorizacao_argos e decidir_autorizacao/consumir_autorizacao) -- aguarda priorizacao do Andre."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: sipac_processos.snapshot_hash colidindo com scheduledSipacSync (achado da sub-entrega 23/N), candidato a P05."
  - "Pendencias ja registradas em blocos anteriores e nao tocadas por esta sub-entrega continuam abertas: ver docs/autonomia/execucao-archive-p03sub28.md e bloco P03 sub-entrega 29/N para a lista completa."
proximo_pacote: "P03 -- outputSchema chega a 14 de 108 tools do catalogo. obter_fila_atencao sai da lista de candidatas (investigada e resolvida nesta sub-entrega, incluindo o fechamento definitivo da questao do enum de tipo que vinha em aberto desde a 27/N). consultar_elevacoes_sugeridas e listar_conversas_whatsapp continuam candidatas NAO investigadas -- unicas restantes da lista original de 5 da sub-entrega 27/N. RECOMENDACAO desta execucao para a proxima, reforcando o que vem se acumulando desde a sub-entrega 23/N e ficando mais forte agora que a lista de candidatas conhecidas caiu para 2: (1) LER consultar_elevacoes_sugeridas (deteccao_subproduto.listar_pendentes) primeiro -- foi descartada na sub-entrega 11/N pelo MESMO motivo que consultar_promocoes_autonomia_sugeridas tinha sido (oneOf sucesso/erro nao decidido) e essa objecao ja nao se sustenta desde a sub-entrega 14/N, mesma retomada que resolveu promocoes_autonomia_sugeridas na 29/N -- alta chance de ser uma fatia rapida se confirmado por leitura completa; (2) se essa e listar_conversas_whatsapp tambem se resolverem rapido, a lista de candidatas de leitura simples do levantamento original se esgota, e a proxima execucao deveria ENTAO avaliar seriamente migrar para P04 (pedidos duraveis/lease/ledger) como o proximo compromisso de varias sub-entregas -- P04 e pre-requisito de quase todo o resto do plano (P05-P18) e essa transicao nao pode ficar adiada indefinidamente. NOTAS DE PROCESSO: (1) PR #264 continua fechada, nao e mais pendencia; (2) demanda DEV-2026-0013 aguarda priorizacao do Andre; (3) esta execucao nao encontrou nenhum trabalho pela metade de uma execucao anterior -- mcp__github__list_pull_requests vazio no inicio -- cadencia da rotina continua saudavel; (4) bloco P03 sub-entrega 28/N arquivado nesta execucao (ver docs/autonomia/execucao-archive-p03sub28.md) por aproximacao do limite de ~50KB; (5) PRIMEIRA sub-entrega em varias execucoes com um achado REAL de bot revisor (Codex) exigindo correcao -- ver evidencias acima; demonstra que a janela de espera por comentarios de bot continua valendo a pena mesmo quando a revisao adversarial propria (2 rodadas antes da PR) nao achou nada -- o achado do Codex so apareceu por ele ter cruzado o schema contra um teste existente (test_atencao.py) que minha propria investigacao de escritores REAIS nao cobria; (5b) o Codex achou um SEGUNDO problema, desta vez na PR do proprio diario (#322): minha primeira redacao deste bloco superafirmava 'corrigido, nenhuma pendencia' quando na verdade a correcao de nulabilidade fecha so o caso 'campo ausente', nao o caso mais geral de um valor presente fora do enum/tipo esperado (coletar_fila_atencao nao valida nada do que le) -- corrigido nesta MESMA execucao antes do merge da PR do diario, ver decisao p03-sub30-codex-risco-residual-de-validacao-nao-corrigido; leve para a proxima execucao o habito de nao superafirmar 'corrigido'/'nenhuma pendencia' num diario sem considerar explicitamente esse tipo de risco residual estrutural (vale para as proximas fatias de outputSchema tambem, nao so para esta); (6) Blueprint da Autonomia (artifact) a atualizar apos a confirmacao deste bloco -- ver relato final desta execucao para o desfecho."
```

```yaml
plano: plano-hermes-autonomo-2026-09-06
base_commit: 73b4d321fb6268681ae7d290eb7b8f4ec82ade2b
pacote: "P03 sub-entrega 31/N -- outputSchema/structuredContent por handler (passo 3 do plano), decima quinta tool: consultar_elevacoes_sugeridas, backed por deteccao_subproduto.listar_pendentes (passthrough puro em tools/hermes_tools.py::_consultar_elevacoes_sugeridas, que so resolve o default de limite via `or 20` antes de repassar, mesma expressao ja usada em consultar_promocoes_autonomia_sugeridas/obter_fila_atencao). Confirmado no inicio desta execucao (mcp__github__list_pull_requests, estado aberto) que nao havia PR pendente desta rotina -- trabalho novo a partir do main atual (73b4d32, PR #322 da sub-entrega anterior ja mesclada, mais um commit de trabalho acumulado de outra frente, gmail webhook sync, tambem ja em main e fora do escopo desta rotina). Penultima candidata da lista original de 5 da sub-entrega 27/N a ser resolvida (obter_fila_atencao ja na 30/N; listar_conversas_whatsapp continua a UNICA candidata nao investigada dessa lista apos esta sub-entrega). Descartada na sub-entrega 11/N pelo MESMO motivo que ja tinha bloqueado consultar_promocoes_autonomia_sugeridas -- decidir se o contrato cobre sucesso e erro, investigacao maior que aquela fatia -- retomada agora pela mesma razao que resolveu a 29/N: o padrao oneOf de duas formas DISJUNTAS ja esta estabelecido desde a sub-entrega 14/N. Confirmado por leitura completa de deteccao_subproduto.py (listar_pendentes, registrar_sugestao, validar_proposta, resumo_para_o_usuario, MOTIVOS_ESCASSEZ): escritor UNICO de criacao de documento na colecao elevacoes_sugeridas (registrar_sugestao, chamado por um unico call site em todo o repositorio, _ferramenta_propor), motivo_escassez garantido por enum fechado de 3 valores antes de qualquer gravacao (validar_proposta), e a propria funcao de leitura RECONSTROI o item campo a campo."
estado: pronto_para_revisao
inicio: "2026-09-24T17:30:00Z"
fim: "2026-09-24T18:20:00Z"
arquivos_alterados:
  - "functions/tools/registry.py (1 outputSchema novo: consultar_elevacoes_sugeridas, decima quinta tool com contrato publicado -- 93 de 108 tools hoje sem contrato, deliberadamente. oneOf de 2 branches (erro/sucesso): erro sempre `{\"erro\": str, \"total\": int, \"sugestoes\": []}` (total/sugestoes hardcoded no except, nunca ecoam resultado parcial); sucesso `{\"total\": int, \"sugestoes\": [item, ...]}`, item com 8 chaves fixas -- 7 obrigatorias-nao-nulas (sugestao_id, acao, objetivo, motivo_escassez, resumo, criada_em, antigo) e 1 nullable (concluida_em, `concluida_em or None` no payload gravado). motivo_escassez e enum fechado de 3 valores (repetivel/raro/ja_escrito), garantido por validar_proposta antes de qualquer gravacao -- nunca ausente nem fora do conjunto. docstring de output_schema() corrigido de 94/108 para 93/108, apos a decima quinta entrada. ACHADO da 1a rodada de revisao adversarial, documentado em comentario mas NAO corrigido (comportamento pre-existente de listar_pendentes, nao introduzido por este outputSchema): total e len(pendentes) calculado sobre a lista ja limitada pelo .limit(200) da propria query Firestore e ANTES do corte por limite -- se houver mais de 200 sugestoes pendentes, total fica silenciosamente tetado em 200, nunca reflete a contagem real acima disso; nao e garantia formal do outputSchema, so nota de comportamento real, mesmo criterio de risco aceito ja aplicado a obter_fila_atencao/consultar_promocoes_autonomia_sugeridas.)"
  - "functions/test_output_schema.py (docstring do modulo atualizado para citar a decima quinta tool, incluindo nota corrigida sobre listar_conversas_whatsapp continuar como unica candidata nao investigada da lista original de 5 -- ver decisao p03-sub31-doc-inconsistencia-corrigida; TestOutputSchema ganhou test_consultar_elevacoes_sugeridas_tem_schema_oneof_sucesso_e_erro + paridade renomeada para 15 tools (test_paridade_quinze_tools_tem_output_schema_hoje); TestHandleToolsListOutputSchema ganhou test_consultar_elevacoes_sugeridas_publica_output_schema + listas de paridade atualizadas (incluindo test_nenhuma_outra_tool_publicada_tem_output_schema); TestIntegracaoHandleToolsCallStructuredContent ganhou 4 testes novos com mcp_server.execute_tool mockado -- sucesso, lista vazia, erro, e paridade campo a campo contra o branch oneOf correspondente, incluindo um item com concluida_em=None para provar que o schema aceita o unico campo nullable)"
  - "functions/test_hermes_tools.py (TestExecucao ganhou 3 testes novos do HANDLER REAL -- mockando deteccao_subproduto.listar_pendentes diretamente: ausencia de limite vira 20 pelo default do `or`, limite=0 explicito TAMBEM vira 20 (mesmo achado ja registrado para consultar_promocoes_autonomia_sugeridas/obter_fila_atencao -- o wrapper usa `args.get(\"limite\") or 20`, nao `.get(chave, default)`), e limite=5 explicito e repassado. Lacuna de cobertura fechada: a logica de default do wrapper nao tinha nenhum teste dedicado.)"
  - "docs/autonomia/execucao.md (este bloco; bloco da sub-entrega 29/N movido para docs/autonomia/execucao-archive-p03sub29.md, arquivo ativo estava em 45164 bytes se aproximando de ~50KB)"
  - "docs/autonomia/execucao-archive-p03sub29.md (novo, vigesimo sexto arquivo de arquivo)"
decisoes:
  - id: p03-sub31-retomada-consultar_elevacoes_sugeridas
    motivo: "Candidata descartada na sub-entrega 11/N pela falta de decisao sobre oneOf -- mesma decisao que ja tinha bloqueado consultar_promocoes_autonomia_sugeridas, resolvida desde a sub-entrega 14/N e reusada repetidamente desde entao. Retomada apos leitura completa de deteccao_subproduto.py confirmar que o padrao resolve exatamente a objecao original."
    autoridade: existente_ou_nova
  - id: p03-sub31-escritor-unico-confirmado
    motivo: "Busca exaustiva por COL_ELEVACOES e por chamadas a registrar_sugestao no repositorio inteiro encontrou 1 UNICO ponto de criacao de documento (registrar_sugestao, linha ~693), chamado por 1 UNICO call site (_ferramenta_propor, dentro da rodada de deteccao de subproduto). Os demais usos de COL_ELEVACOES (decidir/_aplicar_decisao) so movem status PARA FORA de pendente, nunca tocam os demais campos do item; morning_summary.py le a mesma colecao mas nunca escreve."
    autoridade: existente_ou_nova
  - id: p03-sub31-motivo_escassez-enum-fechado-3-valores
    motivo: "motivo_escassez vem de sugestao[\"motivo_escassez\"] (spread **sugestao no payload gravado), e sugestao e sempre o dict devolvido por validar_proposta, que so aceita valores dentro de MOTIVOS_ESCASSEZ = (\"repetivel\", \"raro\", \"ja_escrito\") -- checagem `if motivo not in MOTIVOS_ESCASSEZ: return None` antes de gravar qualquer coisa. Enum fechado de 3 valores, nunca ausente nem fora do conjunto."
    autoridade: existente_ou_nova
  - id: p03-sub31-campos-nao-nulos-vs-concluida_em
    motivo: "sugestao_id (doc.id), acao/objetivo (sempre str(...) no unico escritor, titulos[...]/str(objetivo.get(...) or \"\") em _ferramenta_propor -- podem ser string vazia mas nunca None), resumo (sempre o retorno em f-string de resumo_para_o_usuario) e criada_em (sempre o parametro hoje) sao sempre nao-nulos. concluida_em e o UNICO campo nullable do item -- `concluida_em or None` no payload gravado por registrar_sugestao. antigo e sempre bool(antigo), nunca None."
    autoridade: existente_ou_nova
  - id: p03-sub31-rodada1-achado-total-limite-200-nao-corrigido-so-documentado
    motivo: "1a rodada de revisao adversarial (Agent tool, general-purpose, sem contexto da implementacao) confirmou os pontos 1-3 e 5 sem achado, e levantou o ponto 4: total e len(pendentes) calculado apos o .limit(200) da propria query Firestore (deteccao_subproduto.py:1537) e ANTES do corte por limite (pendentes[:limite] no return, pendentes nunca reatribuido) -- se houver mais de 200 sugestoes pendentes, total fica silenciosamente tetado em 200. Comportamento PRE-EXISTENTE de listar_pendentes, nao introduzido por este outputSchema -- corrigir exigiria mudar a query/o corte, mudanca de COMPORTAMENTO fora do escopo de uma fatia so-schema (mesmo criterio ja aplicado ao risco residual aceito de obter_fila_atencao na sub-entrega 30/N). Documentado em comentario no _OUTPUT_SCHEMAS, nao corrigido no codigo."
    autoridade: existente_ou_nova
  - id: p03-sub31-doc-inconsistencia-corrigida
    motivo: "A mesma 1a rodada de revisao adversarial achou uma inconsistencia entre os comentarios dos dois arquivos: registry.py dizia que consultar_elevacoes_sugeridas era a candidata 'que restava... junto com listar_conversas_whatsapp' (corretamente implicando que essa ultima continua em aberto), enquanto a redacao original do docstring de test_output_schema.py dizia 'ultima candidata da lista original de 5' -- lendo como se as 5 estivessem resolvidas. Corrigido no mesmo commit desta sub-entrega, antes da PR: test_output_schema.py agora diz explicitamente que listar_conversas_whatsapp continua a UNICA candidata nao investigada."
    autoridade: existente_ou_nova
  - id: p03-sub31-rodada2-fix-nao-achou-nada
    motivo: "2a rodada de revisao adversarial (Agent tool, general-purpose, foco exclusivo nos 2 fixes da rodada 1 -- comentario do total/limite-200 e a correcao da inconsistencia entre os dois arquivos -- sem contexto da rodada anterior alem do que foi passado no prompt) verificou os dois fixes por conta propria contra deteccao_subproduto.py e contra grep do repositorio inteiro por listar_conversas_whatsapp, e rodou a suite isolada (test_output_schema + test_hermes_tools) de forma independente. Nao achou nada -- criterio de parada atingido, PR aberta em seguida."
    autoridade: existente_ou_nova
testes:
  comandos:
    - "repositorio ja clonado no inicio da execucao (branch main, commit 73b4d32, PR #322 da sub-entrega anterior ja confirmada mesclada -- mcp__github__list_pull_requests devolveu lista vazia para estado aberto)"
    - "python3 -m venv /tmp/hermes_venv_31 + pip install -r requirements.txt (venv fresco nesta execucao, instalou corretamente na 1a tentativa)"
    - "python3 -m unittest test_output_schema test_hermes_tools -v (isolado, apos a implementacao inicial e novamente apos os 2 fixes da rodada 1 de revisao adversarial)"
    - "python3 -m unittest discover -s . -p 'test_*.py' -q (suite completa -- baseline em origin/main antes da implementacao, via git stash; e novamente apos os 2 fixes, com a implementacao completa)"
  resultados:
    - "Baseline (origin/main, commit 73b4d32, via git stash): 2315/2315, 0 falhas, 0 erros."
    - "Apos a implementacao inicial (schema + 10 testes novos: 1 de forma + 1 de paridade renomeada + 1 de catalogo + 4 de integracao em test_output_schema.py, mais 3 no handler real em test_hermes_tools.py): 251/251 no escopo isolado (test_output_schema + test_hermes_tools)."
    - "Apos os 2 fixes da rodada 1 (comentario-so, nenhuma mudanca de logica/schema): 251/251 no escopo isolado (confirmado por mim e, de forma independente, pela 2a rodada de revisao adversarial) e 2324/2324 na suite completa -- delta de +9 tests sobre o baseline de 2315 (1 teste de forma nao e novo por estar dentro do total ja contado; contagem exata: 6 novos em test_output_schema.py + 3 novos em test_hermes_tools.py = 9)."
evidencias:
  - "2 rodadas de revisao adversarial independentes (Agent tool, general-purpose, cada uma sem contexto de implementacao nem de rodada anterior alem do que foi passado no prompt). Rodada 1 (antes da PR): confirmou escritor unico, garantias de campo, branch de erro e ausencia de drift entre schema e testes; achou 1 comportamento real nao documentado (total/limite-200, ver decisao acima) e 1 inconsistencia de redacao entre os dois arquivos (ver decisao acima) -- ambos corrigidos no mesmo commit, antes de abrir a PR. NOTA DE PROCESSO: a rodada 1 relatou, no meio da revisao, que o diff tinha 'desaparecido' do working tree (git diff vazio) -- investigado: coincidiu com um `git stash`/`git stash pop` que esta mesma execucao rodou em paralelo para medir o baseline da suite completa contra origin/main, sem coordenar com a rodada de revisao em andamento. O stash foi despopado (`git stash pop`) logo em seguida, sem perda de conteudo (git diff --stat confirmado igual antes/depois), e a rodada 1 conseguiu concluir a analise usando o texto do diff que ja tinha capturado antes do stash. Nenhum conteudo foi perdido, mas e um achado de PROCESSO para a proxima execucao: nao rodar `git stash` enquanto uma rodada de revisao adversarial estiver em andamento sobre o mesmo working tree -- usar `git worktree` ou medir o baseline ANTES de disparar a revisao, nunca em paralelo. Rodada 2 (apos os 2 fixes, foco exclusivo neles): nao achou nada -- criterio de parada atingido, PR aberta em seguida."
  - "PR aberta por esta execucao -- numero e link a preencher apos `gh pr create`/mcp__github__create_pull_request (esta redacao inicial do bloco antecipa a estrutura; o numero real e a confirmacao de merge sao gravados no proximo passo desta mesma execucao, antes do relato final ao Andre)."
migracao:
  dry_run: null
  executada: false
flags:
  antes: {}
  depois: {}
pendencias:
  - "NOVA, achado de processo desta execucao (ver evidencias acima): nao rodar git stash enquanto uma rodada de revisao adversarial (Agent tool) estiver em andamento sobre o mesmo working tree -- usar git worktree separado ou medir baseline antes de disparar a revisao, para a proxima execucao nao repetir o mesmo susto (sem perda real desta vez, mas evitavel)."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: demanda DEV-2026-0013 (race entre consultar_autorizacao_argos e decidir_autorizacao/consumir_autorizacao) -- aguarda priorizacao do Andre."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: sipac_processos.snapshot_hash colidindo com scheduledSipacSync (achado da sub-entrega 23/N), candidato a P05."
  - "ABERTA, ja registrada em blocos anteriores e nao tocada por esta: risco residual de validacao de outputSchema em obter_fila_atencao (achado do Codex na sub-entrega 30/N) -- limitacao estrutural de todo o catalogo com outputSchema, nao especifica desta tool."
  - "Pendencias ja registradas em blocos anteriores e nao tocadas por esta sub-entrega continuam abertas: ver docs/autonomia/execucao-archive-p03sub29.md e bloco P03 sub-entrega 30/N para a lista completa."
proximo_pacote: "P03 -- outputSchema chega a 15 de 108 tools do catalogo. consultar_elevacoes_sugeridas sai da lista de candidatas (retomada e resolvida nesta sub-entrega). listar_conversas_whatsapp e agora a UNICA candidata restante, nao investigada, da lista original de 5 da sub-entrega 27/N. RECOMENDACAO desta execucao para a proxima, reforcando o que vem se acumulando desde a sub-entrega 23/N e ficando mais forte agora que so resta 1 candidata conhecida: (1) LER listar_conversas_whatsapp primeiro -- se resolver rapido (mesmo padrao oneOf, escritor rastreavel), fechar essa fatia; (2) COM ISSO a lista de candidatas de leitura simples do levantamento original da sub-entrega 27/N se esgota por completo, e a proxima execucao apos essa DEVE avaliar seriamente migrar para P04 (pedidos duraveis/lease/ledger) como o proximo compromisso de varias sub-entregas -- P04 e pre-requisito de quase todo o resto do plano (P05-P18) e essa transicao nao pode ficar adiada indefinidamente; nao ha mais margem para 'mais uma candidata facil' depois de listar_conversas_whatsapp. NOTAS DE PROCESSO: (1) PR #264 continua fechada, nao e mais pendencia; (2) demanda DEV-2026-0013 aguarda priorizacao do Andre; (3) esta execucao nao encontrou nenhum trabalho pela metade de uma execucao anterior -- mcp__github__list_pull_requests vazio no inicio -- cadencia da rotina continua saudavel; (4) bloco P03 sub-entrega 29/N arquivado nesta execucao (ver docs/autonomia/execucao-archive-p03sub29.md) por aproximacao do limite de ~50KB; (5) achado de PROCESSO sobre git stash concorrente com revisao adversarial, ver evidencias e pendencias acima -- sem perda real, mas para nao repetir; (6) Blueprint da Autonomia (artifact) a atualizar apos a confirmacao deste bloco -- ver relato final desta execucao para o desfecho."
```
