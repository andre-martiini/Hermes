---
type: log
title: Histórico do bundle OKF
description: Registro cronológico de mudanças na documentação operacional do Hermes.
tags: [hermes, okf, changelog]
timestamp: 2026-08-01T00:00:00Z
---

# Log

- **2026-09-05** — Modo Secretário no WhatsApp: Contato Prioritário Pré-Avisado e Investigação Conversacional (`functions/secretario_whatsapp.py`, `functions/atencao.py`, `functions/tools/registry.py`, `functions/tools/hermes_tools.py`, `functions/tools/schemas/`, `functions/test_secretario_whatsapp.py`, PR #172). Implementa a Seção 3.3 do plano (`docs/modo-secretario-hermes.md`), permitindo ao André pré-avisar o Hermes quando aguardar resposta de alguém específico sobre tema específico. (1) Três novas tools MCP sem confirmação prévia no Telegram: `preparar_contato_prioritario_secretario` (registra briefing prioritário com resolução automática de nome/telefone para chat_id, inclusão automática na allowlist e validade padrão até 23:59:59 BRT), `consultar_contatos_prioritarios_secretario` (lista briefings com atualização automática de status para expirado quando vencidos) e `cancelar_contato_prioritario_secretario` (cancela briefing ativo antecipadamente). (2) Coleção `secretario_contatos_prioritarios/{chat_id}` com campos `assunto`, `o_que_precisa_saber`, `status` (`ativo`|`concluido`|`expirado`|`cancelado`), `valido_ate`, `resumo_estruturado` e `informacao_obtida`. (3) Injeção dinâmica no fluxo reativo do WhatsApp (`processar_mensagem_secretario`): quando há briefing ativo, injeta contexto de investigação ativa no prompt do LLM e estende o teto de trocas para até 6 turnos (`max_trocas_prioritario`, configurável via `system/settings.whatsapp_secretario`); disponibiliza a ferramenta estruturada `concluir_investigacao_prioritaria` no loop do LLM. (4) Conclusão estruturada: ao obter a informação requerida (ou ao atingir o teto de trocas), marca o briefing como concluído e registra pendência na fila de atenção (`atencao`) com prioridade MÉDIA (`secretario_investigacao_concluida`), contendo o resumo pronto para leitura do André. (5) Guardrails inegociáveis preservados: prefixo `**Hermes Bot:** `, envio exclusivo pela janela de cancelamento do outbox (`secretario_whatsapp` em `aguardando_janela` com card de cancelamento no Telegram), assimetria estrita de agenda (livre nunca confirma compromisso) e proteção absoluta contra vazamento de dados de saúde ou finanças com escalação imediata em alta prioridade.

- **2026-09-05** — Modo Secretário no WhatsApp (MVP) com assimetria de agenda e veto humano garantido (`functions/secretario_whatsapp.py`, `functions/atencao.py`, `functions/atencao_whatsapp.py`, `functions/outbox_aprovacao.py`, `functions/test_secretario_whatsapp.py`, PR #171). Implementa o atendimento autônomo seguro de contatos autorizados no WhatsApp em nome do André durante períodos de indisponibilidade (reuniões, foco). (1) Configuração e allowlist em `system/settings.whatsapp_secretario` com padrão desligado (`enabled: False`, `chats_allowlist: []`, `max_trocas: 2`, `janela_cancelamento_min: 10`). (2) Identidade explícita obrigatória: toda mensagem enviada pelo assistente é prefixada deterministicamente por `**Hermes Bot:** `. (3) Assimetria estrita de agenda: consulta eventos reais multi-calendário via Google Calendar (`hermes_calendar_tools`); se o André estiver ocupado, informa factualmente o compromisso/horário; se estiver livre, nunca confirma disponibilidade nem fecha compromisso — apenas informa que anotou o recado e repassará para confirmação pessoal posterior. (4) Proteção de dados sensíveis: recusa polidamente qualquer compartilhamento de dados financeiros ou de saúde do André e escala o contato para a fila de atenção (`atencao`). (5) Limite de insistência e escalação prioritária: ao atingir 2 trocas, o bot encerra a conversa e escala para a fila de atenção com prioridade alta (`secretario_insistencia`); tentativas de forçar decisões imediatas ou temas sensíveis também escalam imediatamente com prioridade alta (`secretario_decisao_forcada`, `secretario_assunto_sensivel`). O job periódico `avaliar_interrupcao_atencao` detecta o item e notifica o Telegram do André respeitando o orçamento diário e a janela de silêncio. (6) Veto humano garantido: envio via `outbox_aprovacao.criar_rascunho` com tipo promovido `secretario_whatsapp`, entrando em `aguardando_janela` e emitindo card com botão de cancelamento no Telegram (`[🛑 Cancelar]`); falha na emissão do card degrada automaticamente para aprovação manual (`aguardando_aprovacao`). (7) Gestão de estado na coleção `whatsapp_secretario_conversas/{chat_id}` mantendo histórico dos últimos turnos; mensagens enviadas pelo próprio André (`from_me: True`) marcam o estado como `assumido_por_andre`, impedindo interferência do bot.

- **2026-09-04** — Fechamentos de fundação técnica: outbox em `obter_estado_atual` e detectores determinísticos de finanças e saúde (`functions/outbox_aprovacao.py`, `tools/hermes_tools.py`, `functions/atencao.py`, `functions/main.py`, `functions/ai_notification_planner.py`, PR 4 da Fase 3). Fecha os dois gaps pontuais identificados na revisão do plano Master contra o código. (1) Transforma `obter_estado_atual` em porta de entrada única ao incluir a contagem `outbox_pendentes` (somando rascunhos de WhatsApp em `aguardando_aprovacao` e `aguardando_janela` via `contar_pendentes`), evitando chamadas extras a `listar_rascunhos_pendentes` em novas sessões. (2) Conecta finanças e saúde à fila unificada de atenção (`atencao`) via detectores puramente determinísticos sem chamadas a LLM e com silêncio por padrão, protegidos pelas flags `system/settings.atencao.financeiro.enabled` e `system/settings.atencao.saude.enabled` (ambas com padrão `False`, desligadas por padrão): `detectar_atencao_financeiro` varre contas fixas (`fixed_bills`) do mês corrente via `avaliar_contas_vencendo`, gerando pendências com `origem: "financeiro"` (`TIPO_CONTA_VENCENDO`) para contas não pagas a vencer em até 3 dias (prioridade média) ou vencidas (prioridade alta); `detectar_atencao_saude` avalia a rotina matinal de pesagem (`health_weights`) via `avaliar_rotinas_saude`, gerando pendências com `origem: "saude"` (`TIPO_ROTINA_SAUDE_AUSENTE`, prioridade média) se não houver pesagem registrada há 3 ou mais dias. O agendamento periódico de 30 em 30 minutos é centralizado exclusivamente em `detectar_atencao_acoes` (`atencao.py`), eliminando invocações redundantes em `check_and_send_reminders`. Atualiza a documentação do planner diário (`ai_notification_planner.py`) explicitando que os domínios financeiro e de saúde são cobertos deterministicamente pela fila de atenção.

- **2026-09-04** — Filtragem dinâmica de tools por `voiceEnabled` no cliente local de voz (`hermes-voice-client/orchestrator.py`, `hermes-voice-client/test_orchestrator.py`, PR 3 da Fase 3). Passa a respeitar o campo `_meta.voiceEnabled` retornado pelo servidor MCP ao construir as function declarations expostas ao Gemini (`_build_gemini_tools`), excluindo ferramentas volumosas, visuais, em lote ou de mutação sensível definidas em `_VOICE_EXCLUDED` (`functions/tools/registry.py`, como relatórios, formulários, imagens e investimentos). Adiciona suíte de testes unitários para o cliente de voz (`test_orchestrator.py`) com fixtures defensivas cobrindo cenários com e sem ferramentas habilitadas e backward compatibility para servidores sem metadados. Atualiza a documentação de limitações do cliente de voz registrando o canal fino e esclarecendo que aprovação/descarte de rascunhos de WhatsApp segue restrita à interação via Telegram.

- **2026-09-04** — Promoção gradual de autonomia com janela de cancelamento e sugestão semanal (`functions/outbox_aprovacao.py`, `functions/promocao_autonomia.py`, `functions/retro_agente.py`, `functions/main.py`, `tools/hermes_tools.py`, PR 2 da Fase 3). Implementa a promoção de autonomia para rascunhos de mensagens de WhatsApp com piso inegociável de supervisão humana através de janela de cancelamento (decisão do dono contra bypass total de aprovação). Rascunhos de tipos promovidos (configurados em `system/mcp_access.tipos_promovidos`) são criados com status `aguardando_janela` e campo `envio_liberado_em` (calculado a partir de `janela_cancelamento_min`, default 10 min), emitindo card explicativo no Telegram com botão único `🛑 Cancelar` (`outbox:{id}:no`). A função periódica `check_and_send_reminders` executa `liberar_rascunhos_promovidos` a cada minuto, liberando rascunhos cujo prazo expirou sem cancelamento via `aprovar_rascunho(aprovado_via="janela_automatica")`. Cria o módulo `promocao_autonomia.py` para descobrir tipos elegíveis através de `metricas_por_tipo` (amostra mínima de 8 rascunhos e taxa sem edição >= 90%), registrar sugestões na coleção `promocoes_autonomia_sugeridas/{tipo}` (com trava contra regravação de decisões `aceita`/`nunca`), e decidir promoções via transação Firestore (`decidir_promocao_autonomia`, status `aceita`/`adiada`/`nunca`), atualizando atomicamente `system/mcp_access.tipos_promovidos`. O job semanal `retro_semanal_agente` ganha hook determinístico que analisa métricas de tipos de rascunhos, registra até 2 sugestões e resume na notificação do Telegram. Expõe as tools MCP `consultar_promocoes_autonomia_sugeridas` e `decidir_promocao_autonomia` (esta exigindo confirmação prévia no Telegram).

- **2026-09-04** — Rastreio de tipo e edição no outbox de WhatsApp (`functions/outbox_aprovacao.py`, `tools/hermes_tools.py`, PR 1 da Fase 3). Introduz a fundação de dados para a promoção gradual de autonomia em mensagens WhatsApp (Eixo 3 da evolução Hermes→Jarvis). Adiciona o campo `tipo` (string opcional, default `"outro"`) na criação de rascunhos (`criar_rascunho`, repassado via tool `criar_rascunho_whatsapp` com schema atualizado incentivando rótulos estáveis como `confirmacao_reuniao`, `retorno_promessa`, `cobranca_terceiro`), e o campo `foi_editado` (booleano, default `False`, setado como `True` em `aplicar_edicao_rascunho` sempre que o conteúdo sofre alteração humana antes da aprovação). Implementa a função puramente analítica `metricas_por_tipo(db, tipo, limite=20)` para mensurar o volume de aprovações sem intervenção sobre os últimos `limite` rascunhos aprovados (`status` em `pending` ou `sent`, ordenados por `aprovado_em` decrescente, ignorando itens descartados, expirados ou pendentes de decisão), devolvendo `amostra`, `aprovados_sem_edicao` e a taxa relativa `taxa_sem_edicao` com proteção contra divisão por zero.

- **2026-09-04** — Suíte de regressão dos detectores proativos da fila de atenção (`functions/test_regressao_proatividade.py`, PR 5 da Fase 2). Introduz testes ponta a ponta que encadeiam o disparo determinístico de cada detector ao seu desfecho observável esperado na interface/notificações, garantindo que alterações futuras não silenciem o sistema ou quebrem comportamentos consolidados. Cobre 3 cenários canônicos: (1) Áudio recebido de contato vinculado a ação crítica (`avaliar_audio` com `degradation_count >= 3`) gerando item de prioridade alta e disparando interrupção de notificação via `avaliar_interrupcao_atencao` com payload correto; (2) Promessa em aberto sem retorno há 5 horas (`avaliar_promessas_vencidas`) gerando item de prioridade alta com texto relativo ("ha 5h") e convertendo-se em interrupção para o Telegram; (3) Etapa de ação com status `aguardando_terceiro` com data prevista vencida (`avaliar_etapas`) gerando item de prioridade média, validando sua presença na fila normal via `coletar_fila_atencao` e assegurando que `avaliar_interrupcao_atencao` retorne zero candidatos (dupla trava contra promoção indevida a interrupção ou desaparecimento na triagem de rotina). Reutiliza fixtures `MockDb`/`MockDoc`/`MockQuery` de `test_atencao.py` sem duplicar código nem estender o motor isolado de simulação de agentes (`simulation/`). Executado automaticamente pelo gate padrão de CI (`python -m unittest discover`). Fecha formalmente a Fase 2 ("Reflexos").

- **2026-09-04** — Retrospectiva semanal de execuções e auditoria MCP com proposta de ajuste de POP via evolução autônoma (`functions/retro_agente.py`, `mcp_server.py`, `main.py`, PR 4 da Fase 2). Introduz job agendado semanalmente aos domingos às 20:00 BRT (`retro_semanal_agente`, `@scheduler_fn.on_schedule`) que agrega dados de `agent_runs` (taxa de sucesso e mensagens de erro distintas) e `mcp_audit_log` (volumetria, latência média/máxima e taxa de erro) dos últimos 7 dias. Adiciona extensão aditiva em `mcp_server.py::_audit_log` para registrar `is_error: bool` em todas as chamadas de ferramentas MCP (passado nos 4 call sites existentes sem afetar leituras legadas). Implementa silêncio por padrão quando não há `agent_runs` no período. O texto-fonte enviado ao Gemini estruturado (`generate_content_logged`, tag `feature="retro_agente"`, modelo `GEMINI_STRUCTURED_MODEL`) contém exclusivamente estatísticas agregadas (sem expor argumentos ou payloads sensíveis de tools). Aplica regra inegociável de proposta nula (`null`) na ausência de padrão recorrente concreto (mínimo de 3 repetições de falha na mesma rotina ou ferramenta com taxa anormal). Grava o histórico em `retros_agente/{retro_YYYY_MM_DD}` e, se e somente se houver proposta concreta, registra a proposta na fila existente de `correcoes_pendentes`, delegando a validação por consenso web (Tavily) e consolidação em `conhecimento_mestre` ao Motor de Evolução Autônoma já em operação, sem aplicar modificações diretamente. Notifica o Telegram com mensagem resumida e informativa.

- **2026-09-04** — Proposta de reagendamento em lote de ações atrasadas na revisão semanal (`functions/revisao_semanal.py`, `hermes_core_logic.py`, `main.py`, PR 3 da Fase 2). Introduz job agendado semanalmente às segundas-feiras às 05:15 BRT (`revisar_semana_propor_reagendamento`, `@scheduler_fn.on_schedule`) para identificar ações atrasadas (`data_limite` ou `prazo_final` < hoje, com status `em andamento` ou `stand-by`) e propor redistribuição balanceada determinística via `tools/hermes_tools.py:preparar_reagendamento_em_lote` (sem chamadas a LLM, respeitando capacidade máxima por semana e pulando fins de semana). Implementa silêncio por padrão (não envia nem persiste se não houver tarefas atrasadas) e trava de proposta única em aberto (aborta silenciosamente se já existir documento em `reagendamentos_propostos` com `status: "pending"`). A proposta congelada é persistida em `reagendamentos_propostos/{id}` e uma notificação executiva é enviada ao Telegram com botões interativos inline (`[✅ Aplicar tudo]`, `[❌ Descartar]`). A aplicação de fato nunca ocorre no job agendado: sob o clique em `reagendamento_lote:{id}:aplicar`, o callback em `hermes_core_logic.py` executa `main.confirmarReagendamentoEmLote` em processo via `tools/callable_bridge.py:invoke_callable` com os itens exatamente como congelados na proposta, marcando `status: "aplicado"` e atualizando a mensagem Telegram com confirmação; o clique em `reagendamento_lote:{id}:descartar` marca `status: "descartado"` e remove os botões. Protegido contra duplo clique em ambos os fluxos.

- **2026-09-04** — Notificação event-driven para itens de alta prioridade com orçamento de interrupção unificado (`atencao.py`, `main.py`, PR 2 da Fase 2). Implementa varredura periódica a cada 1 minuto (`avaliar_interrupcao_atencao`, acoplada ao cron de envio determinístico em `check_and_send_reminders`) sobre a fila unificada de atenção (`atencao`). Itens com `estado: "aberto"` e `prioridade: "alta"` são avaliados quase em tempo real, sem chamadas a LLM (reaproveitando `titulo`, `resumo` e `sugestao` pré-sintetizados pelos detectores) e sem novos triggers Firestore. A decisão respeita o orçamento de interrupção compartilhado (`AI_PLANNER_MAX_DAILY_NOTIFICATIONS = 3`, transação Firestore via `_reserve_and_create_notification` em `system_usage/ai_planner_notifications/daily/{today}`) e a janela de silêncio (07:00–22:00, horário de Brasília). Itens fora da janela de silêncio são ignorados sem marcar `avaliado_interrupcao_em` (retry automático no minuto seguinte à abertura da janela); itens avaliados dentro da janela gravam `avaliado_interrupcao_em` no Firestore como deduplicação definitiva (reservados com sucesso ou rejeitados por teto diário esgotado). Mapeamento seguro de categoria de notificação (`"acao"` → `"acoes"`, demais origens como `"whatsapp"`, `"financeiro"` ou `"saude"` → `"geral"`). As notificações são criadas em `scheduled_notifications` com `source: "atencao_interrupcao"` e despachadas imediatamente ao Telegram com botões interativos de feedback (👍/👎) pelo `dispatch_pending_ai_notifications`.

- **2026-09-04** — Modelo de interação auto-mantido por contato (`perfil_pessoas.modelo_interacao`, `main.py`, `hermes_tools.py`, PR 1 da Fase 2). Implementa síntese periódica de perfil comportamental por pessoa via job agendado diário às 5h30 (`atualizar_modelos_pessoas`, `@scheduler_fn.on_schedule`), antes do briefing executivo das 6h45. O job itera contatos com `whatsapp_chat_id` e aplica filtro de sinal (pula se < 6 mensagens nos últimos 90 dias E 0 interações em `interacoes_pessoas`), montando texto-fonte com as últimas ~40 mensagens cronológicas (rotuladas André vs Contato) e até 10 interações vinculadas a ações. Deduplicação via hash MD5 (`last_processed_modelo_hash`). Invoca Gemini estruturado com telemetria de custo (`generate_content_logged`, tag `feature="modelo_pessoa"` e modelo `GEMINI_STRUCTURED_MODEL`), com prompt estrito e regra inegociável contra alucinação (`registro` e `tempo_resposta_tipico` devem ser estritamente `null` se ausente sinal claro). Parser defensivo com tolerância a fences markdown e normalização de variantes nulas. Falhas individuais no loop são isoladas por `try/except`. A tool MCP `buscar_contato` passa a incluir `modelo_interacao` em cada candidato quando presente.

- **2026-09-04** — Contexto de agente auto-mantido em ações críticas (`functions/main.py`, `hermes_tools.py`, PR 5 da Fase 1). Introduz trigger `@firestore_fn.on_document_written` em `tarefas/{taskId}` (`on_tarefa_written_contexto_agente`) para manter automaticamente o campo `contexto_agente` nas ações críticas (`execution_lane == "critica"` ou `degradation_count >= 3`), usando o Gemini estruturado com telemetria de custo (`generate_content_logged`, tag `feature="contexto_agente"`). O texto fonte sintetiza título, descrição, etapas do plano de ação e as últimas 20 entradas do diário de bordo, deduplicando execuções por hash MD5 (`last_processed_contexto_hash`). O resumo estruturado em JSON padroniza: `resumo`, `pessoas_chave`, `onde_esta_o_codigo` (com regra inegociável de nunca inventar repositório/branch/porta se ausentes no texto-fonte, resultando em `null`), `ultimas_decisoes`, `travas` e timestamp ISO `atualizado_em`. A tool MCP `obter_acao` passa a expor `contexto_agente` quando presente (e `None` para ações não críticas ou não processadas), viabilizando sessões agendadas ou transferências de contexto sem handoff manual.

- **2026-09-04** — Webhook do GitHub para anotação automática no diário da ação (`githubWebhook`, `main.py`, PR 4 da Fase 1). Introduz receptor HTTP com verificação HMAC-SHA256 (`X-Hub-Signature-256` via `system/api_keys.github_webhook_secret`), deduplicação de entregas via `core/idempotency.py` (`X-GitHub-Delivery`) e resposta direta a eventos `ping`. Normalização pura (`normalizar_evento_github`) que filtra e formata os dois eventos de interesse: pull requests mergeadas (`action: "closed"`, `merged: true`) e pushes no branch default (`ref == refs/heads/{default_branch}`) com commits. A função localiza todas as tarefas em `tarefas` vinculadas ao repositório via `github_repos_vinculados` (`array_contains`) e inclui a entrada diretamente em `acompanhamento` via `ArrayUnion`, com isolamento de falhas por tarefa.

- **2026-09-04** — Aprovação de rascunhos de WhatsApp respondendo no chat próprio (`atencao_whatsapp.py`, PR 3 da Fase 1). Permite que o dono aprove, descarte ou edite rascunhos do outbox respondendo diretamente em seu próprio WhatsApp ("mensagem para si mesmo"), sem precisar alternar para o Telegram. Desenho desacoplado e robusto baseado em contagem de pendentes e no setting de sistema `whatsapp_owner_chat_id` em `system/settings`: com exatamente 1 rascunho pendente, comandos afirmativos ("sim", "ok", "pode mandar") aprovam (`aprovado_via: "whatsapp"`), comandos negativos ("não", "descarta", "cancela") descartam, e qualquer outro texto atualiza o conteúdo do rascunho com edição. Mensagens em chats de terceiros ou sem `from_me` são descartadas em fast-path sem leituras adicionais; 2+ rascunhos são marcados como ambíguos. Delega execução às funções atômicas de `outbox_aprovacao.py` e atualiza o card do Telegram indicando a aprovação via WhatsApp.

- **2026-09-04** — Observabilidade das sessões agendadas (`agent_runs.py`, PR 2 da Fase 1). Introduz a coleção `agent_runs` para registro do desfecho e contadores de cada execução de rotina agendada do Claude (briefing matinal, varredura de follow-ups, executor de agent_requests). Módulo com separação entre lógica pura (`montar_registro`, com validação estrita de rotina, resumo, status e obrigatoriedade de erro condicional) e I/O Firestore (`registrar`, `listar_recentes`). Duas novas tools MCP adicionadas ao catálogo: `registrar_execucao_agente` (escrita sem confirmação prévia, categoria de telemetria interna) e `consultar_execucoes_agente` (leitura ordenada por data decrescente).

- **2026-09-03** — Fila de trabalho autônomo e consolidação de áudio (`agent_requests.py`, PR 1 da Fase 1). Introduz a coleção `agent_requests` para trabalho do nível "Autônomo" (sem efeito em terceiros), consumido por sessões agendadas do Claude. O detector `audio_relevante` (`atencao_whatsapp.py`) ganha hook determinístico que enfileira/atualiza pedido do tipo `consolidar_audio` com ID determinístico (`consolidar_audio:{item_atencao_id}`), agrupando sequências de áudios dentro da janela de 10 min. Duas novas tools MCP adicionadas ao catálogo: `consultar_pedidos_agente` (leitura dos pedidos pendentes ordenados por antiguidade) e `concluir_pedido_agente` (escrita sem confirmação prévia, idempotente contra toque duplo, aceitando exatamente um entre `resultado` e `erro`). `obter_estado_atual` inclui contagem de `agent_requests_pendentes`.

- **2026-09-03** — Rascunhos de mensagens WhatsApp com aprovação em um toque no Telegram (`outbox_aprovacao.py`, PR 4). Separa o ato de redigir (pelo agente ou copiloto) de aprovar e enviar (pelo dono no Telegram). Criação via tool MCP `criar_rascunho_whatsapp` grava em `whatsapp_outbox` com `status: "aguardando_aprovacao"`, associando opcionalmente a ação (`acao_id`) e a item da fila de atenção (`item_atencao_id`), e dispara card com botões inline no Telegram (`[✅ Enviar]`, `[✏️ Editar]`, `[🗑️ Descartar]`). O botão "✅ Enviar" faz transição atômica via transação Firestore de `aguardando_aprovacao` para `pending`, protegida contra duplo clique, resolve o item de atenção e anota no diário da ação. O botão "🗑️ Descartar" marca o rascunho como `descartado` e reabre o item da fila de atenção se vinculado. O botão "✏️ Editar" trava a sessão no Telegram para capturar a próxima mensagem como novo texto, emitindo um novo card. Expiração automática após 48h sem resposta (`expirar_rascunhos_pendentes`, executado horariamente pelo cron `check_and_send_reminders`). Nova tool de leitura `listar_rascunhos_pendentes` e atualização de `consultar_envio_whatsapp` para descrever os novos status.

- **2026-09-03** — Fila de atenção unificada (`atencao`), primeiro detector determinístico (`aguardando_terceiro_vencido`) e tools MCP (`obter_fila_atencao`, `resolver_item_atencao`). Centraliza os sinais que pedem atenção do dono em formato único e estável, com deduplicação determinística (`tipo:acao_id:etapa_id`) e sem chamadas a LLM na detecção. O detector (`functions/atencao.py:detectar_atencao_acoes`, scheduler a cada 30 min, atrás de `system/settings.atencao.aguardando_terceiro.enabled`) identifica etapas de ações ativas/stand-by em `aguardando_terceiro` com `data_prevista` vencida, suprimindo o alerta se o terceiro tiver enviado mensagem no WhatsApp após o prazo previsto. A tool `obter_fila_atencao` expõe listagem ordenada por prioridade (alta→baixa) e prazo, `resolver_item_atencao` (dupla confirmação) registra o desfecho e anota no diário da ação vinculada, e `obter_estado_atual` passa a incluir o top 10 na chave `fila_atencao`.

- **2026-09-03** — Atualização do `last_activity_ts` em `whatsapp_chats/{chatId}` na captura de mensagens ao vivo (`services/whatsapp-capture/index.js`). Antes, esse campo só era atualizado no registro periódico a cada 6 horas (`client.getChats()`), fazendo com que `listar_conversas_whatsapp` e as triagens ordenassem com defasagem de até 6h (conversas com novas mensagens às 14h apareciam com atividade de 23h da véspera). A gravação de cada mensagem nova agora atualiza o documento do chat com o timestamp da mensagem, utilizando um cache em memória (`chatLastActivityMs`) para avançar o carimbo sem regredir durante backfills de histórico e sem sobrecarregar o Firestore com leituras redundantes.

- **2026-08-31** — Corrigida a criação repetida de eventos de ações no Google Calendar. A Cloud Function já usava ID determinístico (`hermes<uuid5-da-acao>`), `extendedProperties.private.hermes_task_id` e o calendário dedicado **Ações Hermes**, mas o processo local `hermes_cli.py watch` era iniciado junto com o sistema e consumia o mesmo `system/sync`; ele tentava atualizar o ID do evento dedicado em `primary` (**Compromissos**), recebia 404, criava um evento de ID aleatório em `primary` e trocava `tarefas.google_calendar_id`. A execução seguinte da Cloud Function restaurava o ID correto, fazendo cada novo ciclo local criar outra cópia. O watcher local foi retirado de `start.bat`/`start_hidden.vbs` e encerrado na máquina; seu push de Calendar também passou a usar o calendário configurado, o mesmo ID determinístico, a marca `hermes_task_id` e conflito 409 como sucesso idempotente. Testes novos cobrem os contratos. Na reparação dos dados, foram removidas seis cópias inequivocamente correspondentes de **Compromissos** (três da conversa com Alexandre Fiorotti e três da lavanderia); os dois eventos canônicos em **Ações Hermes** foram preservados.

- **2026-08-24** — Nova visualização das ações em **gráfico de Gantt** (`src/views/GanttView.tsx`), terceiro modo de `dashboardViewMode` ao lado de lista e calendário. Cada linha é uma ação e a barra cobre o intervalo entre a **data de execução** (`data_limite`, o início) e a **data final** (`prazo_final`, o término) — os dois campos já existiam no schema, mas `prazo_final` só aparecia como texto no card e nunca havia sido desenhado como duração; ação sem prazo final vira um marco de um dia (losango), não uma barra falsa de largura arbitrária. Cor por situação (concluída/atrasada/stand-by/em andamento), linha vertical do dia atual, zoom por dia/semana/mês, filtros de "incluir concluídas" e "só com prazo final", e clique abre a ação no mesmo modal de execução usado pela lista. Toda a aritmética de datas e o posicionamento das barras ficam fora do componente, em `src/utils/ganttLayout.ts` (strings `YYYY-MM-DD` em horário local, como o resto do app), o que deixou a regra testável sem DOM: `src/utils/ganttLayout.test.ts`, 20 casos, incluindo prazo anterior à execução (dado inconsistente vira intervalo real em vez de barra negativa) e janela que sempre inclui hoje. O teste entrou no script `npm test`.

- **2026-08-24** — Corrigido o `400 INVALID_ARGUMENT` que o copiloto do Telegram devolvia (`⚠️ Erro ao processar`) em toda mensagem de texto livre que chegava ao Gemini — inclusive o pedido de criar uma ação, que é o caminho com ferramentas. A API não diz qual campo recusou, então as três frentes plausíveis foram fechadas em `hermes_core_logic.py`: (1) **thinking da família errada** — o Telegram ainda enviava `thinking_budget=0` (campo da família 2.x) depois de `GEMINI_BALANCED_MODEL` virar `gemini-3.5-flash-lite`, enquanto o copiloto web já usava `thinking_level="MINIMAL"`; agora `_thinking_config_for_model` escolhe pela família do modelo; (2) **schemas de ferramenta** deixaram de ser derivados cegamente da assinatura Python por `_build_gemini_tools` — sai o campo `default`, parâmetro `dict` deixa de virar OBJECT sem `properties` (vira texto JSON, e a ferramenta lê os dois formatos), e `required` passa a refletir a assinatura real, já que o SDK marcava como obrigatório todo parâmetro com padrão `None` e obrigava o modelo a inventar horário/prazo/tags; (3) `editar_acoes_em_lote` ganhou schema explícito com os campos aceitos. Em caso de 400 o turno degrada em etapas (sem thinking, depois sem histórico) e registra no log qual delas passou, em vez de morrer na primeira tentativa. Junto disso, corrigido o commit anterior, que havia deixado `functions/main.py` **sem compilar** (indentação de `protocolo_edicao_em_lote` e um elemento solto no dict de ferramentas — nenhum deploy passaria) e a edição em lote sem ligação no copiloto web: a ferramenta passa a ser declarada e o protocolo entra na instrução de sistema. Novo `functions/test_tool_schemas.py` (10 casos) valida o payload antes do envio, sem rede.

- **2026-08-22** — Correcoes no Resumo Matinal depois do teste com dado real. (1) **Bug de persistencia**: `_persistir` gravava com `merge=True`, que e um merge recursivo — quando `consolidacoes_whatsapp` saiu de `_coletar_filas`, a chave continuou viva no documento e a tela seguiu mostrando a fila removida. Trocado por `merge=[<campos do payload>]`, que substitui cada campo por inteiro e preserva `visto_em` (e a futura `aderencia`). Exatamente o mesmo erro ja corrigido em `scripts/seed_dados_cadastrais.py` na entrada de 2026-08-19. (2) **Segunda camada do mesmo bug, no frontend**: `MorningSummaryView` renderizava qualquer chave de `filas` que viesse no documento, usando o nome cru da chave quando nao havia rotulo — ou seja, um campo residual (ou uma fila nova sem rotulo) vazava para a UI. `FILA_LABEL` passou a ser a allowlist: chave desconhecida nao e renderizada. (3) Quadro "Proximos 7 dias" renomeado para "Volume de acoes nos proximos 7 dias" (o titulo antigo nao dizia o que o grafico media) e movido para o topo da coluna da direita, para a forma da semana vir antes do detalhe. (4) Documentado em `_coletar_contas` que olhar so o mes corrente e decisao, nao descuido: `fixed_bills` tem dezenas de docs de meses antigos com `isPaid: false` que sao registros nunca fechados do inicio do uso do sistema, nao divida real (confirmado pelo usuario). Testes: 37 no backend, 13 no componente — incluindo regressao para o `merge` e para a allowlist de filas.

- **2026-08-22** — Três correções no Resumo Matinal, todas vindas de testar a tela com dado real. (1) `whatsapp_consolidacoes` sem `task_id` saiu da fila "esperando decisão": consolidar uma conversa é muitas vezes um fim em si (ler o que foi dito), e as duas saídas possíveis — anexar a uma ação ou deixar como está — são ambas estados finais legítimos; uma consolidação parada é um arquivo, não uma pendência. (2) O pilar `saude` de `estrategia_pessoal` deixou de ser cobrado por vínculo de ação (`ResumoMeta.gerida_por_acoes`): saúde é executada pelos registros do módulo Saúde, não por `tarefas`, e medir movimento por ação vinculada fazia a tela afirmar "parada há 56 dias" num dia em que o usuário havia se pesado de manhã — o movimento dessas metas agora vem de `saude.ultimo_registro` (pesagem, cintura, check-in, log diário) e elas saíram do denominador de "X de N metas recebem trabalho hoje" (`total_geridas_por_acoes`). Continua em aberto o problema estrutural que isso expôs: `metricaAlvo.valorAtual` nunca é sincronizado com a fonte real do dado. (3) As rotinas de saúde do dia ganharam marcador de cumprimento, mas só as verificáveis (`_rotina_verificavel`): pesagem (`health_weights`), cintura (`health_waist`) e os dois check-ins (`health_exercise_logs.pain.morning`/`.evening`). "Almoço com calma" e "janela alimentar" são avisos para acompanhar o dia, não itens de checklist — vêm com `feito: null` e não ganham marcador nenhum. Testes: 35 no backend, 11 no componente. Criado também `scripts/preview_morning_summary.py` (read-only por padrão, `--gravar` persiste), no mesmo papel de `scripts/preview_weekly_report.py`.

- **2026-08-22** — Resumo Matinal (Camada 1): nova primeira tela do dia em `viewMode 'home'` (`src/views/MorningSummaryView.tsx`), alimentada por `functions/morning_summary.py` — um coletor **prospectivo e determinístico**, sem nenhuma chamada de LLM, par simétrico de `_collect_diary_material` (`personal_diary.py`): o diário das 21h30 fecha o dia, este abre às 04h30. A conta é toda em Python e o frontend só desenha, mesma regra de arquitetura de `health_weekly_report.py`. O que motivou o desenho: `daily_reset_job.py` arrasta toda ação atrasada para hoje às 00:00 e marca `auto_data_atualizada`/`degradation_count`, mas esses dois campos só existiam como badge no card (`UIComponents.tsx`) e cor da borda (`TaskCard.tsx`) — nunca eram agregados, então a diferença entre "escolhi fazer hoje" e "o sistema empurrou para hoje" se perdia todas as manhãs, inclusive no briefing das 5h (`daily_morning_briefing.py`), que lista as duas coisas no mesmo bullet. O resumo mostra também as filas de decisão que só existem dentro de outra tela (`email_action_suggestions` pendentes, `whatsapp_consolidacoes` sem `task_id`, `contact_merge_requests`, `fixed_bills` vencendo, `scheduled_notifications` na fila) e atravessa o elo curto↔longo prazo que já existia e nunca era percorrido num lugar só (`tarefas.estrategia_objetivo_id` → `estrategia_pessoal`): quais metas o dia serve e quais estão paradas há quantos dias. A escolha dos até 3 focos é regra explícita em Python (`_escolher_foco`, precedência prazo duro → degradação ≥3 → SLA estourado → meta parada → hora marcada → fila), nunca modelo — e é testada (`test_morning_summary.py`, 28 casos; `MorningSummaryView.test.tsx`, 10 casos). Nova coleção `resumo_matinal/{data}`, nova callable `gerarResumoMatinal` para regeneração sob demanda. Junto disso, `DEFAULT_HEALTH_REMINDERS` foi extraído de dentro de `check_and_send_reminders` para `functions/health_routines.py` (fonte única no lado Python, agora compartilhada com o resumo) — sem isso a tela mostraria uma rotina de saúde incompleta, já que as rotinas padrão vivem em código e não em `health_telegram_reminders`. `'home'` — que existia declarado em `index.tsx` desde sempre e nunca havia sido usado — passou a ser a tela de abertura do app e a raiz de navegação (antes era `'dashboard'`, que segue a um clique na sidebar); os dois handlers de histórico (`pushState`/`popstate`) foram ajustados junto, senão o gesto de voltar duas vezes para sair do app só funcionaria a partir do Dashboard. O briefing por Telegram das 5h (`daily_morning_briefing.py`) segue no ar sem alteração — substituí-lo é assunto da camada de narrativa, ainda não feita.

- **2026-08-19** — Correções de review (Codex, PR #127) nos dados cadastrais: (1) o objeto completo (~11k caracteres já com o perfil atual) estourava o teto de truncamento de resultado de ferramenta em ambos os loops de tool-calling (8k no Godmode, 12k no copiloto) — `consultar_dados_cadastrais` ganhou um parâmetro `secao` opcional (`dados_cadastrais.py`, `main.py`, `godmode.py`): sem ele lista as seções disponíveis, com ele devolve o conteúdo completo de uma seção (a maior tem ~2.3k caracteres); (2) o gate por palavras-chave em `main.py` só cobria documentos/identificadores, não as demais seções que a própria ferramenta promete (endereço, telefone, família, formação, carreira) — perguntas como "qual é meu endereço?" nunca ativavam a ferramenta; gate ampliado para cobrir todas as seções; (3) `scripts/seed_dados_cadastrais.py` gravava com `merge=True`, um merge recursivo — um campo removido do payload (ex.: conta bancária encerrada) sobrevivia como resíduo obsoleto em vez de desaparecer; trocado por `merge=[<field paths>]`, que substitui `dados_cadastrais` por inteiro preservando os demais campos do documento (`ai_profile`).

- **2026-08-19** — Dados cadastrais pessoais completos (documentos, contato, família, formação, carreira, dados bancários, plano de saúde) disponíveis sob demanda ao copiloto e ao Godmode, gravados em `usuarios/{uid}.dados_cadastrais` (`functions/dados_cadastrais.py`). Diferente de `ai_profile`, esse campo não entra na persona estática — só é lido quando a ferramenta nova `consultar_dados_cadastrais` é chamada (em `main.py`, atrás de um gate por palavras-chave como as demais ferramentas de fluxo raro; em `godmode.py`, sempre declarada, seguindo o padrão já usado ali), evitando que dado sensível trafegue para a API do Gemini/Claude em toda mensagem. Sem ferramenta de escrita para a IA — gravado via `scripts/seed_dados_cadastrais.py` (novo), que lê de um JSON local nunca versionado (`scripts/dados_cadastrais_seed.json`, adicionado ao `.gitignore`) e escreve via Admin SDK, mesmo padrão dos demais scripts administrativos do repositório.
  Corrigido, junto disso, o mesmo gap de autorização já corrigido no Godmode (ver entrada anterior): `askCopilotoHermes` só exigia `req.auth` (qualquer conta Firebase autenticada) em vez do dono verificado exigido por `firestore.rules` para essas coleções — como o callable roda com Admin SDK (ignora as regras) e agora pode retornar dados cadastrais sensíveis, passou a chamar `_require_internal_user` também, mesma checagem usada em `getAutomationSettings`/`updateAutomationSettings` e (desde a PR #126) em `askHermesGodmode`.

- **2026-08-19** — Correções de review (Codex, PR #126) na expansão do Godmode: `askHermesGodmode` só exigia `req.auth.uid` (qualquer conta Firebase autenticada), enquanto o acesso direto a essas coleções via `firestore.rules` é restrito ao dono verificado (`internalUser()`) — como o Godmode roda com o Admin SDK (ignora as regras) e agora expõe dados globais (finanças, saúde, diário, agenda, pessoas, WhatsApp), passou a chamar `_require_internal_user` (`main.py`), a mesma checagem já usada por `getAutomationSettings`/`updateAutomationSettings`. E `consultar_relatorio_semanal_saude`, sem semana especificada, caminhava só um domingo para trás em busca do relatório mais recente — um scheduler parado por mais de uma semana faria a ferramenta devolver "não encontrado" à toa; agora ordena `health_weekly_reports` pelo próprio ID do documento (formato `YYYY-Www`, que ordena corretamente por comparação lexicográfica) e pega o mais recente existente, sem depender de caminhar semana a semana.

- **2026-08-19** — Godmode ganha o panorama completo do usuário (era só tarefas/metas/conhecimento — finanças e saúde estavam explicitamente fora do escopo desde a origem do módulo). Extraídos dois módulos compartilhados com o Copiloto padrão para eliminar duplicação: `strategy_tools.py` (CRUD de `estrategia_pessoal` — antes um closure de ~280 linhas dentro de `askCopilotoHermes`) e `health_tools.py` (resumo de saúde — antes um closure de ~85 linhas no mesmo lugar); `main.py` agora chama esses módulos em vez de reimplementar a lógica. `godmode.py` ganhou 12 ferramentas novas: leitura de finanças (`consultar_financas`, via `tools/telegram_extended.py`), saúde diária e relatório semanal (`consultar_saude`, `consultar_relatorio_semanal_saude`), diário pessoal (`consultar_diario_pessoal` — a fonte mais densa, já que cada diário cruza ações/saúde/finanças/agenda/conversas/pessoas do dia), agenda (`consultar_agenda`), pessoas (`buscar_contato`, `consultar_interacoes_pessoa`), WhatsApp (`buscar_conversas_whatsapp`), e escrita no módulo Estratégia (`criar_objetivo_estrategico`, `editar_objetivo_estrategico`, `gerenciar_item_estrategico`, `excluir_objetivo_estrategico` — antes só leitura). Persona atualizada para refletir o escopo novo e exigir confirmação explícita do usuário antes de qualquer ferramenta de escrita, com destaque para a exclusão (irreversível). Corrigido também um drift entre o que a documentação afirmava e o que o código fazia: `_get_user_profile_text` do Godmode lia só nome/cargo/setor/email, ignorando `ai_profile.personalidade` (o perfil destilado semanalmente do diário pessoal) mesmo com o schema documentando havia semanas que as três superfícies (copiloto web, Godmode, voice bridge) liam esse campo — agora delega para `_format_ai_profile_for_prompt` (`main.py`), a mesma função usada pelas outras duas. Fechado o ciclo nos dois sentidos: `personal_diary.py:_collect_diary_material` passou a agregar também `sessoes_godmode` (antes só `sessoes_copiloto`) — conversas com o Godmode agora alimentam o diário do dia e, por consequência, o consolidador semanal de personalidade.

- **2026-08-08** — Duas correções pós-merge no vínculo sinal↔ação e no copiloto: (1) `criar_acao_no_sistema` (`main.py` e a cópia usada pelo Telegram em `hermes_core_logic.py`) gerava um UUID novo a cada chamada sem nenhuma checagem de duplicidade — quando o modelo chamava a tool mais de uma vez para o mesmo pedido (lote de function calls repetido, retry), cada chamada criava uma tarefa nova, e como o evento de agenda é derivado deterministicamente do ID da tarefa (`build_task_calendar_event_id`), tarefas duplicadas sempre geravam eventos duplicados na agenda no mesmo horário; agora busca por uma tarefa não excluída com mesmo título+data+horário criada nos últimos 15 minutos antes de criar outra. (2) Mensagem do produtor Calendar (`_build_suggestion_message`, `email_action_linker.py`) reformulada: como `tarefas.google_calendar_id` só é preenchido pelo próprio Hermes ao criar o evento a partir do horário da ação (`link_calendar_events_to_actions` nunca corresponde a um evento criado externamente), a correspondência é sempre 100% certa — a mensagem não finge mais uma "relação detectada" ("📅 Reunião relacionado(a) a uma ação"), passa a avisar diretamente que o compromisso já agendado terminou ("📅 A reunião da ação abaixo terminou"). A confirmação continua sendo pedida — o ponto nunca foi confirmar a correspondência (que é certa), é decidir se registra a nota no diário e/ou reativa a ação.
- **2026-08-08** — Correções de review (Codex) no painel de ativação e no pipeline de canais: `getAutomationSettings`/`updateAutomationSettings` (`main.py`) agora exigem o mesmo dono verificado que `internalUser()` em `firestore.rules` (e-mail + `email_verified`), não só qualquer usuário autenticado — essas callables são a única porta de entrada para `system/settings`, que é bloqueado por regra para o cliente; `link_calendar_events_to_actions` (`email_action_linker.py`) parou de comparar `google_calendar_events.data_fim` (gravado com o offset local do evento, nunca normalizado) como string contra limites em UTC — a pré-filtragem por string agora só existe para manter a query barata (folga generosa), e quem decide de fato é uma comparação de datetimes normalizados para UTC; `triage_whatsapp_messages` (`whatsapp_ingest.py`) parou de avançar o cursor de `whatsapp_messages` antes de aplicar o teto de janelas por passada, o que fazia as conversas adiadas por excesso de volume sumirem para sempre da consulta seguinte — agora o cursor só avança até a mensagem mais antiga entre as adiadas (ou até o fim do lote, se nada foi adiado); e `dispatch_scheduled_whatsapp_messages` (`ai_notification_planner.py`) passou a exigir `ready: true` no heartbeat do worker local (não só `last_seen` recente) antes de suprimir o fallback por Telegram — um heartbeat recente só prova que o processo está de pé, não que o client whatsapp-web.js está autenticado.
- **2026-08-08** — Painel de ativação das automações multi-canal na interface: nova aba "Automações" em `SettingsModal` (`src/components/modals/Modals.tsx`) com toggles para `email_action_linker`, `personal_diary`, `whatsapp_ingest` (+ lista de conversas capturadas e indicador de status do worker local) e `whatsapp_auto_send_enabled`. Como `system/settings` é bloqueado por regra de segurança para o cliente, criadas as callables `getAutomationSettings`/`updateAutomationSettings` (`main.py`) como única porta de entrada — só tocam esse subconjunto explícito de campos, o resto de `system/settings` continua editável apenas via Console/Admin SDK.

- **2026-08-08** — Removida `propostas/automacoes-canais-e-diario-pessoal.md`: os três eixos nela descritos foram executados (ver entrada abaixo). Mesma política de `propostas/vinculo-email-acao.md` (2026-08-08) — plano executado não é conhecimento de referência.
- **2026-08-08** — Executados os três eixos de automações por canal e diário pessoal: motor de sugestão sinal↔ação generalizado para múltiplos canais (`email_action_linker.py`, campo `canal`) com novos produtores determinísticos SIPAC (`try_link_sipac_notification`) e Calendar (`link_calendar_events_to_actions`) e opcional Monitor de Páginas (`paginas_monitoradas.task_id`); pipeline completo de WhatsApp — worker de captura corrigido (ID idempotente, identidade de chat, `message_create`, allowlist, resiliência, coordenação de envio), triagem por IA com vínculo a ações (`whatsapp_ingest.py`) e digests vetorizados com tool de busca (`buscar_conversas_whatsapp`); diário pessoal diário em primeira pessoa (`personal_diary.py`, `gerar_diario_pessoal`) com edição via Telegram e consolidador semanal de personalidade (`consolidar_personalidade`) propagado a `usuarios/{uid}.ai_profile.personalidade`, lido por copiloto web/Godmode/voice bridge. `source` passou a ser gravado em `sessoes_copiloto/mensagens` também fora do Telegram (web/drawer/voz). Painéis leves no Dashboard para sugestões multicanal e leitura do diário. Todas as features atrás de flags em `system/settings` (padrão desligado).
- **2026-08-08** — Adicionada a proposta `propostas/automacoes-canais-e-diario-pessoal.md`: viabilidade e plano em três eixos — generalização do padrão sugestão-confirmação para SIPAC/Calendar/WhatsApp/Monitor de Páginas, ingestão de WhatsApp como conhecimento, e diário pessoal diário com consolidação de personalidade. Registra os achados da investigação: `whatsapp_messages` é write-only (nenhum leitor no sistema; o "Assistente WhatsApp" de `integracoes/whatsapp.md` nunca foi construído), o worker de captura tem ID não-idempotente e não grava identidade de chat/grupo, `whatsapp_outbox` tem dois despachantes em corrida, e `ai_profile.historico_deduzido` é um log das últimas 10 perguntas — não um perfil aprendido — porém lido por copiloto web, Godmode e voice bridge.

- **2026-08-08** — Correções de review (Codex) no vínculo e-mail↔ação: removida a auto-aplicação de sugestões (a classificação vem de conteúdo controlado pelo remetente do e-mail — um e-mail malicioso poderia tentar instruir o modelo a escolher uma ação e reportar confiança alta — então toda sugestão `related` sempre exige confirmação humana, independente da confiança relatada); `apply_suggestion` (`email_action_linker.py`) passou a gravar a nota no diário e marcar a sugestão como aplicada numa única transação Firestore (evita duplicar a nota se uma etapa falhar a meio, e recusa aplicar a mesma sugestão duas vezes numa corrida) — mesma correção replicada em `EmailLinkSuggestionsPanel.decide` (`DashboardView.tsx`) via `runTransaction`; e `link_emails_to_actions` passou a paginar os resultados do Gmail (`nextPageToken`, teto de 5 páginas por passada) em vez de olhar só a primeira página, para não deixar e-mails além da posição 20 sem nunca serem analisados numa caixa cheia.
- **2026-08-08** — Removida `propostas/vinculo-email-acao.md`: era o plano de implementação do vínculo e-mail↔ação, e as três fases nele descritas já foram executadas (ver as entradas abaixo). Mesma lógica do handoff removido em 2026-06-17 — plano executado não é conhecimento de referência, o código é a fonte de verdade.
- **2026-08-08** — Fase 3 do vínculo e-mail↔ação: aposentado o recurso `email_trigger` (gatilho de e-mail manual por ação), que nunca teve o job que o processava ligado ao sync (`process_email_triggers` era código morto sem callsite). Removidos `functions/email_trigger_handler.py`, o campo `Tarefa.email_trigger`/tipo `EmailTrigger` e toda a UI de configuração em `TaskExecutionView.tsx`/`Modals.tsx`. No lugar, novo campo `Tarefa.email_link_optout` (toggle simples "silenciar vínculo de e-mail" nesses mesmos componentes) para quem quiser desativar o `email_action_linker` por ação.
- **2026-08-08** — Fase 2 do vínculo e-mail↔ação: novo tipo de entrada rica `EMAIL` no diário de bordo (`src/utils/diaryEntries.ts` + chip em `DiarioBordoUI.tsx`) — mesmo envelope `TIPO::JSON::{...}` usado por `LINK`/`CONTACT`/`FILE`, agora também escrito pelo backend; e painel `EmailLinkSuggestionsPanel` no `DashboardView.tsx` listando sugestões `pending`/`expired` para decidir pelo navegador o que não foi respondido a tempo no Telegram. (A auto-aplicação por confiança, cogitada nesta fase, foi removida no dia seguinte por review de segurança — ver entrada acima.)
- **2026-08-08** — Fase 1 do vínculo e-mail↔ação: novo `functions/email_action_linker.py` (`link_emails_to_actions`, chamado no fim de `run_full_sync`) analisa e-mails recentes via IA (`gemini-3.5-flash-lite`, com `generate_content_logged`), casa com ações `tarefas` em andamento/stand-by e propõe vínculo via Telegram (`emlink:{msgId}:{ok|on|no}`, tratado em `_handle_telegram_callback`). Nova coleção `email_action_suggestions` (dedupe por ID de doc = ID da mensagem do Gmail). Feature flag `system/settings.email_action_linker.enabled` (padrão desligado). Achados que motivaram o desenho: não havia classificador genérico de e-mails no sistema; o ledger `system/processed_emails` tinha truncamento inconsistente entre os dois caminhos de sync existentes (Pix e boletos) — por isso a nova coleção usa dedupe por ID de documento em vez de reaproveitar aquele ledger.
- **2026-08-01** — Correções de review (Codex) na PR do planejador de IA: teto diário de `scheduled_notifications` agora é reservado atomicamente numa transação Firestore (`system_usage/ai_planner_notifications/daily/{data}`) em vez de um contador em memória, que não era seguro sob tool calls concorrentes; `consultar_tarefas_ativas` passou a filtrar `status` no próprio Firestore antes do `limit`; `HealthView` só limpa o formulário de exame depois que `onAddExam` resolve com sucesso.
- **2026-08-01** — Adicionado o planejador proativo de notificações por IA (`functions/ai_notification_planner.py`): agente diário sobre Claude que cruza tarefas ativas e metas estratégicas e propõe até 3 notificações/dia via nova coleção `scheduled_notifications`, despachadas pelo `check_and_send_reminders` existente com botões de feedback no Telegram. Documentada também a coleção `estrategia_pessoal`, que já existia mas não estava no schema.
- **2026-06-17** — Migração inicial: documentação operacional solta na raiz do repo (`HABILITAR_BILLING.md`, `QUICKSTART.md`, `SYNC_BADGES_GUIDE.md`, `WHATSAPP_INTEGRATION.md`, `gemini_models_guide.md`, `SOLUCAO_SIMPLIFICADA.md`, `PLAN_MOBILE_BUTTONS.md`, `HANDOFF_OTIMIZACAO_COPILOTO.md`, `functions/DEPLOY.md`, `functions/DEPLOY_FIREBASE.md`) convertida para o formato OKF (frontmatter `type`/`title`/`description`/`tags`/`timestamp` + estrutura hierárquica com `index.md`).
- **2026-06-17** — Removido `copiloto/handoff-otimizacao-2026-06-11.md`: era um handoff pontual de uma tarefa já concluída, não conhecimento de referência. Handoffs desse tipo seguem vivendo em PRs/commits, não no bundle OKF.
- **2026-06-17** — Adicionada categoria `arquitetura/` (schema do Firestore, mapa de Cloud Functions, convenções de frontend) para orientar desenvolvedores e agentes de IA externos. Adicionado `manutencao.md`, definindo gatilhos obrigatórios de atualização desses documentos sempre que a estrutura do sistema mudar.

- **2026-09-03** — Fase 0 do plano de evolução do Hermes ("Jarvis"), PR 3: dois detectores reativos de WhatsApp para a fila `atencao` (`functions/atencao_whatsapp.py`, novo), acionados por trigger Firestore em `whatsapp_messages` em vez de cron — a latência importa aqui. `promessa_sem_retorno`: reconhece quando o dono se compromete a responder algo ("deixa comigo", "te aviso ainda hoje", ...) e abre um registro em `promessas_abertas` (nova coleção); uma mensagem sua seguinte, longa ou com mídia, fecha o compromisso, e o scheduler `vencer_promessas` (a cada 15 min) cria o item na fila quando o prazo (padrão 4h) estoura sem fechamento — uma resposta do dono depois disso resolve o item sozinho, único caso em que um detector fecha item sem intervenção. `audio_relevante`: um áudio recebido em conversa vinculada a uma ação ativa (por `tarefas.whatsapp_vinculos` ou, em chat individual, pelo par `perfil_pessoas.whatsapp_chat_id` + `interacoes_pessoas.tarefa_id`) vira item da fila para consolidação posterior — nunca transcreve nada sozinho; áudios em sequência no mesmo chat (< 10 min) mesclam num item só. Extraída também `atencao.mapear_acoes_ativas_por_chat`, função compartilhada para a varredura de `tarefas` por chat vinculado, hoje usada só pelo detector de áudio (o detector `aguardando_terceiro_vencido` do PR 2 mantém sua própria varredura por ora, para não tocar em código já mergeado). Ambos atrás de flag em `system/settings.atencao` (padrão desligado). Nota de débito técnico: `services/whatsapp-capture/index.js::captureMedia` ainda não grava a duração do áudio (`media.duration_seconds`) — o detector trata duração desconhecida como relevante em vez de silenciar por falta de dado; gravar esse campo no worker fica para outra PR. 18 testes novos em `functions/test_atencao_whatsapp.py`, lógica de detecção pura e sem LLM, mesmo padrão de `atencao.py` (PR 2).
