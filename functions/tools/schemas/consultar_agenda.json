{
  "name": "consultar_agenda",
  "description": "Consulta eventos e compromissos na agenda do Google Calendar",
  "parameters": {
    "type": "object",
    "properties": {
      "data_inicio": {
        "type": "string",
        "description": "Data de início no formato YYYY-MM-DD"
      },
      "data_fim": {
        "type": "string",
        "description": "Data de fim no formato YYYY-MM-DD"
      }
    },
    "required": ["data_inicio", "data_fim"]
  }
}
