{
  "name": "consultar_contatos_prioritarios_secretario",
  "description": "Lista os briefings prioritários cadastrados para o Modo Secretário no WhatsApp.",
  "parameters": {
    "type": "object",
    "properties": {
      "apenas_ativos": {
        "type": "boolean",
        "description": "Se True (padrão), lista apenas briefings com status 'ativo' e dentro da validade. Se False, lista todo o histórico."
      }
    }
  }
}
