{
  "name": "consultar_historico_acoes",
  "description": "Busca ações, tarefas e projetos no Hermes com pesquisa flexível por frase natural, termos aproximados, sinônimos, erros de digitação e número de processo",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Frase natural ou termos de busca. Pode incluir palavras aproximadas, sinônimos, erros de digitação ou número de processo (ex: 'contratação de mobiliário para som e eventos', 'relatório financeiro', '23543.000286/2026-39')"
      },
      "area_tematica": {
        "type": "string",
        "description": "Área temática para filtrar (ex: 'FINANCEIRO', 'TI', 'RH')"
      },
      "status": {
        "type": "string",
        "description": "Status da ação: 'em andamento', 'concluída', 'cancelada'"
      },
      "data_limite_inicio": {
        "type": "string",
        "description": "Data inicial no formato YYYY-MM-DD"
      },
      "data_limite_fim": {
        "type": "string",
        "description": "Data final no formato YYYY-MM-DD"
      },
      "ultimas_n_acoes": {
        "type": "integer",
        "description": "Número máximo de ações a retornar (padrão 20)"
      }
    },
    "required": [
      "query"
    ]
  }
}