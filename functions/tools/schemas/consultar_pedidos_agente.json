{
  "name": "consultar_pedidos_agente",
  "description": "Lista pedidos de trabalho autônomo enfileirados pelo sistema para o agente executar.",
  "parameters": {
    "type": "object",
    "properties": {
      "tipo": {
        "type": "string",
        "description": "Filtrar por tipo de pedido (ex.: 'consolidar_audio'). Opcional."
      },
      "limite": {
        "type": "integer",
        "description": "Número máximo de pedidos a retornar (padrão 20, máx. 50)."
      }
    },
    "required": []
  }
}
