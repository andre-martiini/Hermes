{
  "name": "salvar_memoria_global",
  "description": "Salva um fato durável ou preferência permanente na memória global",
  "parameters": {
    "type": "object",
    "properties": {
      "fato": {
        "type": "string",
        "description": "O fato ou preferência a ser memorizado (ex: 'André prefere respostas curtas')"
      },
      "categoria": {
        "type": "string",
        "description": "Categoria do fato (ex: PREFERÊNCIA, REGRA, CONTATO, PROCESSO)"
      }
    },
    "required": ["fato", "categoria"]
  }
}
